# @gt-selection/db-types

Generated, API-safe PostgreSQL TypeScript types for the exposed `api` schema. (Currently
generated via the Supabase CLI against the local dev database; the generator moves to the AWS
Aurora dev database as part of the D-012 platform migration — the generated types themselves are
platform-independent.)

Generated output will be replaced through the repository type-generation command and must not
be edited manually.
