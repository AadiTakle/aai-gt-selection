# Browser-QA artifacts (screenshots are QA-only, not source of truth)
qa-screenshots/

# QA harness local install (standalone tool; not an app dependency)
qa/node_modules/
qa/package-lock.json

# QA harness machine-local run outputs (regenerated by the scripts)
qa/results.json
qa/flow.json

# Stage 2 review window build output: the real engine/scoring/phase2 modules compiled to browser
# ES modules, plus the item data split out of the banks. Deliberately NOT committed — a checked-in
# compiled copy is a second copy of the selection rule and the ability fit, free to drift from the
# sources it was built from. Regenerate with `pnpm stage2:review:build`.
stage2-build/
