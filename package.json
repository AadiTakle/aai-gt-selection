{
  "name": "gt-selection-capstone",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-root shortcuts. There is nothing to install here: every script below delegates into product/screener/, which is the npm project. archive/ is a separate pnpm workspace with its own scripts.",
  "scripts": {
    "ui": "npm --prefix product/screener run ui --",
    "test": "npm --prefix product/screener test",
    "typecheck": "npm --prefix product/screener run typecheck",
    "dev": "npm --prefix product/screener run dev",
    "sim": "npm --prefix product/screener run sim",
    "verify": "npm --prefix product/screener run verify",
    "planner": "npm --prefix product/screener run planner",
    "review": "npm --prefix product/screener run review"
  }
}
