# Measurement Registry

62 measurements. Canonical source of truth is `measurements.json`; this file is generated. Each question type references these IDs and may propose additions (merged here).

| ID | Name | What | Tail-precision usefulness | How to collect | How much |
|---|---|---|---|---|---|
| M-ACC | Item accuracy | Correct / incorrect (dichotomous) on a scored item. | The base IRT signal; at the tail, accuracy on very hard items is the primary separator once difficulty is high enough to avoid ceiling. | Log final response vs key per scored item. | >=8-12 scored items per construct per child near the child's ability for a stable theta; more hard items near the cut. |
| M-POLY | Partial-credit (polytomous) score | Graded score for multi-step / multi-part items (0..k). | Extracts tail information from a single rich item (a partial-credit item behaves like several dichotomous ones), sharpening theta with fewer items. | Define ordered scoring categories per item; log category reached. | >=4-6 polytomous items per construct; each contributes more information than one dichotomous item. |
| M-RT | Response time (total) | Milliseconds from item onset to submitted answer. | Combined with accuracy it separates fast-accurate (able) from slow-accurate; feeds effort-moderated and joint speed-accuracy models. | Timestamp item render and submit; store ms. | >=20 RT samples per child for a usable per-child RT distribution; log on every item. |
| M-RTFIRST | First-action latency | Milliseconds from item onset to the child's first interaction. | Indexes planning/encoding before acting; high-ability reflective solvers often show longer first-action latency with fewer revisions. | Timestamp first pointer/key/drag event. | Every interactive item; >=15 samples for a stable per-child median. |
| M-RTVAR | Intra-individual RT variability | SD or coefficient of variation of RT across items. | Consistency (low variability) tracks ability better than peak speed (worst-performance rule); a strong, less-coachable tail signal. | Compute SD/CV over the child's RT series (correct trials). | >=20-30 RT samples for a reliable CV; do NOT reward raw speed. |
| M-LAPSE | Lapse rate | Frequency / magnitude of the child's slowest responses (worst-performance). | The slowest responses, not the fastest, are most diagnostic of ability; fewer lapses => higher ability. | Take the slowest quartile/decile of RTs; count lapses beyond a threshold. | >=25 trials so the slow tail of the RT distribution is estimable. |
| M-REV | Revisions / answer changes | Count of answer changes, undos, or re-drags before commit. | Distinguishes confident tail performers (few, targeted revisions) from guess-and-check; adds process variance beyond right/wrong. | Increment a counter on each answer change / undo event. | Every item; interpret relative to item difficulty. |
| M-PATH | Solution path / move sequence | Ordered sequence of actions taken to reach the answer. | Strategy signature: efficient/novel paths mark strong reasoners; supports evidence-model (Bayes-net) scoring that separates HOW, not just WHETHER. | Append each action {t, action, state} to an event log per item. | Log fully on constructed/interactive items; needs >=5 such items per construct to characterize strategy. |
| M-EFF | Efficiency vs optimal | Moves or time used relative to the optimal solution for the item's difficulty. | Near-optimal solving at high difficulty is a clean tail separator; catches able children who are also economical. | Compare M-PATH length/time to a stored optimal for the item. | >=5 items with known optima per construct. |
| M-HINT | Scaffold / hint usage | Count and timing of hints or scaffolds the child invokes. | Hint-free solving at difficulty is stronger evidence; dynamic-assessment signal (learning rate) can add incremental tail validity. | Log each hint request with timestamp and item state. | Every item that offers hints; interpret against difficulty. |
| M-PERSIST | Persistence / time-on-task | Time spent engaging a hard item before solving or giving up. | Task commitment (Renzulli) is a construct CogAT misses; persistence on hard items adds non-cognitive tail signal and flags disengagement. | Time from onset to solve/skip on items above the child's level. | >=6 above-level items per child. |
| M-ERRTYPE | Error type | Classification of errors: near-miss / systematic / random / motor-slip. | Near-misses at high difficulty indicate latent ability just below threshold, refining tail placement; filters motor/attention noise. | Tag the chosen wrong answer against a distractor taxonomy. | Every incorrect response; needs distractors designed with error meaning. |
| M-CONF | Confidence / wager | Elicited confidence rating or points wager on the answer. | Metacognitive calibration (accuracy of confidence) adds incremental validity and helps down-weight lucky guesses at the tail. | Optional quick confidence tap or wager after answering. | On a subset (>=8 items) to avoid burden; not every item. |
| M-EXPLORE | Exploration behavior | Number of options sampled / states explored before committing. | Systematic exploration vs flailing separates strategic tail solvers; useful in open/interactive items. | Count distinct states/options visited via M-PATH log. | Interactive items only; >=5 per construct. |
| M-PROG | Progress trajectory | Partial progress achieved over a multi-step item over time. | Turns one hard item into a curve; the shape (fast-to-plateau vs steady) adds tail resolution beyond final score. | Snapshot state/score at intervals or per sub-step. | Multi-step items; >=4 per construct. |
| M-SPEEDACC | Speed-accuracy tradeoff | Joint position on the RT x accuracy plane. | Models the child's chosen operating point so speed is only credited when accuracy is held; prevents rewarding impulsive fast-wrong responding. | Pair M-RT with M-ACC per item; fit per-child SAT. | >=20 items with both signals. |
| M-DIFFREACH | Ceiling / max difficulty reached | Highest item difficulty answered correctly. | Directly probes the tail; adaptive escalation to failure pinpoints the child's edge and is the sharpest single tail statistic. | Track max b-parameter solved under adaptive escalation. | Requires an item bank extending well above grade level (above-level items). |
| M-DRIFT | Performance drift (fatigue/engagement) | Change in accuracy/RT across the session. | Detects disengagement/fatigue that would bias a tail decision; lets scoring discount low-effort late items. | Regress accuracy/RT on item position within session. | Whole session; needs >=30 items to estimate a slope. |
| M-ENGAGE | Engagement / off-task | Idle time, focus loss (blur), and re-engagement latency. | Gates all other signals: a tail decision is only valid on engaged responses (supports the 'speed as evidence only when engaged' principle). | Log window blur/idle timers and time-to-resume. | Continuous background logging across the session. |
| M-CONSIST | Cross-item consistency | Reliability of responses across parallel/equivalent items. | Reduces conditional standard error at the cut; inconsistent responders near the cut get more items before a decision. | Compare responses to matched parallel items (same b, cloned via AIG). | >=3 matched pairs near the child's ability. |
| M-RESUME | Post-failure resumption | Post-failure resumption | How a child responds to failure (retries, latency to re-attempt, voluntary-quit timing on hard items) is a task-commitment signal invisible to accuracy; near-cut children who re-engage after failure differ meaningfully from those who quit. | On above-level or near-unsolvable items, log each retry, the latency from failure to next attempt, and any voluntary-quit event with its timestamp. | >=6 hard/near-unsolvable items per child so retry and quit behavior is estimable. |
| M-IDEAFLU | Idea fluency | Idea fluency | Count of distinct valid ideas in an open divergent task; fluency is the most reliable single divergent-thinking index and higher-ability children generate more, providing a continuous creativity axis CogAT lacks. | Count non-duplicate on-topic responses per prompt (dedupe near-identical entries). | >=3 prompts, roughly 2-3 minutes each, to stabilize the fluency estimate. |
| M-FLEX | Category flexibility | Category flexibility | Number of distinct semantic/functional categories the child moves among; flexibility separates truly divergent thinkers from fluent-but-repetitive ones and is harder to coach than raw count. | Cluster responses into categories (functional, physical, imaginative) and count distinct categories and switches. | Same open prompts as fluency; needs a category scheme or embedding model. |
| M-ORIG | Statistical originality | Statistical originality | Infrequency of a response against a reference norm bank; rare-but-appropriate ideas are the cleanest creativity tail signal and mark the Renzulli creativity ring. | Score each response's frequency against a norm corpus; low frequency plus on-topic appropriateness scores as original. | Requires a norm bank per prompt; >=3 prompts per child. |
| M-ELAB | Elaboration | Elaboration | Amount of detail/embellishment added to an idea or figure (extra words, sub-features, strokes); adds incremental creativity variance beyond how many ideas are produced. | Count added descriptors/words per verbal idea or strokes/added features per figural response. | Open verbal and figural tasks; interpret relative to time on task. |
| M-QUERY | Information-seeking / question count | Information-seeking / question count | Number and quality of questions asked or information-requests made during a novel activity; a direct behavioral index of epistemic curiosity that reasoning tests miss and that predicts intrinsic drive to learn. | Count question-card taps or composed questions and rate their depth (surface fact vs causal/why question) during a mystery activity. | >=1 novel exploration activity of 2-3 minutes; more activities improve reliability. |
| M-UNCERT | Uncertainty preference | Uncertainty preference | Proportion of choices for uncertain/novel over certain/known options; a preference for informative uncertainty behaviorally marks curiosity-driven explorers and is hard to fake. | Present repeated certain-vs-uncertain choices; compute the proportion of uncertain picks and how it shifts as uncertainty is resolved. | >=10 certain-vs-uncertain choice trials per child. |
| M-CHOICE | Free-choice engagement | Free-choice engagement | Time and voluntary actions spent on an activity after the required portion ends and stopping is free; the canonical behavioral intrinsic-motivation measure, avoiding self-report bias and predicting the sustained engagement behind gifted achievement. | After a 'you're done' gate with an always-visible exit and no rewards, log continued time, count of self-initiated actions, and variety of self-directed play. | >=1 free-choice window of 60-120 seconds; multiple windows across activities improve reliability. |
| M-DELAY | Delay-of-gratification wait time | Delay-of-gratification wait time | Time willingly waited (or the choice to delay) for a larger-later reward; a self-regulation/future-orientation signal that predicts achievement independent of IQ and shows the most variance in young children where reasoning tests are least reliable. | On a growing-reward wait task with an always-available cash-out, log total wait time, the delay choice, and any strategic self-distraction behaviors. | 1-3 wait trials (a single well-designed trial can be long and informative). |
| M-EFFALLOC | Effort allocation ratio | Effort allocation ratio | Proportion of free time spent on a beneficial-but-tedious task versus an available fun distraction; a behavioral academic-diligence/self-discipline signal that predicts grades beyond IQ and is far less fakeable than conscientiousness self-report. | In a dual-panel task-vs-distraction block, log time and actions on the diligent task divided by total, plus sustained on-task stretch lengths. | A single 3-5 minute free-allocation block yields a usable ratio; more blocks improve reliability. |
| M-RUBRIC | Rubric-scored product quality | Rubric-scored product quality | Polytomous expert/consensual rating of an open product on originality, reasoning, and elaboration; captures authentic creative-productive giftedness (Renzulli Type III) that keyed items miss and adds ecological tail signal. | Score the stored product and reflection with an anchored rubric using multiple trained raters; report the mean and inter-rater reliability. | >=1 product per child; >=2 raters per product to establish inter-rater reliability. |
| M-CARELESS | Careless / invalid self-report flag | Careless / invalid self-report flag | A validity index for self-report (straight-lining, too-fast responses, inconsistent reverse-coded pairs, infrequency-item failures, social-desirability lean) that gates non-cognitive self-report so faked or careless questionnaires do not distort tail decisions. | Log per-item response time and response variance, compare reverse-coded pairs for consistency, and check infrequency/attention items; combine into an invalidity score. | Whole questionnaire; requires several reverse-coded pairs and at least one infrequency item. |
| M-RULEID | Rule dimensionality solved | Number of independent relational rules or attributes the child integrated simultaneously in the correct answer (e.g., a 3x3 matrix solved under three co-acting rules). | Separates children by the relational complexity they can bind at once (Halford). Two children may both be correct, but at 1 vs 3 co-acting rules; exposing that structure sharpens tail placement where a single difficulty index blurs it, and it is harder to coach than surface speed. | Tag each item with its rule set and design distractors so each foil violates exactly one named rule; the chosen option reveals which rule-dimensions were satisfied. Log the maximum simultaneously satisfied rules on correct high-complexity items. | >=6 items whose distractors are orthogonally keyed to individual rules, spanning 1 to 4 co-acting rules near the child's edge. |
| M-HYP | Hypothesis-search efficiency | Informativeness of the child's exploratory choices during rule discovery: how quickly errors are eliminated and whether placements test maximally disambiguating cases versus redundant or random ones. | In rule-discovery tasks, able children run efficient disambiguating tests and converge fast; this indexes inductive/scientific reasoning and dynamic-assessment learning rate, adding tail signal beyond final accuracy and resisting coaching. | From the placement/action log, compute the reduction in the viable-rule space per action (or trials-to-criterion after the first informative feedback) and flag perseverative or random tests. | >=1 rule-discovery item with >=6 feedback-bearing actions; aggregate over >=3 such items for a stable rate. |
| M-COMBO | Accuracy-gated speed combo | Accuracy-gated speed combo | Rewards sustained joint speed+accuracy rather than impulsive peak speed; a coach-resistant index of controlled processing efficiency that separates children who hold both at pace from those who trade one for the other. | Increment a streak on each response meeting BOTH a correctness check and a per-child adaptive RT band; reset to 0 on any error, false alarm, or timeout; store max streak and the streak distribution. | >=30 speeded trials so streak length is estimable. |
| M-FALSEALARM | Commission / false-alarm rate | Commission / false-alarm rate | Isolates impulsive fast-but-wrong responding so raw speed is never rewarded; separates careful-fast (able) from impulsive-fast responders and underlies the concentration-performance index. | Count taps on non-target/foil stimuli plus anticipatory responses faster than a plausible floor (<150 ms), divided by response opportunities. | >=40 target/non-target events with a meaningful foil rate (>=30% non-targets). |
| M-HICKSLOPE | Hick's-law RT slope | Hick's-law RT slope | A device- and base-motor-robust estimate of information-processing RATE: the intercept absorbs motor/device latency while the slope reflects central processing, so it is less confounded than absolute RT at the tail. | Administer blocks at 1, 2, and 4 response alternatives; regress mean correct RT on log2(N) and store slope and intercept. | >=15 correct trials per set size (>=45 total) for a stable slope. |
| M-ITTHRESH | Inspection-time threshold | Inspection-time threshold | A near motor-free, practice-resistant index of perceptual intake speed - the processing-speed measure with the cleanest (still modest) link to g and the least motor/device confound, making it the most defensible single speed adjunct at the tail. | Staircase the pre-mask exposure duration (e.g., 2-down/1-up) in a backward-masked two-alternative discrimination and estimate the exposure at the target accuracy (~71-75% correct). | >=40-60 masked trials for a stable staircase threshold; report threshold in ms alongside the measured display frame time. |
| M-PAE | Placement error (percent absolute error) | Placement error (percent absolute error) | Continuous placement error turns a single number-line item into a fine-grained latent-magnitude signal; small, consistent PAE at hard scales (fractions, wide ranges) separates top reasoners far better than dichotomous accuracy and avoids ceiling. | Record released position, convert to a value, compute absolute(placed - target) / line-range as a percent per item; also fit the child's placement curve (linear vs log R^2). | >=10-15 placements spanning the scale for a stable PAE and linearity estimate. |
| M-WEBER | ANS acuity (Weber fraction) | ANS acuity (Weber fraction) | Estimates the finest numerical ratio a child can reliably discriminate; a less-coachable, low-literacy tail signal that keeps separating children when symbolic items would floor or ceiling, especially in K-1. | Adaptively vary the ratio of the two non-symbolic arrays (staircase), fit a psychometric function to accuracy-by-ratio, and report the Weber fraction w and its standard error. | >=30-40 non-symbolic trials spanning ratios ~1.1 to 2.5 for a stable w. |
| M-EQREL | Relational-vs-operational equivalence signature | Relational-vs-operational equivalence signature | Flags whether a child treats '=' relationally (balances both sides) or operationally (dumps a running total), a research-backed early separator of algebra-ready reasoning that dichotomous accuracy hides; concentrates information exactly where pre-algebraic talent emerges. | On non-canonical equivalence items, classify the child's first substantive placement/entry against known operational-error targets vs the relational solution, and record whether they self-correct. | >=4-6 non-canonical equivalence items to reliably classify the child's dominant strategy. |
| M-PROPSTRAT | Proportional strategy signature (multiplicative vs additive) | Proportional strategy signature (multiplicative vs additive) | Classifies whether the child preserves the ratio (multiplicative/scaling) or a constant difference (additive) during adjustments, the decisive divide in proportional reasoning that a correct/incorrect score cannot see; isolates genuine proportional reasoners at the tail. | From the move sequence, fit whether successive adjustments hold the quantity-ratio constant vs the quantity-difference constant, classify the strategy, and record consistency across items. | >=4-6 proportion items including non-integer/non-doubling targets to classify strategy reliably. |
| M-ROTSLOPE | Mental-rotation RT slope | Slope of correct-trial response time regressed on target angular disparity. | The classic Shepard-Metzler chronometric signature; a shallow slope held at high accuracy is a rate-of-mental-transformation measure that is hard to coach and finely separates the spatial tail. | Present matched trials at several angular disparities and regress correct-trial RT on angle per child. | >=12 trials spanning >=3 disparities for a stable slope estimate. |
| M-MIRRORFA | Mirror false-alarm rate | Rate of selecting an enantiomorph (mirror-image) foil instead of a true rotation. | Distinguishes genuine 3D mental rotation from partial feature-matching; low mirror false alarms at high disparity is a clean tail separator. | Include exact mirror foils and log the chirality of the chosen distractor. | >=8 trials that contain a mirror foil. |
| M-VIEWANG | Perspective-taking angular error | Signed angular error between the child's indicated heading/direction and the true direction to the target from an imagined viewpoint. | Grades perspective responses far more finely than right/wrong, and its systematic component quantifies residual egocentric bias; low angular error at large viewpoint offsets is a navigation-linked tail signal standard batteries omit. | On pointing/heading items, record the dial angle and subtract the geometric correct heading; decompose into random and egocentric-bias components. | >=10 pointing trials across a range of viewpoint offsets for a stable error and bias estimate. |
| M-VOCABLVL | Lexical frequency ceiling | Lexical frequency ceiling | Vocabulary breadth is one of the strongest, most stable verbal-ability correlates; the rarest word-frequency (Zipf) band a child reliably handles separates gifted lexical depth once common words ceiling. An adaptive staircase individualizes the frontier, making it less coachable than a fixed word list. | Run an adaptive frequency staircase over frequency-tagged word items (receptive vocab, opposites/synonyms, context cloze); record the lowest-frequency (rarest) band answered correctly at criterion. | >=15-20 adaptive vocabulary items spanning several frequency bands to localize the ceiling with acceptable error. |
| M-LURETYPE | Distractor lure profile | Distractor lure profile | Which class of distractor (phonological cousin, category/semantic cousin, thematic associate, surface match) pulls a child's errors reveals the grain of their semantic/relational discrimination; category-cousin near-misses at high difficulty indicate latent ability just below the cut, refining placement beyond right/wrong. | Tag every distractor with a lure class; on each error log the lure class chosen; aggregate the per-child error-by-lure distribution. | Every incorrect response; requires items built with a designed lure taxonomy; >=10-15 errors for a stable profile (pooled across verbal types). |
| M-INFDEPTH | Inference depth level reached | Inference depth level reached | Comprehension discrimination at the tail is about inference layer, not literal recall; recording the deepest layer answered correctly (literal -> local/bridging inference -> global/thematic -> author's-purpose/evaluative) cleanly separates strong comprehenders who reach evaluative inference from those who plateau at literal. | Tag each comprehension/absurdity/cloze probe with an inference-depth level; record the maximum level answered correctly at criterion within a passage/unit. | >=4-6 probes spanning depth levels per passage/unit; several passages to stabilize the estimate. |
| M-ORALSPAN | Oral-direction condition span | Oral-direction condition span | The maximum number of simultaneous spoken conditions a child can integrate before dropping one is a sharp, near-language-neutral tail separator that jointly loads receptive language and verbal working memory; gifted children reliably cross-reference 4-5 embedded constraints. | Adaptive condition-count staircase on following-oral-directions items; record the highest condition count satisfied on all constraints at criterion; log which single condition is dropped on near-misses. | >=12-16 adaptive direction items across condition counts; report span plus the near-miss condition-drop pattern. |
| M-SPAN | Memory span threshold | Memory span threshold | The staircase-estimated longest sequence a child reliably reproduces is a continuous, fine-grained capacity index that keeps separating high performers who all hit ceiling on fixed-length items. | Track the highest sequence length reproduced correctly under a 1-up/1-down span staircase; average the last several reversal lengths. | 6-10 adaptive trials per mode (forward/backward) for a stable threshold. |
| M-MANIPCOST | Manipulation cost | Manipulation cost | The drop from forward (storage) span to backward/reorder (manipulation) span isolates central-executive manipulation, which tracks fluid reasoning better than raw storage at the tail. | Compute forward max span minus backward or sorted max span under matched staircases. | Enough trials in each mode to estimate both spans (6-10 each). |
| M-DPRIME | Signal-detection sensitivity (d-prime) | Signal-detection sensitivity (d-prime) | Separates true match-detection sensitivity from response bias in continuous tasks, so a cautious child and a guesser are not scored alike near the cut. | From hits and false alarms compute d-prime = z(hit rate) minus z(false-alarm rate) with standard edge corrections. | >=20 target and >=20 non-target trials for a stable estimate. |
| M-UPDATECOST | Updating cost | Updating cost | The accuracy or RT decrement per additional to-be-updated item or level indexes the efficiency of gating and replacing WM contents, a purer executive signal than static span. | Regress accuracy (or RT) on memory load / update count and take the slope. | >=4 load levels with several trials each. |
| M-POSTERR | Post-error adjustment | Post-error adjustment | Slowing and accuracy recovery after an error index performance monitoring; adaptive post-error slowing (not rigid or absent) marks metacognitive control at the tail. | Mean RT on trials immediately after an error minus mean RT after correct trials; also track post-error accuracy. | >=10 error trials with valid following trials. |
| M-COMM | Commission-error rate | Commission-error rate | Failures to withhold on no-go/stop trials index prepotent-response inhibition; low commission rates at high go-probability distinguish strong inhibitory control from impulsivity in a way speed cannot. | Proportion of no-go/stop trials on which a response was executed. | >=15-20 no-go/stop trials (rare relative to go). |
| M-SSRT | Stop-signal reaction time | Stop-signal reaction time | The estimated latency of the covert inhibitory process is one of the least coachable executive metrics; shorter SSRT marks stronger response control at the tail. | Track stop-signal delay with a staircase and estimate SSRT (median go RT minus mean stop-signal delay). | >=30 go and >=15 stop trials for a usable estimate. |
| M-CONGEFF | Congruency/interference effect | Congruency/interference effect | Incongruent-minus-congruent RT (flanker/Stroop) isolates interference control independent of baseline speed; a small effect at high accuracy is a strong, hard-to-game tail marker. | Median RT on incongruent trials minus median RT on congruent trials (correct trials only). | >=20 correct trials per congruency condition. |
| M-SWITCHCOST | Task-switch cost | Task-switch cost | Switch-trial minus repeat-trial RT/accuracy indexes set-shifting efficiency; low switch cost with maintained accuracy is a clean flexibility signal that fixed-format tests miss. | Compare RT/accuracy on rule-switch trials vs rule-repeat trials in a mixed block. | >=15 switch and >=15 repeat trials. |
| M-PERSEV | Perseverative-error rate | Perseverative-error rate | Continuing to apply the old rule after a switch or silent rule change directly indexes failure to update task set; near-zero perseveration at high difficulty is a sharp executive marker. | Count post-switch errors whose response matches the previously correct rule. | >=6 post-switch trials per switch across several switches. |
| M-PROCACC | Processing-task accuracy (complex-span gate) | Processing-task accuracy (complex-span gate) | Accuracy on the interleaved processing task gates the validity of the storage score; it detects children who trade away processing to boost recall, protecting tail comparability. | Proportion correct on the interleaved processing judgments; enforce a minimum (e.g. >=80%) for a valid storage estimate. | All processing trials in a complex-span session (typically >=15). |
| M-BETWEENERR | Between-search errors | Between-search errors | Returning to a location that already yielded a token indexes failure to maintain and update a spatial WM record across searches; few between-errors at large set sizes is a strong tail separator. | Count revisits to boxes that have already produced a token in a self-ordered search. | Several rounds at set sizes 4-8. |
| M-SEARCHSTRAT | Search strategy score | Search strategy score | The degree to which searches follow a fixed, systematic sequence (few distinct starting boxes) reveals planful strategy use that predicts giftedness beyond error counts. | Count distinct boxes used to begin each new search (lower = more strategic); log the full move path. | Higher set sizes (>=6 boxes) across several rounds. |
