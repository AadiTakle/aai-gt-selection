# Browser-QA artifacts (screenshots are QA-only, not source of truth)
qa-screenshots/

# QA harness local install (standalone tool; not an app dependency)
qa/node_modules/
qa/package-lock.json

# QA harness machine-local run outputs (regenerated by the scripts)
qa/results.json
qa/flow.json
