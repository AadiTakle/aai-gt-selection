/**
 * Single source of truth for the screening instrument's product name (AX-05).
 * Rename the whole surface here. Born-synthetic prototype (D-016, R11): the
 * name brands a workflow demo, never a validated determination (R10).
 */
export const EXAM_NAME = 'Ascend';

/** Illustrative, low assessment fee shown in the prototype payment screen (USD). */
export const EXAM_FEE_USD = 15;

/** Default synthetic proctor policy version seeded for the demo. */
export const EXAM_POLICY_VERSION = 'exam-syn-v1';
