# Decision Log

## Rules

Record decisions that change scope, requirements, evidence standards, product direction, or major trade-offs. Do not use this log for routine implementation choices.

Each decision must include the requirements served, alternatives considered, evidence, consequences, owner, and date. Reversals create a new entry; do not rewrite history.

## Decisions

### D-001 — Establish canonical project governance

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Use the charter, requirements, rubric, traceability matrix, evidence register, decision log, and scope-exception log as the canonical governance package.
- **Requirements served:** R7, R8, R10, H9
- **Alternatives considered:** Rely on the BrainLift alone; rely on a PRD alone.
- **Rationale:** Research does not define operating scope, and a PRD can prematurely lock a solution. Separate governance makes boundaries explicit and reviewable.
- **Consequence:** All contributors must use the canonical reading order in `PROJECT_CHARTER.md`.
- **Owner:** Team lead

### D-002 — Keep project requirements solution-agnostic

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Requirements define outcomes and evidence standards, not a lottery, specific assessment, workflow, or architecture.
- **Requirements served:** R1–R10
- **Alternatives considered:** Adopt the BrainLift's capability-gated lottery as the required solution.
- **Rationale:** The BrainLift is a core resource, but its proposed solution is not the capstone requirement.
- **Consequence:** Preferred mechanisms must be justified during concept selection rather than treated as fixed.
- **Owner:** Team lead

### D-003 — Require requirement-ID traceability

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Every substantive plan, feature, experiment, or technical work item must map to at least one R/H requirement.
- **Requirements served:** R7, R8, R10
- **Alternatives considered:** Informal alignment during review.
- **Rationale:** Explicit IDs make scope drift visible before implementation.
- **Consequence:** Unmapped work is rejected or requires an approved scope exception.
- **Owner:** Team lead

### D-004 — Treat previous PRD sprint outputs as historical exploration

- **Date:** 2026-07-17
- **Status:** Superseded by D-007
- **Decision:** Retain `docs/prd-sprint/` for research and design reference, but do not treat it as an approved product direction or binding PRD.
- **Requirements served:** R7, R8, R10
- **Alternatives considered:** Delete the files; adopt them as the current PRD.
- **Rationale:** The work contains useful analysis but was created before the team selected a product direction.
- **Consequence:** Canonical governance documents override any conflicting material in that folder.
- **Owner:** Team lead

### D-005 — No product concept is currently ratified

- **Date:** 2026-07-17
- **Status:** Superseded by D-008
- **Decision:** Product discovery remains open until the team evaluates concepts against the development rubric.
- **Requirements served:** R1–R10
- **Alternatives considered:** Ratify the integrated pilot from the historical PRD sprint.
- **Rationale:** The team must own concept selection and trade-offs.
- **Consequence:** Agents may analyze or prototype concepts but must not describe one as approved without a new decision entry.
- **Owner:** Team lead

### D-006 — Scope the capstone to two people and four weeks

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Target a critic-ready synthetic-data prototype and evaluation evidence pack within four weeks and 40 human person-days, using parallel AI factories.
- **Requirements served:** R1–R10, H1–H10
- **Alternatives considered:** Production-ready admissions system; live pilot; full academic-year impact evaluation.
- **Rationale:** AI can compress research, implementation, testing, and documentation, but cannot remove stakeholder, psychometric, ethical, or elapsed-time constraints.
- **Consequence:** Production launch, live student data, final external approvals, and measured program impact are explicitly out of scope. The prototype must distinguish proven, simulated, assumed, and future work.
- **Owner:** Team lead

### D-007 — Distill and remove the historical PRD sprint

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Preserve reusable concept, critic-review, metric, guardrail, and scoring material in solution-agnostic canonical references, then delete `docs/prd-sprint/`.
- **Requirements served:** R7, R8, R10, H5, H9
- **Alternatives considered:** Retain the folder indefinitely; delete it without preserving reusable material.
- **Evidence:** `CONCEPT_OPTIONS.md`, `CRITIC_REVIEW_CHECKLIST.md`, `METRICS_AND_GUARDRAILS_LIBRARY.md`, and the updated development rubric.
- **Rationale:** The folder contained valuable analysis but also a large unratified PRD that could mislead contributors into treating Concept C as approved.
- **Consequence:** The distilled references preserve useful options and test patterns without carrying forward a product decision.
- **Owner:** Team lead
- **Supersedes:** D-004

### D-008 — Ratify Track B Talent Evidence Snapshot prototype

- **Date:** 2026-07-17
- **Status:** Approved
- **Decision:** Preserve the authorized Track A CogAT admissions rule and add a profile-aware Track B invitation for promising below-cutoff CogAT applicants. Final Track B eligibility combines the CogAT gate with a 10–15 minute parent/guardian Talent Evidence Snapshot requiring no new child work.
- **Requirements served:** R1, R5, R7–R10, H1, H2, H4, H7, H9, H10
- **Evidence:** E-005 and E-006 support caution around a single hard screen while preserving capability; E-019 shows nominations provide limited complementary information; E-021 supports replacing unstructured recommendations with structured evidence while noting transfer limits; E-023 supports structured rules over freeform holistic judgment. None validates the Snapshot for live GT eligibility, so approval is restricted to the synthetic prototype.
- **Evidence routes:** Primary route uses one or two existing artifacts per selected domain. Applicants without artifacts may use a bounded structured narrative fallback completed by a parent/guardian or other directly observing adult.
- **Review:** Artifact route receives two independent reviews and a blind third review on disagreement. Narrative fallback receives three independent blind reviews from the start. Majority classification controls.
- **Domain rule:** Any demonstrable talent domain may contribute when evidence also shows learning rate, transfer, abstraction, or comparable capacity relevant to thriving in GT. Domain prestige cannot affect the decision.
- **Alternatives considered:** Keep the current parent essay; require new child performance tasks; restrict Track B to academic domains; make artifacts mandatory; use one reviewer; use automated scoring.
- **Rationale:** The selected workflow reduces parent-prose bias, preserves access for applicants without recorded artifacts, retains asynchronous/domain-specific talent, and fits the no-additional-child-work and four-week constraints.
- **Consequences:** The exact CogAT band, battery-profile rule, domain anchors, rubric thresholds, service catalog, and allocation/evaluation method remain configurable/open. Track B eligibility does not itself establish program effect or guarantee a seat.
- **Owner:** Team lead
- **Supersedes:** D-005

### D-009 — Use Supabase and PostgreSQL for the prototype backend

- **Date:** 2026-07-17
- **Status:** Superseded by D-012
- **Decision:** Use Next.js with the local Supabase development stack and PostgreSQL for the four-week synthetic prototype.
- **Requirements served:** R7, R8, R9
- **Alternatives considered:** Firebase Emulator Suite.
- **Rationale:** The team selected PostgreSQL and Supabase in a separate technical discussion.
- **Consequences:** PostgreSQL stores synthetic application and workflow data; Supabase provides local APIs, authentication, storage integration, and role-based access. No live child data or production Supabase project is authorized.
- **Owner:** Team lead
- **Superseded by:** D-012 (retain PostgreSQL, replace the Supabase platform with AWS managed services). PostgreSQL — and therefore the row-level-security, definer-RPC, immutable-versioning, and deterministic-replay design — is unchanged; only the platform bindings move.

### D-010 — Ratify future two-stage evaluation design, MAP outcome, and MVP remedy/consent boundaries

- **Date:** 2026-07-18
- **Status:** Approved
- **Decision:** Ratify the following governance-sync clarifications, restoring the BrainLift's intended two-stage evaluation interpretation and aligning MVP scope with the RES-008/RES-009 findings: (1) any future evaluation of Track B is a two-stage design, not a single Track-A-vs-Track-B comparison — stage one randomizes offer-versus-not-offered among equally eligible Track B candidates to estimate the Track B initial-offer/package effect, and stage two compares treated Track B against treated Track A as a service-fit noninferiority comparison; neither stage, nor their combination, supports a claim that Track A and Track B have equal route-specific causal effects without a Track A counterfactual. (2) MAP is ratified as the selected common future outcome measure for this design, verified only with lightweight upper-tail and administration-consistency checks, never scored or used for eligibility. (3) The proposed non-offered-candidate follow-up is a fall/winter/spring MAP testing schedule, a proposed next-cycle application-fee waiver of about $100 for submitting all three MAP results, and a 2–3 minute structured questionnaire per MAP submission covering schooling, tutoring, adaptive software, enrichment, and MAP prep since the prior test. (4) The questionnaire is hidden from admissions and cannot affect eligibility or re-entry; the questionnaire and other post-assignment resource data are descriptive mechanism/substitution information for a future evaluator, not primary intent-to-treat controls. (5) Sufficient Track B applicant volume/oversubscription (needed for randomized offer-versus-not-offered) is treated as an external go-to-market assumption, not a product blocker. (6) Pooling cohorts across cycles and handling rule/rubric/rater version drift are future analyst responsibilities using existing operational metadata; the MVP adds no new applicant-facing fields to support this. (7) MVP decision remedies are limited to explanation and factual/procedural correction; a substantive rubric-application appeal and automated re-entry are deferred beyond MVP scope, though manual staff-initiated re-entry may remain. (8) All prototype Track A/Track B rules remain fictional, versioned, and tagged `synthetic_only=true` and `validated=false`. (9) Any future seat allocation and the future evaluation design described above remain strictly downstream of this MVP and outside its scope.
- **Requirements served:** R2, R3, R4, R6, R7, R8, R9, R10, H3, H5, H6, H8, H9
- **Evidence:** RES-008 (two-stage estimand correction), RES-009 (cohort pooling and version-drift analysis), RES-004 (explanation/correction/appeal boundary), RES-005 (privacy/consent separation for research fields)
- **Alternatives considered:** Ratifying a single Track-A-vs-Track-B comparison as an equal-effect causal claim; collecting the resource questionnaire as a decision-used or admissions-visible field; adding new MVP persistence fields for cohort pooling or version tracking now; implementing substantive rubric appeal and automated re-entry within the four-week MVP; leaving prototype rules unmarked or implicitly authoritative.
- **Rationale:** RES-008 confirms the BrainLift intended two distinct comparisons — a randomized intent-to-treat estimate and a non-randomized service-fit comparison — while the prior PRD wording collapsed them into one equal-effect causal claim; separating them preserves a valid future identification strategy without overstating what either stage can show. Treating MAP, the fee waiver, and the questionnaire as proposed and hidden from admissions keeps the eligibility product from being contaminated by future-outcome incentives while still recording enough design intent that a future evaluator is not foreclosed. Deferring substantive appeal, automated re-entry, and cohort-pooling fields keeps the four-week MVP scoped to explainable eligibility and correction rather than adjudication or longitudinal-analysis capacity the team cannot build or validate in the available time.
- **Consequences:** The PRD's "Allocation and Evaluation," "Future Evaluation Handoff," and "Correction, Appeal, and Re-entry" sections, and `B-08`, must state these boundaries; no claim of Track A/Track B causal equivalence, live allocation, or implemented substantive appeal/automated re-entry is authorized. Applicant-volume risk is explicitly not a reason to block MVP delivery. All prototype rules remain fictional/versioned/`synthetic_only=true`/`validated=false` until superseded by a live-validation decision.
- **Owner:** Team lead
- **Relationship to prior decisions:** Builds on D-008 (Track A/Track B prototype ratification) and D-009 (Supabase/PostgreSQL backend) by fixing the future evaluation and remedy boundaries those decisions left open; does not supersede either.

### D-011 — Approve the synthetic web application architecture and integration boundary

- **Date:** 2026-07-18
- **Status:** Approved
- **Decision:** Approve `docs/architecture/ARCHITECTURE_PLAN.md` as the technical architecture for the synthetic prototype: one Next.js App Router sub-application in a pnpm monorepo, local Supabase/PostgreSQL, framework-independent Zod contracts, generated API-only database types, fixed synthetic fixtures, forced-RLS database boundaries, deterministic replay seams, and no ORM. Use Mode A (a linked sub-application on its own origin) for the prototype integration default; retain Mode B (a reverse-proxy path mount) as the future production-shaped option. The synthetic application owns its own local authentication and does not share or infer a live host-site session.
- **Requirements served:** R1, R5, R7, R8, R9, R10, H1, H2, H4, H7, H9, H10
- **Evidence:** D-008, D-009, D-010; E-034, E-037, E-039–E-045; `PROVISIONAL_IMPLEMENTATION_CONTRACT.md`; `RLS_AND_AUTH_BLUEPRINT.md`; `CANONICALIZATION_AND_REPLAY_BLUEPRINT.md`
- **Alternatives considered:** Rewriting or absorbing the pre-existing GT website into the application; embedding authenticated flows in the host DOM; using a shared live-site SSO session in the MVP; adding a separate API service or ORM; linking a hosted Supabase project.
- **Rationale:** A bounded sub-application preserves the synthetic/privacy boundary while still allowing brand-compatible navigation from a pre-existing host site. The monorepo packages keep shared contracts and generated types independent of Next.js, while local Supabase exercises the real Auth/PostgREST/PostgreSQL boundary without authorizing live data or deployment.
- **Consequences:** Phase B may scaffold the application, package, database, test, and CI boundaries but may not implement admissions business logic, real uploads/data, hosted Supabase, live host integration, production deployment, allocation, or evaluator systems. Host-link capability, brand tokens, and any future SSO remain assumptions until confirmed by GT IT.
- **Owner:** Team lead
- **Relationship to prior decisions:** Implements D-009 and the MVP boundaries in D-008/D-010; does not supersede them. Its PostgreSQL/RLS/replay design is unchanged by D-012; only the platform bindings named in D-009 move to AWS.

### D-012 — Move the prototype backend platform from Supabase to AWS (retain PostgreSQL)

- **Date:** 2026-07-19
- **Status:** Approved
- **Decision:** Replace the Supabase development platform with AWS managed services for the synthetic prototype, **keeping PostgreSQL as the database engine**. The row-level-security firewall, `SECURITY DEFINER` RPC write surface, immutable successor versioning, hash-chained audit, reason-code explanations, and deterministic canonical replay are retained unchanged; only the platform bindings move. The canonical Supabase→AWS mapping is:

  | Concern | Supabase (D-009) | AWS (D-012) |
  |---|---|---|
  | Database engine | Supabase-hosted PostgreSQL | **Amazon Aurora Serverless v2 (PostgreSQL-compatible)** — same SQL, RLS, RPCs, replay |
  | Auth / identity | Supabase Auth (GoTrue) JWT | **Amazon Cognito** user pool; JWT carries a `custom:user_role` claim; identity is `sub` |
  | RLS principal binding | `auth.uid()`, `auth.jwt()->>'user_role'` | App verifies the Cognito JWT and sets request-scoped session GUCs (`SET LOCAL app.user_id`, `app.user_role`); RLS predicates read `current_setting(...)` |
  | DB client | `@supabase/ssr` request-scoped client | **`pg` (node-postgres)** via **Amazon RDS Proxy**, one transaction per request, connecting as a non-`BYPASSRLS` role |
  | Write/read API surface | PostgREST-exposed `api` RPCs | Same `api` schema `SECURITY DEFINER` RPCs, invoked over `pg` from Next.js Server Actions/route handlers |
  | Object storage | Supabase Storage | **Amazon S3** (private buckets, pre-signed URLs; no public objects) |
  | Compute / hosting | Supabase-adjacent / local | **Next.js container on Amazon ECS Fargate**, fronted by **CloudFront + ALB** |
  | Secrets / elevated access | service-role key | **AWS Secrets Manager** + IAM database authentication; no RLS-bypassing credential in the app runtime (the "by construction" control is retained) |
  | Infrastructure as code | Supabase CLI/config | **Terraform** (cloud-portable) |
  | Synthetic/dev safety | local-only, loopback fail-closed | **dedicated dev AWS account, synthetic-only, resource-tag/account-id fail-closed guard** (replaces the loopback guard) |
  | Website integration | own origin | **own CloudFront origin** (subdomain, or a path behavior on the host distribution) |

- **Requirements served:** R7 (auditability/replay preserved), R8 (feasibility/portability), R9 (student protection — synthetic-only, least-privilege, no RLS-bypass credential), R10 (claim boundaries unchanged)
- **Evidence:** D-009 (prior platform), D-011 (architecture it revises); `docs/architecture/ARCHITECTURE_PLAN.md`; `RLS_AND_AUTH_BLUEPRINT.md`; `PROVISIONAL_IMPLEMENTATION_CONTRACT.md`; `CANONICALIZATION_AND_REPLAY_BLUEPRINT.md`
- **Alternatives considered:** (a) DynamoDB + Lambda + Cognito (fully serverless/NoSQL) — rejected because it discards PostgreSQL RLS, forcing a full rewrite of the data model, auth firewall, and canonical replay, and reduces portability. (b) RDS Postgres + API Gateway/Lambda for the RPC layer — deferred; retained as a possible future refactor, but keeping the definer-RPC surface in-database minimizes churn now. (c) Staying on Supabase — overridden by the team's platform decision.
- **Rationale:** Keeping PostgreSQL preserves the security and replay invariants that most of the corpus depends on, so the migration is a binding swap rather than a redesign. Standard PostgreSQL + a container + Terraform keeps the system portable across clouds or self-hosting and cleanly attachable to a pre-existing website via a CloudFront origin, satisfying the "easy to migrate / move across platforms / hook up to an existing site" objective.
- **Consequences:** Documentation and stack descriptions across the canonical, active-contract, and code layers are updated to the AWS mapping. The **functional code migration** (replacing `@supabase/ssr`/`@supabase/supabase-js` with `pg` + Cognito verification and session GUCs, replacing the Supabase CLI dev loop, and adding Terraform) is a **tracked follow-up**, not completed by this decision; the existing Supabase prototype code remains runnable until that migration lands (see `ASSUMPTIONS_AND_EVIDENCE.md` A-AWS-1..3 and the backend backlog). Development uses a dev AWS account with synthetic data only; no live child data, production account, or public endpoint is authorized.
- **Owner:** Team lead
- **Supersedes:** D-009

### D-013 — Separate reusable account profiles from cycle applications and expand onboarding intake

- **Date:** 2026-07-20
- **Status:** Approved
- **Decision:** Structure pre-CogAT onboarding as (1) reusable, guardian-owned student/household profiles and (2) an immutable cycle-specific application snapshot. The synthetic MVP field set includes: student name, date of birth, gender, and current grade; requested entry year/grade; a versioned current-school directory selection with name/address autofill and school type; accommodation/support-plan selections and bounded details; disciplinary-sanction and non-health withdrawal disclosures with a conditional explanation; guardian relationship, primary household address, prior-GT relatives and names, and a home-language survey; household income and number of household members for financial-aid intake; and versioned acknowledgements, referral source, and signature. Current-school enrollment date and prior-school history are removed from this setup flow. The two general application essays are not collected or persisted.
- **Privacy/use boundary:** Identity, demographic, household, language, accommodation/support, discipline, finance, referral, and signature fields are purpose-separated private context. They are excluded from Track A/Track B eligibility inputs and hashes, hidden from eligibility reviewers, and available only to explicitly authorized operational roles. Household income and household size may support a future financial-aid process but cannot affect capability or eligibility. The setup flow does not collect W-2s or other proof documents; any future proof request occurs only after an authorized downstream admission/aid trigger and remains blocked by `B-08`.
- **Live-use boundary:** The prototype remains born-synthetic. “COPPA compliant” or equivalent legal-compliance language is not authorized; `B-06` still requires GT privacy/legal approval of notice, parental authority, consent, access, retention, deletion, security, and operator/data-flow facts before live child data.
- **Requirements served:** R1, R7, R8, R9, R10, H2, H4, H7, H9, H10
- **Evidence/assumptions:** Team-lead field direction dated 2026-07-20; E-032–E-040 and E-054–E-057. School-directory authority, field requiredness/vocabularies, financial definitions, retention, and live legal applicability remain open assumptions.
- **Alternatives considered:** Keep all setup data in one application JSON document; keep accommodation/language and financial intake deferred; collect W-2 proof during setup; treat every setup field as durable account data; or let contextual fields enter eligibility review.
- **Rationale:** Separating reusable profile facts from cycle-specific assertions reduces repeat burden while preserving the exact application snapshot that was signed. Purpose-separated storage makes the “store it but never use it for eligibility” boundary testable and avoids turning family resources, disability context, language, or advocacy into capability evidence. Deferring proof documents avoids collecting the highest-risk financial data before there is an authorized need.
- **Consequences:** The PRD, feature map, onboarding backlog, traceability matrix, and assumptions register must reflect the expanded synthetic field set. The current onboarding backend remains a verified create/save/submit/status foundation, not field-complete account setup. New profile/private-context storage, contracts, draft-read projection, RLS, and noninterference tests are required. B11B enables local synthetic frontend integration; B11A remains required only for AWS/live wiring.
- **Owner:** Team lead
- **Relationship to prior decisions:** Narrows and extends D-010/D-011/D-012 without changing their causal, allocation, or platform decisions. It supersedes only the prior PRD/D-010-derived statement that no household-income or household-size fields are persisted in the MVP; it does not authorize financial-aid decisions, admission/offer logic, W-2 collection, or live child data.

### D-014 — Organize the MVP around a six-stage family journey and four functional personas

- **Date:** 2026-07-20
- **Status:** Approved
- **Decision:** Make the PRD page-oriented around the family journey: account/application setup; external CogAT handoff; automatic initial routing; Track B artifact/narrative submission when invited; blind reviewer/supervisor workflow; and final eligibility/explanation/correction. The only functional MVP personas are family/guardian, admissions operator, reviewer, and blind review supervisor. Track A displays `eligible`, never accepted/admitted. A valid CogAT result and the final required review automatically execute the locked rule and publish the applicant-safe notice.
- **Reviewer/supervisor boundary:** Reviewers receive assigned queues/deadlines, blind evidence, the six-dimension rubric with citations, draft/locked submit, abstention, and a non-vote blocker action. Live artifact/narrative de-identification is deferred; fixed synthetic evidence remains the MVP boundary. Supervisors act only as blind slot-three voters under the artifact-disagreement/narrative rules, with no prior-vote access, assignment management, calibration authority, discussion, or override.
- **Admissions boundary:** Admissions checks completeness, enters/imports CogAT, resolves pending work, manages assignments/replacements, and applies factual/procedural corrections followed by a locked rerun. Admissions cannot directly override eligibility. In-app notifications are MVP; email notifications are an extension.
- **CogAT boundary:** The MVP assumes an external portal handoff. Portal/provider, authentication, scheduling, return/status, and result-exchange details remain part of `B-01` and must follow the confirmed current GT workflow.
- **Requirements served:** R1, R5, R7–R10, H1, H2, H4, H7, H9, H10
- **Evidence/assumptions:** Team-lead workflow decisions dated 2026-07-20; D-008/D-010/D-013; current contract blindness, abstention, blocker, and majority invariants. B-01/B-05/B-06/B-07 remain open for live operation.
- **Alternatives considered:** Topic-only PRD organization; embedded CogAT; manual routing/release; direct admissions override; supervisor adjudication/override; full applicant context for reviewers; functional auditor/privacy/accessibility/leader pages; MVP email notifications.
- **Rationale:** Page and persona boundaries give frontend/backend contributors one navigable workflow, while automatic locked transitions and blind role limits preserve reproducibility, applicant rights, and reviewer independence.
- **Consequences:** The PRD, feature map, architecture surface map, and traceability must show the six stages and four functional personas. Auditor/configuration, privacy-steward, accessibility-coordinator, and school-leader pages are deferred as functional UI, without removing their required backend/security controls or future feature-library stories.
- **Owner:** Team lead
- **Relationship to prior decisions:** Operationalizes D-008’s review mechanics, D-010’s claim/remedy boundaries, and D-013’s account/application lifecycle; it does not change Track A policy, allocation, causal evaluation, or production authorization.

### D-015 — Reframe the selection target to Timeback-fit (giftedness core) and adopt a scalable, tunable, GT-validated screener

- **Date:** 2026-07-24
- **Status:** Approved
- **Decision:** Reframe the selection target from "identify giftedness" to "identify applicants who are gifted *and* able to thrive and accelerate on the Timeback platform," with giftedness a necessary but not sufficient component. Adopt a scalable, tunable, GT-validated screener as a first-class deliverable (new R11) and reframe R5 to a capability-and-fit standard. Retain the counterfactual/lottery design (R2; D-010) unchanged as the program-effect evaluation arm — the screener defines the capable/fit pool, the lottery measures program effect. Record GT's current operational anchors — the multi-path admission rubric (high CogAT; or two MAP screeners above the 95th percentile; or a blended CogAT+MAP aggregate above the 90th percentile; or a 99th-percentile composite waiver) and the 85th-percentile fall MAP reading gate with an ESL/exceptional-cognitive exception — as documented validation references, not a locked cut. The screener must run algorithmically at applicant volumes in the thousands (GT Anywhere at scale), with a human shadow-day retained for behavioral fit. North-star context: the "MIT-ready by 8th grade, ~100,000 students" leadership goal (expansion-dependent; current cohorts ~40-46 physical + ~300 virtual).
- **Requirements served:** R1, R5, R8, R11; complements R2–R4, R6, R7, R9, R10; H1, H4, H10
- **Alternatives considered:** Keep the giftedness-only target; target Timeback-fit only (dropping giftedness); keep the charter counterfactual-only and treat the screener as "not predetermined."
- **Evidence:** E-096–E-102 (GT admissions-director interview, Crystal Martel, 2026-07-23, Otter transcripts pt.1/pt.2)
- **Rationale:** The GT admissions director specified that the platform serves a specific learner profile: some gifted students do not thrive on Timeback (twice-exceptional, ESL, heavy-repetition needs, non-academic prodigies), while some students who are not conventionally "gifted" accelerate on it. Optimizing the screener for platform fit (with giftedness necessary) matches what GT actually selects for and what its data can validate, while the retained counterfactual keeps program-effect claims honest and non-circular (R4, R10).
- **Consequences:** `PROJECT_CHARTER.md` (mission, goals, non-goals) and `docs/product/project-requirements.md` (core success, R5, new R11, non-requirements) are updated. Deferred, tracked follow-ups (not this pass): reframing the two brainlifts toward the Timeback-fit target (counterfactual brainlift 10K→100K figure and a forward note; reconciling the assessment-quality brainlift's Insight 14 "never a will-benefit prediction" — proposed to the author, not rewritten); designing the GT-data validation study (new-test vs CogAT/MAP overlap; Timeback-acceleration criterion); re-orienting the question-type catalog toward the Timeback-fit / learning-rate construct. Tracked as RES-012 and RES-013. Does not supersede D-008 (Track A/B), D-010 (two-stage evaluation), D-012 (platform), or D-014 (personas/journey).
- **Owner:** Team lead
- **Relationship to prior decisions:** Refines the charter mission and R5; retains D-008's Track A/Track B and D-010's evaluation design; does not change platform (D-012) or MVP persona/journey (D-014) decisions.
- **Transcription note (2026-07-30, D-032):** Decided 2026-07-24 on `feat/interview-scope-update` and transcribed here verbatim except for its evidence citation. The interview evidence was originally registered as `E-071–E-077`; those IDs were independently consumed on `dev` while this lineage was unmerged, so the seven entries are re-registered as `E-096–E-102` and the citation above re-pointed accordingly. No other wording is changed.

### D-016 — Build the R11 screener as a born-synthetic adaptive test sub-application

- **Date:** 2026-07-24
- **Status:** Approved
- **Decision:** Build the R11 screening instrument as a bounded, born-synthetic adaptive test sub-application inside the existing pnpm monorepo (Mode A per D-011), on the runnable local Supabase/PostgreSQL stack (D-012 keeps this code runnable until the AWS binding migration). Scope: (1) an item bank of interactive question types drawn from the `research/exam-question-types` catalog, each item carrying versioned IRT parameters; (2) an in-stack TypeScript IRT/CAT engine (`packages/cat-engine`) that estimates per-domain ability (theta), selects each next item by maximizing item information, applies an engagement gate and consistency weighting, and stops on a target standard error or item cap with exposure control; (3) private `app.*` tables + `api.*` `SECURITY DEFINER` RPCs + forced RLS for participants, sessions, item responses, raw telemetry events, and derived per-domain metrics, exposed only through RPCs and generated `api` types; (4) a Next.js test-taking surface that embeds the existing self-contained demos through a `postMessage` telemetry contract and drives the adaptive loop. All item parameters, stopping rules, and cut scores are versioned synthetic policy (`synthetic_only=true`, `validated=false`), tunable and owned by GT (R11).
- **Reconciliation with D-014:** This instrument is GT's own screening tool — **not** embedded CogAT and **not** a live testing integration. The D-014 external-CogAT handoff and the `api.record_assessment_version` import path are unchanged; CogAT remains GT's trusted signal and a validation anchor (E-099). The screener is a parallel R11 capability whose outputs feed the capability-and-fit decision only after GT-data validation (RES-012). It does not supersede D-014.
- **Requirements served:** R11 (primary), R5, R8; bounded by R7, R9, R10; advances H1, H4, H6, H10; complements R1.
- **Alternatives considered:** Embedded CogAT (rejected in D-014); a separate off-stack FastAPI/Python service (rejected — breaks the RLS/definer-RPC firewall, generated-type discipline, and D-012 portability); a client-only prototype with no backend (rejected — cannot persist metrics server-side or run server-side adaptive selection); deferring the build (rejected — R11/D-015 make the screener a first-class deliverable).
- **Evidence:** D-015 (R11 adoption), E-096/E-100 (GT validation data), `research/exam-question-types/` catalog + `measurements.json`; D-011 (Mode A architecture), D-012 (portable PostgreSQL design). No item parameters are empirically calibrated; initial IRT parameters are assumed/synthetic pending GT data (validated=false).
- **Consequences:** Remains born-synthetic per D-006/D-011/D-013 — no live child data, no production deployment, loopback-only, no service-role/elevated key in app runtime. The "production-oriented" build follows the repo's production-shaped RLS/definer-RPC/immutable-versioning design, portable to the D-012 AWS target; per-app containerization/Terraform is not added now. Pseudonymous test-takers use a proctor/admin-authenticated session-token pattern with no PII (participant records store only a synthetic pseudonym code). Adds feature IDs AX-01–AX-06 and traceability work item TECH-004; the question-type catalog re-orientation remains RES-013 and the validation study RES-012. Does not supersede D-014 (external CogAT), D-011/D-012 (architecture/platform), or D-006 (synthetic scope).
- **Owner:** Team lead
- **Relationship to prior decisions:** Implements R11/D-015; extends D-011's Mode A sub-application and D-012's portable PostgreSQL design; reconciles with D-014 without superseding it.
- **Transcription note (2026-07-30, D-032):** Decided 2026-07-24 on `feat/adaptive-exam-app` and transcribed here verbatim except for its evidence citations, re-pointed from `E-071/E-075` and `E-074` to `E-096/E-100` and `E-099` for the reason given in D-015. Two facts have moved on since the decision and are recorded here rather than edited into the text above: the engine package named `packages/cat-engine` now exists on `dev` as `packages/exam-engine` (selection and adaptive loop) plus `packages/exam-scoring` (metrics and deterministic scoring), and the per-item correctness authority moved into the database under D-027. Neither changes what was decided.

### D-017 — Baseline reading literacy is a required capability for the in-house screener (reading barrier; no audio)

- **Date:** 2026-07-24
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Treat baseline reading literacy as part of the capability the in-house adaptive screener requires, not as construct-irrelevant variance to be mitigated. All question-type instructions and stimuli are delivered as on-screen TEXT only — **never audio** — and a child who cannot read/understand the grade-appropriate text is intentionally screened out, on the rationale that the target program (reading-based, self-paced Timeback) would not have worked for a non-reader. K-1 is retained wherever a task can be conveyed in very simple, high-frequency words a beginning reader can interpret; the K-1 band is removed from types whose K-1 access previously depended on audio narration of language the child must comprehend (CX-sjt-01, VER-CLOZE-01, VER-EVIDENCE-01, VER-POLYSEME-01 → floor raised to grade 2). The barrier is hard: no audio, ELL, or 2e/dyslexia accommodations within the screener.
- **Requirements served:** R5 (defensible capability-to-benefit standard), R11 (scalable/tunable screener).
- **In tension with:** H4 (broaden who can demonstrate ability) and the charter's "capability, not privilege" / rights-before-research principles — a reading gate has foreseeable disparate impact by reading ability, which correlates with ELL status, SES, age, and disability (2e/dyslexia). Acceptable only if literacy is genuinely required to benefit from the program; must be stated, monitored for adverse impact, and never conflated with general ability.
- **Alternatives considered:** (a) Keep the prior wordless/audio-supported design (reading as construct-irrelevant variance to minimize) — rejected per the reading-based-program rationale; (b) drop K-1 entirely — rejected in favor of retaining K-1 with very simple language where feasible; (c) provide audio/ELL/2e accommodations — rejected for the screener per the hard-barrier direction (accommodation/alternate-pathway deferred).
- **Evidence:** E-071 (Crystal Martel stakeholder claim that Timeback requires baseline literacy, from the 2026-07-23 interview per team direction; the interview summary is not yet transcribed into the repo, so this is her belief/claim pending verification). No independent evidence yet establishes the specific literacy floor or that a reading gate does not exclude capable non-readers who could benefit.
- **Consequences:** `research/exam-question-types/DEMO_REBUILD_GUIDE.md` §3/§3b updated (reading required, no audio); catalog specs updated (reading policy on all 66 types; K-1 removed from the 4 audio-dependent verbal-comprehension types); demos present instructions as text. Born-synthetic status unchanged (`synthetic_only=true`, `validated=false`). Does NOT establish a validated cut, an accommodations policy, or legal/fairness clearance; an adverse-impact/DIF review and an accommodations/alternate-pathway decision remain open follow-ups.
- **Owner:** Proposed by product direction (session 2026-07-24); requires team-lead ratification.
- **Governance note:** This log currently ends at D-014, but the exam workstream references D-015 (Timeback-fit target) and D-016 (adaptive screener) in `research/exam-question-types/METRIC_FRAMEWORK.md`; those entries are not yet transcribed here. This entry uses D-017 to avoid collision; transcribing D-015/D-016 and reconciling the canonical log are open governance tasks.
- **Status of that note (2026-07-30):** Closed by D-032. D-015 and D-016 are now transcribed above, so the log no longer skips them; the D-017 number is kept as allocated. The literacy evidence this entry cites as `E-071` is `dev`'s own reading-literacy entry and is unaffected by the `E-096–E-102` re-registration.

### D-018 — Interim hosted deploy on Supabase Cloud behind an explicit opt-in flag

- **Date:** 2026-07-24
- **Status:** Proposed (awaiting team-lead ratification) — records an interim divergence from D-012's end-state, not a replacement for it.
- **Decision:** To get a working hosted deployment sooner than the full D-012 rebind allows, permit running the Next.js portal against **Supabase Cloud** (auth + Postgres RPC) in production, gated behind an explicit **`GT_DEPLOY_MODE=hosted`** runtime flag. Default behavior is unchanged and remains fail-closed (loopback-only Supabase, `NODE_ENV` dev/test, local synthetic adapter pinned to `:65421`). Hosted mode only relaxes the non-loopback URL guard (requiring https) and widens CSP `connect-src` to the Supabase origin; it does **not** relax the ban on service-role/secret keys in the app runtime. Hosting target remains ECS Fargate per D-012 (`apps/web/Dockerfile`, `infra/`, `.github/workflows/deploy.yml`).
- **Requirements served:** operational (get a reviewable live environment); does not alter any product requirement.
- **In tension with:** D-012, which ratifies the end-state as `pg`/Aurora + Cognito (not Supabase). This is an explicitly-temporary bridge; the Supabase→pg/Cognito rebind remains the tracked follow-up and is NOT superseded.
- **Alternatives considered:** (a) block all deploy until the full D-012 rebind lands — rejected as too slow for MVP demonstration; (b) silently loosen the env guards — rejected (safety-critical guards must stay default-on and opt-in must be explicit and logged).
- **Consequences:** `apps/web/src/lib/env.ts` gains `isHostedDeploy` + hosted branch (with tests); `next.config.ts` CSP `connect-src` is hosted-aware; `docs/DEPLOYMENT_RUNBOOK.md` and `infra/README.md` updated; `.github/workflows/deploy.yml` added (dormant until deploy secrets exist). Data remains born-synthetic until a separate decision authorizes real applicant data. No production account is authorized by this entry alone.
- **Owner:** Proposed by product direction (session 2026-07-24); requires team-lead ratification before pointing hosted mode at a real project.

### D-019 — Add one AWS Lambda for exam scoring & deterministic replay

- **Date:** 2026-07-26
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Satisfy the project's external "use at least one AWS Lambda" constraint by deploying the exam **scoring + deterministic-replay engine** as an event-invoked, re-invokable Lambda. On session completion the app invokes the Lambda with the session's response log; the Lambda holds the server-side answer keys, IRT/scoring primitives, a pinned item-parameter snapshot, and the session seed, and returns the score/classification; re-invoking it with the same inputs reproduces the result bit-for-bit. This is **additive to D-012**: Next.js on ECS Fargate, the in-database `SECURITY DEFINER` RPC write surface, Aurora/PostgreSQL, forced RLS, and immutable versioning are unchanged, and the RPC write surface is **not** moved to Lambda (D-012 deferred that; its rationale stands).
- **Requirements served:** R7 (the versioned Lambda is the deterministic canonical-replay / audit unit D-012 already requires), R1 (produces the selection-relevant score), H10 (isolates the keyed scoring engine and answer keys from the web/app tier). Also satisfies the external "≥1 Lambda" project constraint (E-072).
- **Alternatives considered:** (a) telemetry-ingestion Lambda (append-only audit/RT stream); (b) async report/summary generator Lambda writing to S3; (c) scheduled DIF/equity-monitoring Lambda (EventBridge); (d) API-Gateway + Lambda RPC layer — rejected because it contradicts D-012's in-database definer-RPC design; (e) no Lambda — rejected because the constraint requires at least one.
- **Evidence:** E-072 (the ≥1-Lambda constraint); D-011/D-012 (replay design + AWS platform); `docs/architecture/ARCHITECTURE_PLAN.md` §3, §8.
- **Rationale:** Scoring is a pure, stateless, event-driven compute — a natural Lambda rather than a forced insertion into the always-on web tier. Packaging it as a versioned Lambda directly realizes D-012's deterministic-replay invariant, isolates answer keys (anti-gaming), and is **structure-agnostic** — it scores whatever the sequencer produced — so it survives whether the proposed adaptive/two-stage test structure is accepted or modified.
- **Consequences:** `docs/architecture/ARCHITECTURE_PLAN.md` §3 gains a serverless-compute row; the exam scoring/replay engine is built as a portable pure function (overnight-plan WS4/WS5) so it runs identically in local dev and in Lambda; Terraform, IAM, and invocation wiring are dormant follow-ups until deploy. Remains born-synthetic (`synthetic_only=true`, `validated=false`, D-006); no live child data or production account is authorized by this entry.
- **Owner:** Proposed by product direction (session 2026-07-26); requires team-lead ratification.
- **Relationship to prior decisions:** Augments D-012 (AWS platform) and relies on the replay design in D-011/D-012; supersedes nothing.

### D-029 — One source of truth for screener selection and scoring: the database stores, the TypeScript packages decide

> **Renumbered 2026-07-28 (was D-019).** This decision originated on the exam
> backend branch as "D-019"; that number was independently used on `dev` for the
> AWS-Lambda scoring decision (kept above as D-019). On merging the unified
> platform into `dev`, this entry was renumbered D-029 to preserve both. Doc and
> application-code references were updated; some **already-applied exam migration
> comments** (`20260725050000`, `20260725160000`, `20260725170000`) and the
> stored `comment on function` strings they set may still read "D-019" — they were
> left untouched to avoid file-vs-live-DB drift on migrations that are already
> applied. Likewise E-072/E-073 on this branch were renumbered to E-093/E-094.

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Split ownership of the adaptive screener along the line `docs/architecture/EXAM_ADAPTIVE_BUILD_PLAN.md` already draws. The **database keeps** answer keys, per-item answer verification against the server-only key (`app.exam_score_response`), the full trace (responses, metrics, telemetry), and outcome **storage**. The database **stops being an authority** on item selection, on the battery stop rule, and on the final score. `api.exam_submit_response` no longer auto-computes an outcome; outcomes now arrive through a new `api.exam_record_outcome`, which stores the `packages/exam-scoring` output **verbatim** together with the audit inputs needed to recompute it (scoring policy id, scorer version, and a database-derived sha256 of the canonical scorer input drawn from the stored trace). `app.exam_compute_outcome` and the difficulty-stepping logic are **demoted, not deleted**: both remain callable, only the automatic call site and the convergence branch are removed. The database retains a **hard safety cap** on item count (`hardItemCap`, seeded at 120) as a runaway guard that sits above the engine's own cap of 60, so it can never truncate a legitimate battery; it is explicitly not the stop rule.
- **Requirements served:** R7 (auditability and reproducible replay), R8 (feasibility — one tested implementation rather than two), R10 (claim boundaries), R11 (scalable, tunable in-house screener)
- **Why the contract already allocates it this way:** BUILD_PLAN **§3** assigns `startState/nextType/nextItem/update/isDone` to `packages/exam-engine` and specifies a stop rule keyed on core-metric coverage, even area spread, and estimate stability. BUILD_PLAN **§5** assigns the score to `packages/exam-scoring` and specifies *accuracy sets the bracket, metrics position within the bracket*. BUILD_PLAN **§6** scopes `supabase/` to storage tables plus RPCs and never grants the database scoring authority. This decision does not create a new allocation; it removes database behaviour that contradicted the existing one.
- **Defects this resolves (both independently verified against the running local stack):** (1) *Duplicate, disagreeing scoring* — `app.exam_compute_outcome` reported per-area proficiency as simply the final adaptive difficulty, while `packages/exam-scoring/src/scorer.ts` implements the §5 bracket-then-position contract, so one trace could yield two competing scores and reproducibility was not well defined. (2) *A broken stop rule in the wrong place* — the rule `attempts >= minItemsPerArea and (attempts >= maxItemsPerArea or abs(delta) <= stableDelta)` with seeded `stepSize = 0.8` and `stableDelta = 0.5` could effectively never take its convergence branch (`abs(delta)` is 0.8 on a correct answer and 0.8 on a wrong one, 0.4 only on a near miss), so every area ran to `maxItemsPerArea`, silently converting the REQUIRED variable-length battery of BUILD_PLAN §0 into a fixed-length one.
- **Alternatives considered:** (a) *Make the database the scoring authority* — port the §5 bracket-then-position pipeline into plpgsql, retire the TypeScript scorer's authority, and fix `stableDelta` so it exceeds `stepSize`. Rejected: it rewrites roughly 3,000 tested TypeScript lines into a language with weaker test ergonomics to obtain a number already computed correctly, it puts the admin-tunable policy behind migrations, and it still leaves selection split. It is a coherent choice and remains available (see reversal). (b) *Retune `stableDelta` and leave both scorers live* — rejected: it fixes the battery length but leaves the two-competing-scores defect untouched. (c) *Move answer verification into the application tier too* — rejected: the answer-key firewall is only meaningful because key comparison happens inside a schema no client role can reach.
- **Evidence/assumptions:** `supabase/EXAM_BACKEND_STATUS.md` §2/§4/§6 and unreconciled item 4 (the prior agent's executed verification: 274/274 pgTAP, the three-way answer-key firewall proof, and the recommendation this decision acts on); BUILD_PLAN §0/§3/§5/§6; `packages/exam-engine/src/done.ts` and `src/config.ts` (the §3 stop rule as implemented); `packages/exam-scoring/src/scorer.ts` (the §5 score as implemented). Fresh evidence from this change: `supabase/tests/122_exam_outcome_ownership.test.sql` (30 assertions) plus the amended `120` (46), 306/306 pgTAP across 15 files, `db lint` clean. **Open assumption:** no engine-versus-database convergence simulation has been run against the real banks, so the claim that an engine-driven battery finishes well under the 120-item guard is a design expectation, not a measured result. Everything remains born-synthetic (`synthetic_only = true`, `validated = false`); nothing here establishes predictive validity or authorizes an admissions decision.
- **Rationale:** Reproducibility does not require in-database computation — it requires a frozen trace plus a frozen policy, both of which this schema stores. `scoreExam` is pure in `(items, policy)`, so it reproduces wherever it runs. Key custody, by contrast, genuinely requires the database. Splitting on that line keeps the security property that must be in Postgres in Postgres, and puts the psychometric logic where it is specified, tested, and cheap to tune.
- **Consequences:** The database now stores a score it did not compute and cannot independently re-derive. That risk is mitigated in the same change rather than deferred: `app.exam_scorer_input_json` exposes the canonical scorer input from the stored trace, `app.exam_scorer_input_hash` hashes it server-side at record time, and both are stored on the outcome, so any recorded score can be recomputed from the trace and checked. `api.exam_submit_response` keeps its signature but its payload changes (`done` and `outcome` are replaced by `itemsAdministered`, `hardItemCap`, `hardCapReached`, and `stopRuleOwner`), and it no longer closes a session except at the safety cap — the application must now drive the stop through the engine and end the session by recording an outcome. `api.exam_get_next_item` becomes a fallback selection path that must not be mixed with engine-driven selection in one session. The dead policy knobs `minItemsPerArea`, `maxItemsPerArea`, and `stableDelta` are removed from the shipped policy; `stepSize` stays because it still drives the fallback difficulty bookkeeping. Two RPCs are added to the `api` surface, so `packages/db-types` regenerates additively. `apps/web` must be wired to the new flow before an end-to-end battery can complete.
- **Reversal:** Nothing was dropped, so reverting is additive. To restore database scoring authority: (1) re-add `if v_hard_cap_reached then v_outcome := app.exam_compute_outcome(p_session_id); end if;` to `api.exam_submit_response` (or restore the original `v_session_done` variable and its stop rule); (2) restore `minItemsPerArea`, `maxItemsPerArea`, and `stableDelta` to `app.exam_policy.config`, setting `stableDelta` **above** `stepSize` so the convergence branch is reachable at all; (3) optionally revoke `api.exam_record_outcome`. Reverting migration `supabase/migrations/20260725050000_exam_outcome_ownership.sql` wholesale also works, at the cost of the outcome audit columns. `app.exam_compute_outcome` was left intact and carries a `DEMOTED` comment naming the exact call site to restore.
- **Owner:** Proposed by the backend workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Implements the ownership already stated in BUILD_PLAN §3/§5/§6 and acts on the recommendation recorded in `supabase/EXAM_BACKEND_STATUS.md` §6. Does not supersede D-017 or any prior decision, and does not change the platform bindings ratified in D-012.

### D-020 — Reconcile the question banks with the canonical item contract; lure taxonomy becomes two fields

- **Date:** 2026-07-25
- **Status:** Proposed
- **Decision:** Where the 63 generated banks and `packages/contracts` disagreed on item shape, the contract is widened when the banks' convention is better and the banks are migrated when they are wrong. Specifically: (a) `questionTypeCodeSchema` accepts a mixed-case middle segment; (b) `answer.distractorRationales` becomes a record keyed by the selectable element's own id, not an array aligned to option order; (c) the lure taxonomy is split into a **required coarse `lureClass`** drawn from the existing nine-value enum and an **optional free-form `lureDetail`** carrying the type's own label; (d) `answer`, `scoring`, and `provenance` accept type-specific fields instead of being closed objects. A permanent conformance test (`packages/contracts/src/bank-conformance.test.ts`) parses every bank on every `pnpm -r test` run.
- **Requirements served:** R5, R7, R10
- **Alternatives considered:** (1) Migrate all banks to the contract's array-of-enum rationales — rejected: several types have selectable elements that are not positional options at all (`VER-EVIDENCE-01` keys passage sentences, `WM-bubble-01` keys per-lane n-back steps), and server-side M-ERRTYPE must look up a chosen lure by key once options are shuffled. (2) Widen `lureClassSchema` to admit all 219 domain labels — rejected: M-ERRTYPE and M-LURETYPE are counts over buckets and become uncomputable with an open vocabulary. (3) Keep only the coarse enum and discard the domain labels — rejected: it throws away the diagnosis the labels exist to carry. (4) Rename the 11 mixed-case catalog types — rejected as churn against the established vocabulary in specs, demos, banks, and docs.
- **Evidence:** E-074 (0/63 `bankItemSchema`, 14/63 `servedItemSchema`, measured 2026-07-25).
- **Rationale:** The banks encoded a de-facto schema that was, on the contested fields, better designed than the contract; the contract encoded assumptions no generator followed. Neither side was automatically right, so each field was decided on its merits. The two-field lure model is the only resolution that keeps the metrics computable and the diagnosis intact at the same time.
- **Consequences:** All 63 banks were regenerated as a pure shape migration — item counts, difficulties, content, and correct answers are byte-identical. `answer`, `scoring`, and `provenance` are now open objects; this does not weaken the security boundary, because `servedItemSchema` omits all three wholesale, and a test asserts it. Nine labels have no honest coarse home and are recorded as `distractor_other` with the gap named explicitly in `NO_HONEST_COARSE_CLASS`: non-responses (`abandoned`, `empty_response`, `no_response`, `guess`), conforming foils in find-the-odd-one-out tasks (`correct_placement`, `shared_pair`, `sound_given`, `sound_step`), and one developmental representation bias (`representation`). A further family — correct-but-inefficient responses (`complete_detour`, `solved_verbose`, …) — is bucketed as `local_fit` on the reading "planned locally rather than globally", which is defensible but is not what the enum was written for.
- **Owner:** Proposed by the contracts workstream (session 2026-07-25); requires team-lead ratification.

### D-021 — `demoPath` is a required bank field and an optional contract field

- **Date:** 2026-07-25
- **Status:** Proposed
- **Decision:** `demoPath` is added to `bankItemSchema` as **optional** and is now present on every item in every bank; `EXAM_ITEM_SCHEMA_SPEC.md` §6.1 continues to require it, and `audit_banks.mjs` moves it from `OPTIONAL_ITEM_KEYS` to `REQUIRED_ITEM_KEYS`.
- **Requirements served:** R7, R8
- **Alternatives considered:** Drop `demoPath` from the spec and the banks, and let each host resolve `typeCode` to a renderer. Rejected: 38 demo pages already embed served-item samples carrying it, and `apps/web`'s hand-maintained eight-type `typeCode -> path` map is evidence of what that costs at 66 types, not evidence the field is unnecessary.
- **Evidence:** E-074 (49 banks carried it, 14 did not; `.strict()` rejected all 49).
- **Rationale:** This resolves the three-way conflict the auditor documented and explicitly left to a governance decision. Optional in the contract because a host may legitimately override the path — `apps/web` serves from `/exam-demos/` — and because engine- and test-constructed items should not have to invent one. Required in the banks because a generated item with no renderer is incomplete.
- **Consequences:** 14 generators now emit `demos/<typeCode>.html`. The value is bank-relative and advisory; hosts that resolve their own path are unaffected. Since the value is fully derivable from `typeCode`, it carries no independent information today — its purpose is to let a future item version point at a different renderer without a client-side registry change.
- **Owner:** Proposed by the contracts workstream (session 2026-07-25); requires team-lead ratification.

### D-022 — Split the screener's core-metric registry into per-item observed and trace-derived metrics

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Treat the adaptive screener's core-metric set as two kinds of metric rather than one. `observed` metrics are genuinely emitted per item by a renderer or by server verification, and their `minSamples` counts emissions as before. `derived` metrics are session-level aggregates the engine and scorer FIT from the accumulated trace, and their `minSamples` counts the inputs to that fit. `M-RTVAR`, `M-CONSIST`, `M-LEARNRATE`, `M-DIFFREACH` and `M-ROTSLOPE` are reclassified as derived. Additionally, the per-child response-time family (`M-RT`, `M-RTFIRST`, `M-RTVAR`) is given `session` rather than `per_area` adequacy, matching how `MEASUREMENTS.md` states their "how much to collect". `M-ROTSLOPE` remains derived but is demoted to NOT enforced, with the reason recorded in code.
- **Problem it fixes:** The stop rule in `packages/exam-engine/src/done.ts` required every *enforced* core metric to reach `minSamples` in every area, and `update.ts` counted a sample only when a metric id appeared on a per-item result. Four enforced metrics could never accumulate that way: `M-ROTSLOPE` is declared by 0 of the 66 catalog types; `M-LEARNRATE` by 0 verbal and 0 spatial types; `M-RTVAR` by 3 types with none in fluid or quantitative; `M-CONSIST` by 6 types spread 1–2 per area. A fifth, `M-DIFFREACH`, is only attached by `/api/exam-submit` on a correct answer. `isDone` could therefore only ever return true via `state.itemsServed >= hardItemCap`, making the battery silently FIXED-LENGTH at 60 items. The root cause is a category error: a response-time variance, a consistency rate, a growth slope and a rotation slope are statistics over a series of items, so no correct renderer can ever emit one per item.
- **Requirements served:** R11 (scalable/tunable in-house screener) via `EXAM_ADAPTIVE_BUILD_PLAN.md` §0 ("Battery length is variable — keep asking until there is adequate data in all core metrics to conclude a score. No fixed item count"), §3 (engine stop rule), §4 (basic-core metric set) and §5 (deterministic, trace-reproducible scoring).
- **Alternatives considered:** (a) Delete or lower the unreachable metrics until the tests pass — rejected: the metric taxonomy is a deliberate product artifact traced to `MEASUREMENTS.md`, and deleting it would hide the coverage gap rather than close it. (b) Require renderers to emit the aggregates — rejected as a category error no correct renderer can satisfy. (c) Make enforcement dynamically conditional on live bank supply — rejected: it would require passing the banks into `isDone`, breaking the callers that already code against the §3 signature, and it would silently relax the rule whenever a bank was missing. (d) Enforce `M-ROTSLOPE` as derived — rejected on supply grounds (below).
- **Evidence:** Cross-tabulation of `research/exam-question-types/catalog/master_types.jsonl` (66 type specs) against the 49 wired bank files, verified in this session; `MEASUREMENTS.md` entries for M-RT (">=20 RT samples per child"), M-RTFIRST (">=15 samples"), M-RTVAR (">=20-30 RT samples"), M-CONSIST (">=3 matched pairs"), M-LEARNRATE (">=8-12 escalating trials") and M-ROTSLOPE (">=12 trials spanning >=3 disparities"). Born-synthetic throughout (`syntheticOnly=true`, `validated=false`); no metric here is validated against any outcome.
- **Rationale:** Deriving aggregates from the trace is the only formulation under which "adequate data to conclude a score" is both meaningful and reachable. It also makes coverage *stronger*, not weaker: `M-CONSIST` and `M-LEARNRATE` become reachable in every area instead of depending on which one or two types happened to be selected. Because every derived value is a pure function of the stored per-item trace, the §5 requirement that a score be reproducible from a stored trace is preserved.
- **M-ROTSLOPE demotion:** The metric IS fittable — `SPA-VIEW-01` (140 items, disparities 0/60/120/180) and `SPA-XSCAN-01` (38 of 140 items, disparities 0–90) record `answer.angularDisparityDeg`, and a forced 40-trial block recovers a slope. It is not adequately SUPPLIABLE: no catalog type declares it, `SPA-XSCAN-01`'s disparity items all sit at difficulty >=14.6, and a >=12-trial fit cannot be obtained inside a proportionate spatial block without crowding out spatial construct breadth. It is therefore tracked and scored where fittable, but does not gate completion, and should be re-enforced when a dedicated mental-rotation type is wired with disparity across the full difficulty ramp.
- **Consequences:** `packages/exam-engine` gains `derived.ts` (adequacy conditions), `replay.ts` (state rebuilt from a stored trace), a per-area `ItemObservation` trace on `AreaState`, an optional `ScoredItem.stimulus` for server-only stimulus parameters, and `auditMetricSupply`. `packages/exam-scoring` gains `derived-metrics.ts`, which fits the aggregate VALUES; without it the exam profile's `learningRate` and `consistency` signals would have stayed permanently `unknown` on any real trace. A new real-bank test drives the stop rule against the actual registry and banks, closing the blind spot that let this ship: the previous synthetic harness fabricated every metric, so 25 passing tests could not detect an unsatisfiable rule. Three sole-source fragilities are now pinned by test (quantitative `M-PAE`, `M-REV`, `M-RT`, each supplied by exactly one wired type). **Open follow-up, not fixed here:** core-metric coverage now completes at 33 items for every ability profile, but total battery length is set by the estimate-stability arm and ranges 33–83 items, because item selection targets the current estimate and so pins the difficulty update to its 0.4/item floor. A child seeded far from their true ability — including a gifted child, the screener's target population — still reaches the 60-item cap. That is a distinct defect in the difficulty-update/selection interaction; changing it would move every score, so it is escalated rather than retuned here.
- **Owner:** Proposed by the exam-engine workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Implements the variable-length battery already locked in `EXAM_ADAPTIVE_BUILD_PLAN.md` §0; does not change the metric taxonomy's membership, the scoring policy defaults, the claim boundaries, or the born-synthetic status.

### D-023 — Replace the screener's fixed difficulty step with a reversal-indexed decaying step schedule

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** The per-area difficulty update in `packages/exam-engine/src/update.ts` no longer moves the estimate by a fixed magnitude spanned by "surprise". It moves it by a **scheduled step that starts large and decays as evidence accumulates**: `step(m) = minUpdate + (initialStep − minUpdate) / m^stepDecayExponent`, where `m` counts the **direction reversals** already accumulated in that area (a reversal being a correct answer following a wrong one or vice versa), including the reversal the current response produces. Surprise is retained but demoted to a multiplier on the scheduled step (`step × (1 + surpriseGain × surprise)`), and the `M-ERRTYPE` near-miss softening is unchanged. The whole schedule is exposed as tunable engine policy: `initialStep` (2.0), `minUpdate` (0.25, was 0.4), `maxUpdate` (3.0, was 1.0), `stepDecayExponent` (1.0), `stepBurnInReversals` (0), `surpriseGain` (1.0). Nothing else changes: the stop rule, the core-metric registry, the selection rule, the scoring policy, and the public signatures of `startState/nextType/nextItem/update/isDone` are all untouched.
- **Problem it fixes:** The open follow-up recorded in D-022's consequences. Core-metric coverage completed at 33 items for every ability profile, but *total* battery length was set by the estimate-stability arm and ran 33–83 items. Item selection targets the **current** estimate, so a child is always served items matched to where the engine already believes they are; the surprise term (`|difficulty − estimate| / 19`) therefore stays near zero by construction and the estimate could only ever move at its 0.4-per-item floor. Climbing the 7 points from a '4-5' seed of 11 to a true ability of 18 took ~18 items of pure climbing **per area** before stability could even begin to be judged. A gifted child — the screener's entire target population — ran 83 items and passed the 60-item safety cap without the estimate settling; an above-level-seeded child with low ability ran 128. Measured against the real registry and banks, the same profiles now finish at 36 and 45 items, and no profile in the matrix reaches the cap.
- **Requirements served:** R11 (scalable, tunable in-house screener) via `EXAM_ADAPTIVE_BUILD_PLAN.md` §0 (variable-length battery that concludes when there is adequate data, realistic target ≤45 minutes of child time) and §3 (engine stop rule and estimate); R7 (auditability and reproducible replay), since the schedule is indexed off the stored trace and adds no hidden state.
- **Alternatives considered:** (a) *Widen the item-selection window so the engine deliberately serves off-target items and re-animates the surprise term* — rejected: it buys estimate movement by degrading measurement, serving items the child has no chance on (or no challenge from) purely to generate a signal, and it changes what the child experiences rather than how the engine reasons. (b) *Decay the step by item count instead of reversals (plain Robbins-Monro `a_n = c/n^α`)* — rejected: the step would shrink while the estimate is still travelling, so it penalises exactly the child the fix is for. Indexing on reversals keeps the full stride until there is evidence the ability region has been bracketed (Kesten's accelerated variant of Robbins-Monro; the same rule as a Levitt/transformed up–down staircase, which shortens its step at each reversal). (c) *Raise the fixed floor from 0.4 to ~1.0* — rejected: it fixes the climb and breaks the settle, since the estimate can then never resolve finer than a full scale point and the tail of the scale loses precision. (d) *Loosen `stabilityDrift`/`stabilitySd` so the unsettled estimate counts as settled* — rejected outright: that lowers the bar for "adequate data" instead of meeting it, which is the failure mode D-020 was written to close. (e) *Escalate again without fixing* — rejected: the escalation ground was that changing the update "would move every score", which is true but not blocking, because every bank is born-synthetic and uncalibrated (`syntheticOnly=true`, `validated=false`), no score is validated against any outcome, and no admissions decision depends on any current number.
- **Evidence:** Fresh, from this session, against the **real** registry and banks (`research/exam-question-types/`), via the extended `packages/exam-engine/src/real-bank.test.ts`. Items administered and final per-area estimate versus planted ability, before → after (a hard cap of 400 was used so an overrun could happen and be measured rather than truncated):

| Profile (band, planted ability F/V/Q/S) | Items before | Items after | Estimate before | Estimate after |
| --- | --- | --- | --- | --- |
| At the seed (4-5, 11/11/11/11) | 33 | 33 | 11.00/11.00/10.59/11.00 | 11.18/11.19/11.29/11.18 |
| Gifted (4-5, 18/16/17/15) | **83 (past the cap)** | 36 | 17.85/16.60/17.01/15.40 | 18.65/16.05/17.37/15.58 |
| Struggling (4-5, 5/4/6/3) | **85 (past the cap)** | 37 | 4.99/4.20/4.20/2.98 | 5.07/3.71/5.00/2.99 |
| All-correct responder (4-5, off-scale high) | **100 (past the cap)** | 36 | 20.00 ×4 (pinned) | 20.00 ×4 (pinned) |
| All-wrong responder (4-5, off-scale low) | **108 (past the cap)** | 36 | 1.00 ×4 (pinned) | 1.00 ×4 (pinned) |
| Mixed spread (4-5, 14/8/11/5) | **66 (past the cap)** | 34 | 15.03/7.43/10.99/4.98 | 14.92/7.73/11.29/5.55 |
| Above-level seed, low ability (18 seed, 8/7/9/6) | **128 (past the cap)** | 45 | 8.36/6.79/9.17/5.97 | 7.74/7.16/9.28/5.74 |

  Worst per-area estimate error across the matrix is **1.00 scale points** (was 1.80 among the profiles that concluded at all). The band-matched 33-item case did not regress in length and its estimate did not become jittery. Knob perturbation (each knob varied independently around its default) keeps the worst battery at 42–53 items and the worst error at ≤2.6, so the defaults are not a knife edge. Test counts: exam-engine 43 → 68 passing, exam-scoring 41/41 unchanged, `apps/web` 113/113 unchanged; typecheck and lint clean. Everything remains born-synthetic; the simulated child is a deterministic threshold responder against uncalibrated banks, so these are **convergence** results, not psychometric validity.
- **Rationale:** A single fixed step cannot serve the two jobs an adaptive estimate has — travel far enough to find a child who is nowhere near their grade-band seed, then move finely enough to settle on them. A decaying gain is the standard resolution in stochastic approximation, and indexing the decay on reversals rather than on item count is what makes it correct **here**: because selection targets the current estimate, one-sided responses are the engine's only signal that it has not arrived yet, and reversals are its only signal that it has. Keeping the asymptote strictly positive (`minUpdate` 0.25) preserves the ability to travel again if a child's performance shifts, and makes the tail of the scale **finer** than the 0.4 fixed floor it replaces, so precision improves rather than being traded away. Reversal counts are read off `AreaState.trace`, which `update` already builds purely from stored `ScoredItem`s, so the schedule introduces no state that is not persisted and a stored trace still reproduces the estimate exactly (asserted for every profile).
- **Consequences:** **Every score moves, as expected and as sanctioned above** — but the movement is much larger than a step-size retune would suggest, and it exposes a pre-existing miscalibration rather than creating one. `packages/exam-scoring` brackets an area by its difficulty-weighted accuracy over the items served. Under the old slow climb, a gifted child accumulated a long tail of far-below-ability items answered correctly, so their measured accuracy was 0.88 and they landed in the top bracket (composite 15.81 against a true mean ability of 16.5). Under a battery that actually converges, every child — gifted or struggling — ends near 50% accuracy, because that is what a matched item is; the same gifted child now measures 0.49–0.62 accuracy and scores a composite of **8.72**, while the band-matched child at true ability 11 scores 6.33. The engine's own per-area estimates separate those two children cleanly (16.05 vs 11.19 in verbal); the scorer no longer does. **This is a defect in the §5 accuracy-bracket contract, not in this change, and it is deliberately NOT fixed here** — see the open follow-up below. `difficultyDelta` changes signature (first argument is now the prior area state rather than a bare estimate) and `directionReversals`/`stepSize` are added to the public surface; `difficultyDelta` has no caller outside the engine and its tests, and the five contract functions are unchanged, so `apps/web` needs no edit. `EngineConfig` gains four knobs and changes two defaults; `apps/web`'s `EXAM_ENGINE_OVERRIDES` sets none of them and inherits the new schedule. The database's demoted `stepSize` (D-029) is a separate fallback knob and is untouched.
- **Open follow-up, not fixed here (escalated):** The scorer positions a child by accuracy first and difficulty second. That inverts the information content of a converged adaptive battery, in which accuracy is approximately constant by construction and **difficulty reached** carries the ability signal. As delivered, a gifted child and a band-matched child now receive composites of 8.72 and 6.33 on a 1–20 scale while their true abilities are 16.5 and 11. Fixing it means changing what §5 brackets on — most likely scoring from the converged per-area estimate and `M-DIFFREACH` rather than from raw accuracy — which is a change to a ratified scoring contract and to every reported number, so it needs its own decision rather than being folded into a convergence fix. Nothing depends on the current numbers (born-synthetic, `validated=false`, no admissions decision), so no applicant is affected while it is open.
- **Reversal:** No code needs to be reverted. The schedule strictly generalises the rule it replaces: setting `initialStep` equal to `minUpdate` removes the decay entirely, and `{ minUpdate: 0.4, maxUpdate: 1.0, initialStep: 0.4, surpriseGain: 1.5 }` reproduces the pre-D-023 `magnitude = 0.4 + 0.6 × surprise` **exactly** — pinned by a test in `engine.test.ts` that checks equality to 10 decimal places across the estimate/difficulty/near-miss grid. Restoring the old behaviour is therefore a policy edit, not a revert, which also means the admissions team can A/B the schedule from config once the admin portal lands.
- **Owner:** Proposed by the exam-engine workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Closes the open follow-up recorded in D-022's consequences. Does not change the metric taxonomy, the stop rule, the selection rule, the scoring policy defaults, the claim boundaries, or the born-synthetic status. Does not supersede any prior decision.

### D-024 — Offer a difficulty-adjusted ability statistic as an alternative bracket driver; the accuracy default is unchanged pending the owner's decision

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification). **Nothing is switched on by this entry.**
- **Decision:** Add `bracketing.mode` to `ExamPolicy` in `packages/exam-scoring`, with two values. `accuracy` is the existing, unchanged contract and remains the default: (difficulty-weighted) accuracy over the items served picks the bracket by each bracket's `accuracyMin`. `ability` picks the bracket by each bracket's existing θ span, using a **difficulty-adjusted ability fitted from the trace** — the location parameter of a one-parameter (Rasch-style) logistic response model over the (difficulty, score) pairs, i.e. the difficulty at which the child's fitted success probability is 50%. The fit lives in a new `packages/exam-scoring/src/ability.ts`, is exposed as `ABILITY_BRACKET_POLICY`, and is tunable through `bracketing.ability` (`slope` 1.0, `priorSd` 6.0). **The two-stage §5 structure the project owner specified is preserved in both modes:** the bracketing statistic sets the bracket and never positions; the within-bracket metric average (M-DIFFREACH, inverse M-RTVAR, M-LEARNRATE, M-ERRTYPE, process/domain terms) positions the child inside it and never brackets. `scoreExam` keeps its `(items, policy)` signature, its purity, and its single input: the fit reads only per-item `difficulty` and `score` from the stored trace and consults no engine state.
- **Problem it fixes:** The open follow-up escalated in D-023's consequences. Since the engine began converging (D-023), item selection targets the current estimate, so a well-targeted item is by definition one the child has about an even chance on, and every child — gifted or struggling — finishes near the same accuracy. Accuracy therefore no longer carries the ability signal; **difficulty reached** does. Measured on this branch against the real registry and banks, a child with equal true ability in all four areas seeded at band '4-5' produces a composite that spans only **2.26–11.04** across true abilities 3–19 on a 1–20 scale, never approaches the top of the scale for a maximally able child, and barely separates true ability 11 from 15 (6.33 vs 6.59). The engine's own per-area estimates track the same children to within about 1.0 point, so the information is present in the trace and the scorer discards it (E-078).
- **Requirements served:** R11 (scalable, tunable in-house screener) via `EXAM_ADAPTIVE_BUILD_PLAN.md` §5 (deterministic bracket-then-position scoring on the [1, 20] scale); R7 (auditability and reproducible replay), since the new statistic is fitted from the stored trace and adds no hidden state; R10 (claim boundaries), since the alternative is offered as evidenced and unratified rather than adopted.
- **Alternatives considered:** (a) *Bracket on `M-DIFFREACH` (the hardest difficulty solved)* — rejected: it is an extreme order statistic, so one lucky correct answer high on the ramp defines the whole bracket and no amount of subsequent failure can move it down; it is also already the dominant within-bracket positioning term, so promoting it to bracket driver would double-count it and leave stage 2 mostly refining on the same number it just bracketed on. (b) *Bracket on the mean difficulty of correct items* — rejected: it ignores failures entirely, so it is bounded above by whatever the child happened to solve and is dragged down by however many easy items the adaptive path served early; two children with identical ability but different seeds would bracket differently. (c) *Bracket on the engine's converged per-area estimate* — rejected on contract grounds even though it is the most accurate number available: it would make the scorer depend on engine internal state rather than on the trace, breaking the §5 property that a stored trace plus a frozen policy reproduces the score, and would couple two packages that BUILD_PLAN §7 keeps independent. (d) *Bracket on a "highest difficulty with k consecutive correct" reliability threshold* — rejected: `k` is arbitrary, it discards partial credit, and an adaptive path frequently never serves `k` items at one difficulty. (e) *Fit a two-parameter model and estimate discrimination as well* — rejected: with ~8–11 items per area the slope is not identifiable, and against a deterministic responder it runs to infinity; discrimination belongs in policy, where an admin can see and change it. (f) *Change the default outright* — rejected: the two-stage bracket-then-position contract and its accuracy driver are the owner's stated product requirement, so replacing the driver is the owner's decision, not the implementer's. (g) *Fix it by widening the brackets or reweighting the within-bracket metrics instead* — rejected: it treats a symptom, since no reweighting of stage 2 can recover a signal stage 1 has already thrown away.
- **Evidence:** E-078, fresh from this session against the **real** registry and banks (`research/exam-question-types/`), via `scripts/exam-bracketing-comparison.ts` (`pnpm exam:bracketing`), which drives the `exam-engine` simulation harness and scores each resulting trace under both modes. Composite versus true mean ability:

| Profile (band, true ability) | Raw accuracy | Engine estimate | Composite, accuracy mode | Composite, ability mode |
| --- | --- | --- | --- | --- |
| 4-5, equal 3 | 24% | 3.05 | 2.26 | 2.26 |
| 4-5, equal 5 | 32% | 5.04 | 2.35 | 5.80 |
| 4-5, equal 7 | 27% | 6.00 | 2.47 | 5.96 |
| 4-5, equal 9 | 36% | 9.11 | 3.51 | 10.18 |
| 4-5, equal 11 | 39% | 11.21 | 6.33 | 10.33 |
| 4-5, equal 13 | 55% | 15.32 | 6.44 | 14.44 |
| 4-5, equal 15 | 56% | 15.74 | 6.59 | 14.59 |
| 4-5, equal 17 | 62% | 17.43 | 8.76 | 18.76 |
| 4-5, equal 19 | 67% | 18.86 | 11.04 | 19.04 |
| 4-5, unequal 14/8/11/5 (mean 9.5) | 38% | 9.87 | 4.46 | 9.20 |
| 4-5, unequal 18/16/17/15 (mean 16.5) | 61% | 16.91 | 8.72 | 16.72 |
| 4-5, unequal 5/4/6/3 (mean 4.5) | 27% | 4.20 | 2.31 | 4.03 |
| 4-5, off-scale high (all correct) | 100% | 20.00 (pinned) | 19.17 | 19.17 |
| 4-5, off-scale low (all wrong) | 0% | 1.00 (pinned) | 2.32 | 2.32 |
| above-level seed, low ability 8/7/9/6 (mean 7.5) | 20% | 7.48 | 2.54 | 7.05 |
| above-level seed, high ability 18/17/18/16 (mean 17.25) | 36% | 17.30 | 5.78 | 17.69 |
| 2-3 seed, equal 7 | 36% | 7.11 | 5.15 | 6.03 |
| 6-8 seed, equal 19 | 61% | 19.33 | 9.93 | 18.93 |

  Over the sixteen on-scale profiles, mean absolute composite error against true mean ability falls from **5.73 to 0.69** and worst-case error from **11.47 to 1.76**; the engine's own estimate, which is the practical ceiling for any trace-derived statistic, sits at 0.41/2.32. The ability composite is strictly monotone across the nine-level sweep and reaches 19.04 for a maximally able child. Knob perturbation (`slope` 0.4→3.0, `priorSd` 3→Infinity, each varied independently) moves mean error only between 0.68 and 0.71, so the defaults are not a knife edge — though see the consequences below for why that insensitivity is partly a symptom rather than a virtue. Test counts: `exam-scoring` **41 pre-existing tests unmodified and passing, plus 30 new** (71 total); `exam-engine` 68/68 and `apps/web` 313/313 unchanged; `pnpm -r exec tsc --noEmit` clean; lint clean. **Default byte-identity was verified directly, not assumed:** the full `ExamScore` JSON for all eighteen profiles above hashes to the same sha256 (`a3e9dc58…`, 100,562 bytes) with the change applied and with `packages/exam-scoring` reverted to its pre-change state. Everything remains born-synthetic — a deterministic threshold responder against uncalibrated banks — so these are **recovery** results, not psychometric validity.
- **Rationale:** A bracketing statistic has to carry between-child variance to be a bracketing statistic. Under a fixed-form test, accuracy carries it, which is why the §5 contract was reasonable when written. Under a battery that converges, the design deliberately holds accuracy near a constant, so the statistic that once separated children now separates them mainly by how far off the seed they started. The difficulty at which a child succeeds is the quantity the adaptive design actually estimates, so bracketing on it uses the trace the way the trace was produced. Fitting it as an IRT location parameter rather than reading a summary statistic off the trace matters for the same reason: it weighs every item, successes and failures alike, so it cannot be set by one lucky or one careless response, and it is defined for the partial-credit scores the contract already allows.
- **Consequences:** No shipped number changes. `DEFAULT_EXAM_POLICY` gains an explicit `mode: 'accuracy'` and `AreaScore` gains an optional `abilityEstimate` that is **present only under ability bracketing**, so the default output object is unchanged key-for-key. `apps/web` calls `scoreExam` with the default policy and is untouched. **Where the alternative still falls short, stated plainly:** (1) *The bracket quantizes.* The residual error is now dominated by the two-stage structure, not by the statistic. At true ability 17 the fit lands at 16.7–17.3 — nearly exact — but the bracket is [16, 20] and the metric position averages 0.68, so the score is pushed to 18.76. Symmetrically, a child fitted just under a bracket edge is pulled down. (2) *Neighbouring levels inside one bracket are still barely separated.* Consecutive rungs of the sweep that share a bracket differ by only 0.14–0.29 composite points (5 vs 7, 9 vs 11, 13 vs 15, 17 vs 19), because the within-bracket metric average is nearly ability-blind; the composite is effectively a five-rung staircase with fine jitter. Gifted-versus-band-matched separation is nonetheless solved (16.72 vs 10.33 for true 16.5 vs 11, against 8.72 vs 6.33 before). (3) *The knob insensitivity above is partly an artifact of (1)* — a bracket only changes when the fit crosses an edge, so a wide range of slopes yields the same bracket. It is evidence the mode is robust to the knobs, not evidence the fit is well conditioned. (4) *`slope` is a design assumption, not a calibrated discrimination*, because no bank item has a calibrated `a` or `b`. (5) *Bottom-of-scale behaviour is unchanged*: at true ability 3 both modes return 2.26, because both already bracket at [1, 4]. If the owner adopts this mode, the natural follow-up — not done here — is to decide whether stage 2 should position on the fitted ability's location inside the bracket rather than on the metric average, which would remove (1) and (2) but is a change to the *positioning* half of the contract and therefore a separate decision.
- **Reversal:** Nothing to revert; the change is purely additive. `mode` is optional and absent means `accuracy`, so every policy object written before this entry — including any stored by an admin portal later — keeps its exact behaviour. Adopting the alternative later is a one-field policy edit (`bracketing.mode: 'ability'`) with no code change; abandoning it is deleting `ability.ts`, the `mode`/`ability` policy fields, and two test files.
- **Owner:** Proposed by the exam-scoring workstream (session 2026-07-25); requires team-lead/project-owner ratification. **This entry ratifies nothing about which mode the product should use.**
- **Relationship to prior decisions:** Acts on the open follow-up escalated in D-023's consequences, and does not close it — the follow-up closes when the owner chooses a mode. Preserves the §5 two-stage contract cited in D-029. Does not change the metric taxonomy (D-022), the difficulty-update schedule (D-023), the stop rule, the selection rule, the default scoring policy's numbers, the claim boundaries, or the born-synthetic status. Does not supersede any prior decision.

### D-025 — Price the screener's age-band item preference in scale points instead of letting it override difficulty targeting

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** `nextItem` in `packages/exam-engine/src/selection.ts` no longer ranks an age-band match strictly ahead of closeness to the area estimate. It ranks candidates by a single **targeting cost**, `|item.difficulty − estimate| + (item is tagged for the child's grade band ? 0 : ageBandBias)`, with the seeded jitter and item-id tie-breaks unchanged behind it. `ageBandBias` is a new `EngineConfig` knob, default **0.5** scale points, so the age band still decides between comparably targeted items but can no longer buy a 3-point targeting error. Nothing else changes: the `±difficultyWindow` pre-filter, the type-level `AGE_BAND_BONUS`, the difficulty update (D-023), the stop rule, the core-metric registry, the scoring packages, the banks, and the public signatures of `startState/nextType/nextItem/update/isDone` are all untouched.
- **Problem it fixes:** A systematic estimate bias that the existing accuracy tests could not see. `real-bank.test.ts` asserted a 1.5-point tolerance over seven hand-picked `CONVERGENCE_PROFILES`; sweeping every integer ability from 2 to 19 (equal ability in all four areas, band '4-5', real registry and banks) breached that tolerance at **five** of the eighteen levels — 12 by 2.58, 13 by 2.67, 14 by 1.87 upward, and 5 by 1.53, 7 by 1.91 downward — while all seven named profiles passed. The mechanism is an interaction between selection and the D-023 update, not a bank shortage. Almost every wired type's `'4-5'` items stop between difficulty 11.8 and 13.4 (`VER-*` at 11.8, `FLU-MATRIXBUILD-01` at 12.0, `FLU-ANALOGY-01` at 13.4) and start around 7.6–8.1, so once an area's estimate left that range every band-matched candidate sat on one side of it. At true ability 13 the engine served items **1.19 points below the estimate on average**, and 22 of 43 items were not the closest available: with the estimate at 15.00 it served a difficulty-12.00 item while a difficulty-15.00 item of the same type was unseen. `difficultyDelta` takes its direction from correctness alone and its `surprise` term is clamped at zero below, so it amplifies a response that contradicts the estimate but never attenuates one that merely confirms it — a correct answer on an item 3 points *below* the estimate still raised the estimate by a full scheduled step. That closes a positive feedback loop: the estimate rises, the band-matched items become relatively easier, the child keeps getting them right, and the estimate parks around 15.0–15.7 for a child at 13. Below the band the same loop runs in reverse (at true 7 the engine served a difficulty-8.00 item to an estimate of 5.15, because no band-matched fluid item exists below 7.6), which is why 5 and 7 undershoot rather than overshoot. The region hit hardest, 12–14, is exactly where a screener decides whether a child looks gifted.
- **Requirements served:** R11 (scalable, tunable in-house screener) via `EXAM_ADAPTIVE_BUILD_PLAN.md` §0 (variable-length battery that concludes when there is adequate data) and §3 (engine estimate and selection rule); R7 (auditability and reproducible replay), since selection stays a pure function of stored state and config and every swept trace still rebuilds its estimate exactly; R10 (claim boundaries), since the entry reports a recovery result against born-synthetic banks and claims nothing about validity.
- **Alternatives considered:** (a) *Attenuate the difficulty update when a response merely confirms what the estimate already says* — the mirror of D-023's `surprise` term, and the more general fix, since it holds whatever selection does. Rejected as the primary fix after tracing it: with the confirming step damped, the only items that can still raise a '4-5' estimate above ~13 are the off-band ones selection was already declining to serve, so the gifted climb stalls near 15 and either the accuracy or the 48-item budget breaks. It treats the symptom (mis-weighted evidence) while leaving the cause (the engine declining on-target items that exist and are unseen). Worth revisiting **after** selection is targeted, where it would be a second-order refinement rather than a compensator. (b) *Drop the age-band preference from item selection entirely* — rejected: age-band tagging is a content-appropriateness signal, not noise, and `ageBandBias: 0` is a strictly worse default than a small positive value because it discards a free tie-break between items the engine considers equally informative. It remains reachable from config. (c) *Widen `difficultyWindow` so more on-target items enter the pool* — rejected: the pool already contained the on-target items; they were being outranked, not filtered out. Widening the window would have served items even further off target. (d) *Loosen `ESTIMATE_TOLERANCE` past 2.7, or keep asserting only the seven named profiles* — rejected outright: that ratifies the defect and is the same failure mode D-020 was written to close. (e) *Compensate in the update with a term tuned to the current banks' age-band ranges* — rejected: it would encode today's bank shape into engine logic and silently break the first time a bank is retagged. (f) *Treat it as a bank problem and retag or extend the banks* — rejected on the evidence: supply is not the constraint (see below), and the banks, generators and sync script are out of scope for this change.
- **Evidence:** E-080, fresh from this session against the **real** registry and banks, via the ability sweep now pinned in `packages/exam-engine/src/real-bank.test.ts` (a hard cap of 400 was used so an overrun could be measured rather than truncated). Every integer ability 2–19, equal in all four areas, seeded at band '4-5':

| True ability | Items before | Items after | Estimate before (F/V/Q/S) | Estimate after (F/V/Q/S) | Worst error before | Worst error after |
| --- | --- | --- | --- | --- | --- | --- |
| 2 | 43 | 41 | 1.5/1.7/2.0/2.4 | 2.4/2.2/2.1/2.2 | 0.50 | 0.37 |
| 3 | 38 | 40 | 3.7/2.7/2.8/3.0 | 2.7/2.8/2.8/2.8 | 0.68 | 0.28 |
| 4 | 34 | 38 | 4.2/4.1/3.6/3.7 | 4.2/4.2/4.3/4.2 | 0.40 | 0.25 |
| 5 | 35 | 36 | 4.5/5.4/3.5/5.6 | 4.8/4.8/4.6/4.8 | **1.53** | 0.43 |
| 6 | 40 | 40 | 5.2/5.2/5.0/6.2 | 6.2/6.3/6.1/6.3 | 1.03 | 0.31 |
| 7 | 33 | 33 | 5.1/5.7/6.6/6.6 | 6.8/6.7/7.2/7.8 | **1.91** | 0.77 |
| 8 | 33 | 33 | 7.8/7.7/8.3/8.8 | 7.8/7.7/8.3/8.8 | 0.81 | 0.81 |
| 9 | 33 | 33 | 9.2/9.2/8.8/9.2 | 9.2/9.2/8.8/9.2 | 0.22 | 0.22 |
| 10 | 33 | 33 | 10.1/10.1/10.5/10.1 | 10.1/10.1/10.5/10.1 | 0.53 | 0.53 |
| 11 | 33 | 33 | 11.2/11.2/11.3/11.2 | 11.2/11.2/11.3/11.2 | 0.28 | 0.28 |
| 12 | 45 | 33 | 12.8/14.6/12.3/12.4 | 11.8/12.7/12.2/11.8 | **2.58** | 0.73 |
| 13 | 43 | 33 | 15.7/14.7/15.0/15.3 | 13.8/13.6/12.6/13.6 | **2.67** | 0.79 |
| 14 | 33 | 33 | 14.9/15.2/15.9/14.9 | 14.2/14.1/13.7/14.1 | **1.87** | 0.32 |
| 15 | 36 | 36 | 15.8/15.6/16.1/14.5 | 15.6/15.6/15.0/14.5 | 1.05 | 0.57 |
| 16 | 33 | 33 | 16.7/16.5/16.1/16.5 | 16.7/16.5/16.1/16.5 | 0.65 | 0.65 |
| 17 | 40 | 39 | 17.8/17.0/17.6/17.6 | 17.8/17.0/17.4/17.6 | 0.77 | 0.77 |
| 18 | 33 | 36 | 18.0/17.9/18.5/17.9 | 18.6/18.5/18.5/18.5 | 0.48 | 0.65 |
| 19 | 36 | 40 | 18.9/18.9/18.9/18.9 | 19.5/19.4/19.4/18.5 | 0.14 | 0.54 |

  Worst error anywhere in the sweep falls from **2.67 to 0.81**, and no level exceeds the 1.5-point tolerance. Battery length across the sweep moves from 33–45 items to 33–41, inside the 48-item convergence budget and well under the 60-item safety cap; the longest run in the whole matrix is the above-level-seeded low-ability profile at 45 items, unchanged. The seven named `CONVERGENCE_PROFILES` do not regress, and the two that changed length both got shorter or stayed accurate:

| Profile (band, planted ability F/V/Q/S) | Items before | Items after | Estimate before | Estimate after |
| --- | --- | --- | --- | --- |
| At the seed (4-5, 11/11/11/11) | 33 | 33 | 11.18/11.19/11.28/11.18 | 11.17/11.18/11.28/11.18 |
| Gifted (4-5, 18/16/17/15) | 40 | **36** | 18.23/16.55/17.57/15.11 | 18.65/16.05/17.39/14.51 |
| Struggling (4-5, 5/4/6/3) | 40 | 40 | 4.63/4.26/4.97/2.53 | 5.33/3.74/6.13/2.74 |
| All-correct responder (4-5, off-scale high) | 36 | 40 | 20.00 ×4 (pinned) | 20.00 ×4 (pinned) |
| All-wrong responder (4-5, off-scale low) | 36 | 44 | 1.00 ×4 (pinned) | 1.00 ×4 (pinned) |
| Mixed spread (4-5, 14/8/11/5) | 34 | 34 | 14.92/7.73/11.28/5.55 | 14.23/7.73/11.28/4.78 |
| Above-level seed, low ability (18 seed, 8/7/9/6) | 45 | 45 | 7.74/7.16/8.77/5.74 | 7.74/7.16/8.77/5.74 |

  **Bank supply is not the cause, and the fix does not compensate for one.** Fluid-reasoning difficulty is near-uniform across the scale (≈90 items per integer, 95 at difficulty 13 exactly, 1800 in the area), and in every mis-served case an unseen item of the *same type* sat within 0.2 points of the estimate. The engine was declining on-target items it already had.

  **The default is not a knife edge.** Sweeping `ageBandBias` with everything else fixed: 0, 0.25, 0.5 (default), 0.75 and 1.0 all give **zero breaches** across the eighteen levels, with worst error 0.81–0.91 and longest battery 45; 1.5 reopens one breach (1.53), 2.0 one (1.60), and 3.0 or wider reopens all five and reproduces the pre-change numbers exactly. The default sits in the middle of a plateau roughly four times as wide as the perturbation that would break it, and the four in-plateau settings are pinned by test. Test counts: exam-engine **68 → 112 passing** (the 68 pre-existing tests are unmodified), exam-scoring 71/71 unchanged, `apps/web` 325/325 unchanged, `pnpm -r exec tsc --noEmit` clean. Engine suite runtime 2.15s → 4.63s. The sweep was mutation-checked: restoring the pre-change ordering (`ageBandBias` wider than the selection window) fails it at exactly abilities 5, 7, 12, 13 and 14 while every named profile still passes. Everything remains born-synthetic — a deterministic threshold responder against uncalibrated banks — so these are **recovery** results, not psychometric validity.
- **Rationale:** In an adaptive test the item that carries the most information is the one at the child's current estimate; that is the entire reason selection targets the estimate at all (and the premise D-023 reasoned from). Age-band tagging is a *content-appropriateness* constraint, and the honest way to combine a measurement objective with a content constraint is to price the constraint in the measurement's own units rather than to lexicographically dominate it. Expressing the preference as a penalty in scale points does that: at 0.5 the engine will trade half a point of targeting for age-appropriate content and no more, so a band-typical child — who has band-matched items at their estimate — sees essentially the battery they saw before, while a child whose ability has left the band stops being measured with the wrong ruler. It also fixes the low end and the high end with one number, because both breaches are the same lexicographic override seen from opposite sides. Keeping the knob strictly positive rather than deleting the preference preserves the free tie-break, and keeping it on `EngineConfig` means the admissions team can A/B it from the admin portal alongside D-023's schedule.
- **Consequences:** **Scores move again, in the direction of the truth.** Every trace-derived number shifts for children whose ability sits outside their band's item range; children inside it are essentially unchanged (the at-seed profile moves by 0.01). Re-running `pnpm exam:bracketing`, the engine's own per-area estimate — the practical ceiling for any trace-derived statistic, and the reference row in D-024's E-078 table — improves from **0.41 mean / 2.32 worst** absolute error to **0.18 / 0.43**. D-024's conclusion is unaffected: accuracy-mode composite error is 5.80/11.46 (was 5.73/11.47) and ability-mode is 0.69/1.78 (was 0.69/1.76), so the accuracy bracket is still miscalibrated by roughly the same margin and the open follow-up D-024 escalated stays open. **D-024's recorded evidence table is now stale in its engine-estimate column and is deliberately not rewritten**, because it is the measurement that was taken at that session; note in particular that its "4-5, equal 13 → engine estimate 15.32" row *was* this defect, visible in a ratified evidence table and not recognised. `EngineConfig` gains one knob and no default changes; `apps/web`'s `EXAM_ENGINE_OVERRIDES` does not set it and inherits 0.5. `nextItem`'s signature, purity and determinism are unchanged, and replay equality is asserted for all eighteen swept traces as well as the seven named profiles. **Not fixed here, and reported rather than compensated for:** (1) the update's confirm/contradict asymmetry described in alternative (a) is still present — a correct answer below the estimate still moves it up by a full step, and a wrong answer just above the estimate is additionally softened by the `M-ERRTYPE` near-miss term (`errType ≈ 1` whenever the item is barely too hard), so the update path is still mildly biased upward; it is no longer *material* because selection now keeps items near the estimate, but it would resurface under any future selection change. (2) `nextType`'s `AGE_BAND_BONUS` (1.5) is the same lexicographic-ish preference one level up, at type granularity; it did not need changing to close this defect and was left alone. (3) The whole mechanism is bank-shaped: it exists because the banks' age-band tags correlate with difficulty. A bank retag can change the size of the residual, which is why the regression is a sweep against the real banks rather than a fixed expectation.
- **Reversal:** No code needs to be reverted. The cost function strictly generalises the rule it replaces: any `ageBandBias` wider than `difficultyWindow` makes an age-band match dominate any targeting difference, reproducing "age-band match first, closeness second" exactly — pinned by a test in `engine.test.ts`, and confirmed end-to-end by the sweep, which returns the pre-change estimates to the digit at every one of the eighteen abilities. Restoring the old behaviour is therefore a policy edit, not a revert.
- **Owner:** Proposed by the exam-engine workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Does **not** revert, weaken or retune D-023 — the step schedule, its five knobs and its defaults are untouched, and the pre-D-023 equivalence test still passes unmodified. D-023's convergence gains are preserved or improved (the gifted battery goes 40 → 36 items). Does not change the metric taxonomy (D-022), the stop rule, the scoring contract or its defaults (D-029, D-024), the claim boundaries, or the born-synthetic status. Does not close D-024's open follow-up. Does not supersede any prior decision.

### D-026 — Served bank items are registered into the database on demand, and the two per-item verdicts are both recorded

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Wire `apps/web` to persist the full adaptive exam trace through the existing `api.exam_*` RPCs, adding exactly one RPC — `api.exam_register_item` — which idempotently admits a generated bank item (with its SERVER-ONLY answer key) into `app.exam_item` before the first response against it is recorded. Registration is INSERT-only, is reachable only by the `admissions_operator` proctor persona, and returns no part of the key. Two supporting changes ride with it: `app.exam_question_type.type_code`'s check is widened to the mixed-case vocabulary D-020(a) ratifies, and `app.exam_question_type` / `app.exam_item` gain INSERT (not UPDATE or DELETE) policies for `api_executor` so a `SECURITY DEFINER` function can write under FORCED RLS. Separately, `/api/exam-submit` now appends an `app_verdict` telemetry event per item recording the verdict and metric map that `packages/exam-engine` and `packages/exam-scoring` actually consumed.
- **Requirements served:** R7 (auditability and reproducible replay), R11 (the in-house screener stores a full trace, `EXAM_ADAPTIVE_BUILD_PLAN.md` §0/§6)
- **Why registration was necessary:** `api.exam_submit_response` resolves `p_item_id` against `app.exam_item` and raises `PT400` when it is unknown. The only rows in that table are the 80 placeholders `20260724130200_exam_adaptive_seed.sql` generates at migration time; the runner serves from the 63 generated banks (7,900 items) which carry their own ids. The two sets are disjoint, so before this change not one real served item could be recorded and no trace existed at all.
- **Alternatives considered:** (a) *Bulk-load all 7,900 bank items in a migration or an ops script*, so the database owns the whole bank as BUILD_PLAN §6 describes — rejected for this change: ~22 MB of generated content in version control, a load step that must be re-run whenever a bank is regenerated, and bank syncing is a separate workstream. It remains available and does not conflict, since registration is a no-op for an item already present. (b) *Rename the 11 mixed-case type codes to satisfy the existing check* — rejected for the same reason D-020 rejected it (churn against the established catalog vocabulary). (c) *Grant UPDATE alongside INSERT on the bank tables* — rejected: an immutable bank means a registered item's answer key cannot be swapped after a response was scored against it.
- **Evidence:** A round trip against the running local stack, 2026-07-25: a 22-item engine-driven battery recorded as 1 session row, 22 `exam_item_response` rows in administration order with raw answers and metric maps intact, 88 `exam_telemetry_event` rows, and one `exam_session_outcome` row with `scorer_source = 'packages/exam-scoring'` and `scorer_input_hash` re-derivable from the stored trace. Reproduced by `apps/web/src/lib/exam/persistence.integration.test.ts`. Everything is born-synthetic (`synthetic_only = true`, `validated = false`).
- **Rationale:** The trace is worthless if it cannot record the items actually served, and lazy registration records exactly those and nothing else. Putting the real key in `app.exam_item.answer_key` — a column revoked from every client role and excluded from every served projection — strengthens rather than weakens the firewall D-029 protects, because it is what lets the database verify at all.
- **Consequences:** D-029's split is preserved: `packages/exam-engine` still selects (`api.exam_get_next_item` is never called on a persisted session), `packages/exam-scoring` still scores and is stored verbatim through `api.exam_record_outcome`, and the demoted `app.exam_compute_outcome` is never called. Persistence is opt-in per environment and every write is best-effort, so an unreachable database degrades to the previous in-memory behaviour rather than failing a child's exam. The proctor persona is authenticated with the synthetic `admissions_operator` fixture seeded by `pnpm db:users` and the app's existing publishable-key convention; no service-role or secret key enters the app runtime. `packages/db-types` regenerates additively.
- **Open defect this exposes, escalated not fixed:** `app.exam_score_response` and the app's per-type verifiers grade the same raw response by different rules and disagree on **15 of 22 items** in the recorded round trip. The database's vocabulary is a comparison against `answer_key.correctKey`/`solution`; 30 of the 63 wired types are graded by a constructed-response verifier that re-derives the expected answer from `content` or from a non-key field of `answer`, 18 declare `computed_solver` but store `correctKey` rather than the `solution` the database reads, and 7 key by numeric option index the database does not read. Because `api.exam_submit_response` writes its own verdict into `exam_item_response.correct/score/metrics['M-ACC']`, and `app.exam_scorer_input_json` projects those columns, re-running `scoreExam` over the database's canonical scorer input yields **3.775** against a recorded composite of **7.434** — the D-029 audit path does not currently reconcile. Reconciling it means either porting 30 typed verifiers into plpgsql or moving verification authority to the application tier, which D-029 considered and rejected as its alternative (c); both are governance decisions, not persistence work. Note D-029(c) was already contradicted in practice before this change, since `/api/exam-submit` has verified against on-disk keys in the Next.js server tier since the runner was wired. The mitigation shipped here is that nothing is silent: the `app_verdict` telemetry rows carry the verdict the score was actually computed from, and re-running `scoreExam` over them reproduces the recorded composite to the last digit (7.43376016008243, difference 0.000000).
- **Reversal:** Additive throughout. Reverting `supabase/migrations/20260725160000_exam_item_registration.sql` drops the RPC, the two INSERT policies, and the widened check (the narrower check must be restored by hand). Setting `GT_EXAM_PERSISTENCE_ENABLED` to anything but `true` disables every write with no code change.
- **Owner:** Proposed by the exam-persistence workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Implements the storage contract in BUILD_PLAN §6 under the ownership D-029 sets; adopts D-020(a)'s type-code vocabulary in the database. Supersedes nothing.

### D-027 — The database is the single authority on per-item correctness: the per-type verifiers are ported to plpgsql

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Port the per-type answer verifiers from `apps/web/src/lib/exam/verifiers/` into plpgsql, behind a dispatcher `app.exam_verify_response(p_item_id uuid, p_raw_answer jsonb)` that resolves a verifier in the same precedence order as `resolveVerifier()` — registered per-type verifier, then the generic named by the item's server-only `scoring.rule`, then the option-key default — and make `api.exam_submit_response` verify through it. `app.exam_score_response` is **demoted, not deleted**, following the convention D-029 established for `app.exam_compute_outcome`. This migration lands the foundation and a proof of the pattern: the dispatcher, all three generic verifiers, and four per-type verifiers (one per bank domain, each deliberately the awkward shape in its domain). The remaining 27 land in later additive migrations, each adding one function and one row to `app.exam_verifier_registry`.
- **Requirements served:** R7 (auditability and reproducible replay), R11 (in-house screener, `EXAM_ADAPTIVE_BUILD_PLAN.md` §0/§2/§5/§6)
- **Owner's rationale (anti-cheat):** Verification that happens inside a schema no client role can reach cannot leak a key, a solution, or a derived expected answer, and cannot be bypassed by a client that reaches the RPC surface directly. Verification that happens in the Next.js server tier depends on that tier being the only caller, which is an operational property rather than a schema-enforced one. This is a security posture, not a performance or convenience choice.
- **Relationship to D-029:** This **implements D-029's stated intent rather than reversing it**. D-029 rejected moving verification into the app tier as its alternative (c) precisely because "the answer-key firewall is only meaningful because key comparison happens inside a schema no client role can reach". Practice had drifted from that: `/api/exam-submit` and `bank-loader.ts` have graded against on-disk keys in the Next.js server tier since the runner was wired, and the database's own comparison had become a second, disagreeing verdict rather than the authority. D-027 restores the database as the authority D-029 named.
- **The defect this closes:** D-026 escalated it and `docs/architecture/EXAM_PERSISTENCE_NOTES.md` §4 documents it. On a real 22-item battery the database's verdict and the app tier's disagreed on **15 of 22 items**, so recomputing the composite from `app.exam_scorer_input_json` — the canonical scorer input the D-029 audit path hashes — gave **3.775** against a recorded composite of **7.434**. The hash was fine; what it hashed was not the input the recorded score came from.
- **Alternatives considered:** (a) *Move verification authority to the app tier and stop verifying in the database* — rejected: this is D-029(c), and it is what the owner's anti-cheat rationale rules out. (b) *Keep both verdicts and reconcile at read time* — rejected: it is what D-026 already shipped as a mitigation, and it leaves `exam_item_response.correct/score/metrics['M-ACC']` — the columns `app.exam_scorer_input_json` projects — holding the verdict nobody scored from. (c) *Widen `app.exam_score_response`'s single comparison to cover more shapes* — rejected: 31 of the served types re-derive the expected answer from `content` or a non-key field of `answer`, so there is no single comparison that covers them; attempting one would produce a third set of rules agreeing with neither side. (d) *Port all 31 in one migration* — rejected as unreviewable and un-parallelisable; the registry table exists so the remaining 27 can land in independent migrations that cannot conflict.
- **Evidence:** E-081. Measured by `pnpm exam:verify:diff` over 63 served types × 12 real bank items × 3 responses (correct / wrong / skipped) = 2,268 cases: the demoted `app.exam_score_response` agrees with the app tier on **1,659/2,268**; `app.exam_verify_response` agrees on **1,989/2,268**. Every one of the 33 generic-verifier types and all four ported types agree on every case, metric maps included, and 444 of those agreements are cases both sides scored **correct**, so the agreement is not the vacuous kind. Every remaining disagreement is an unported per-type verifier, reported as PENDING rather than passing quietly. `pnpm db:test` 335/335 (was 306/306; +29 new), `pnpm db:lint` clean, `pnpm -r test` 718/718 unchanged.
- **What becomes of the app-tier verifiers:** they stay, and they become a **cross-check, not the authority**. `/api/exam-submit` still grades the response it returns to the runner, because the adaptive engine runs client-side and must update on a verdict before the persistence write completes; and it still appends the `app_verdict` telemetry event D-026 added. What changes is that `exam_item_response.correct/score/metrics` — the columns the audit path reads — are now written from the database's own verdict, and the differential harness makes any divergence between the two an observable, failing condition rather than a silent one. Deleting the app-tier verifiers is explicitly **not** part of this decision: two independent implementations that must agree is the property the harness tests, and collapsing to one would remove it.
- **Consequences:** Per-item verdicts change for every type whose database verdict was previously wrong, which is most of them — this is the point, and it means **no stored trace recorded before this migration is comparable to one recorded after it**. `api.exam_submit_response` now merges the verifier's key-dependent metrics (`M-POLY`, `M-PAE`, `M-HYP`, …) into `exam_item_response.metrics` between the client-tracked metrics and the server-authoritative `M-ACC`, so the database can no longer be given a metric map the client invented for a metric only the server can compute. A skipped or timed-out submission is now scored incorrect **without grading**, mirroring `/api/exam-submit`, which is the only way a partially-built artefact submitted at timeout cannot be credited in the database while the app tier calls it wrong. Two pgTAP fixtures in `120_exam_adaptive_backend.test.sql` moved from `{"key":"B"}` to `{"selectedKey":"B"}`: the ported keyed verifier reads the two fields the runner actually sends, and the demoted implementation's acceptance of `key` was never exercised by a client. The answer-key firewall is unchanged in posture and newly asserted in `123_exam_verify_plpgsql.test.sql`: every new function and the registry are revoked from PUBLIC, `anon`, `authenticated` and `service_role`, granted only to `api_executor`, and one assertion fails if any future port forgets its revoke. **Reported and not fixed:** five app-tier verifier behaviours that look wrong (including one metric id, `M-POLY`, carrying opposite directions on different types) are listed in `docs/architecture/EXAM_VERIFIER_PORT_INVENTORY.md` §8 — changing any of them is a scoring-semantics decision this one does not make. **One type cannot be ported without a further decision:** GB-WORDLADDER-01 judges a rung against a 4,000-word child lexicon read off disk, which is not a bank field and so is not in `app.exam_item`; §6 of the inventory sets out the three options and recommends none of them on the owner's behalf.
- **Reversal:** Additive and forward-only. Nothing is dropped: `app.exam_score_response` is unchanged and still granted to `api_executor`, and pgTAP assertion 8 in `123_exam_verify_plpgsql.test.sql` fails if that stops being true. To restore it, change the one call site in `api.exam_submit_response` back to `v_scored := app.exam_score_response(p_item_id, p_raw_answer);` and drop the metric merge. To retire a single ported verifier without touching code, delete its row from `app.exam_verifier_registry` and the dispatcher falls back to the generic chain for that type.
- **Owner:** Decided by the project owner (session 2026-07-25) on the anti-cheat rationale above; implementation proposed by the exam-verification workstream and requires team-lead ratification.
- **Relationship to prior decisions:** Implements D-029's alternative-(c) reasoning rather than reversing it, and closes the defect D-026 escalated. Does not change selection ownership (D-029: `packages/exam-engine` still selects), the final score (D-029/D-024: `packages/exam-scoring` output is still stored verbatim), the stop rule, the metric taxonomy (D-022), the age-band pricing (D-025), the claim boundaries, or the born-synthetic status. Supersedes nothing.

### D-028 — The child-facing screener hides the per-child telemetry panel; `?telemetry=1` retains it for demonstration

- **Date:** 2026-07-25
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Every renderer demo hides its `Live telemetry · per-child` sidebar when it is embedded in a host (the exam runner), unless its own URL carries `?telemetry=1`. Opened standalone by a researcher the panel is shown exactly as before. The gate is presentational only: sampling, metric computation, the `metrics` map on the `ItemResult`, and the `{type:'telemetry', event}` postMessage stream are untouched, so the recorded trace is byte-for-byte what it was.
- **Requirements served:** R5 (a defensible capability standard depends on measuring the construct rather than the child's response to being told what is measured), R9 (a child taking a scored assessment is not shown internal tooling), H10 (a metric the child can read is a metric the child can perform for); R7's auditability is preserved because nothing stops being recorded
- **Why this is a validity decision, not a cosmetic one:** The panel does not merely look internal — it *names the behavioural construct as it is being measured*, beside its live value. `M-PLANFUL` is labelled "Planful: pause before first move"; `M-ENGAGE` is "Focus losses (tracked, not enforced)"; `M-REV` is "Revisited cells". A child who can read that a pause before the first move is being scored has been given a reason to pause that has nothing to do with planning, and the metric stops measuring planning. The same reactivity applies to every process metric on the panel, and it is worst for exactly the metrics that are meant to distinguish deliberate problem-solving from guessing — the ones the profile leans on hardest. The appearance problem (a scored child assessment that looks like a debugging view) is real but secondary; it would not by itself justify a change to all 63 served demos.
- **Alternatives considered:** (a) *Delete the panel from the demos* — rejected: it is the researcher's only live read of the renderer, and standalone use is an explicitly protected behaviour. (b) *Strip the panel in `scripts/sync-exam-demos.mjs` at copy time* — rejected: the script's own `COMPAT_PATCHES` docblock records that a copy-time patch is a latent divergence between what ships and what the demo author sees, and it would also make the panel unreachable for a live demonstration of the real runner. (c) *Host-driven toggle over postMessage* — rejected as heavier than the problem: it needs a protocol addition, and the panel would still be visible for the frames before the host's message lands. (d) *A per-demo bespoke edit* — rejected; see the mechanism note below.
- **Mechanism (uniform across all 66 source demos, 63 of them served):** One `<style>` rule plus one guard script, copied verbatim into every demo's `<head>`. The rule is `html[data-gt-telemetry="off"] #telemetry{display:none}`; the guard sets that attribute only when the demo is framed and the flag is absent, and treats a cross-origin parent (which throws on access) as framed. This was possible because the markup is uniform in the two ways that matter: `#telemetry` is a direct child of `<body>` in all 66 demos, and every `body` is a flex row with the activity column on `flex:1`, so removing the panel from flow lets the activity take the freed width with no grid hole to patch. No demo needed special handling. The demos' own `embedded` flag is deliberately not reused — it is spelled seven different ways across the corpus and is declared in the body script, after first paint.
- **Evidence:** E-082.
- **Consequences:** The override lives on the demo's own URL, which is what a researcher loads when demonstrating a renderer directly. The runner requests `/exam-demos/<CODE>.html` with no query string and does not forward one, so turning the panel back on *inside a live runner session* is not possible without a follow-up change to `demoPathFor`; that was out of scope here and is recorded as the known limit of this decision. A new demo that ships a panel without the gate is caught by `apps/web/src/lib/exam/telemetry-panel-gate.test.ts`, which checks source and published copies.
- **Reversal:** Deleting the two `<head>` lines from the demos and re-running `node scripts/sync-exam-demos.mjs` restores the previous behaviour exactly; the panel markup, its metric list, and every emission path are unchanged, so there is nothing else to undo. Removing only the guard script leaves the panel visible everywhere; removing only the style rule leaves it visible everywhere. Deleting the regression test is required for either.
- **Owner:** Proposed by the exam question-type workstream (session 2026-07-25); requires team-lead ratification.
- **Relationship to prior decisions:** Complements D-017's prohibition on shipped text that misleads a child, and sits alongside E-079's fix for the same class of defect (developer-facing content reaching a child mid-assessment) without touching the standalone-fallback rule that fix installed. Does not change the metric taxonomy (D-022), selection (D-025), scoring (D-029, D-024), or persistence (D-026). Supersedes nothing.

### D-030 — Learning rate is measured once per session over a novel block in one area, and is reported as an ordinal band or not at all

- **Date:** 2026-07-28
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Three parts, which stand or fall together.
  1. **A learning rate is a session-level figure over ONE area, not four per-area figures.** A dedicated novel block of 30 unseen items runs in a single area, fixed by policy for every child, and only after that area's standing estimate has settled (`areaEstimateStable`). `M-LEARNRATE` moves from `enforced: true, minSamples: 8` per area to `enforced: false, minSamples: 30`, so the battery stop rule no longer demands a growth fit in all four areas.
  2. **Every item records which regime served it.** `stage: 'standing' | 'learning'` is added to `ItemObservation`, `ScoredItem` and the contract, defaulting to `standing`. The learning-rate fit reads `learning` items only. `stage` is server-added, not part of `itemResultSchema`, so a client cannot relabel its own trace into or out of the fit.
  3. **The reportable output is an ordinal band, and `indeterminate` is a first-class answer.** The rate is fitted by a MAP logistic learning curve (`theta(t) = theta0 + lambda * t`, 1PL matching `deriveAbilityEstimate`) and reported through `learningRateReadout`, which returns `below | typical | above | indeterminate` with a reason. It returns `indeterminate` when the block is short, when the fit does not converge, or when the posterior SE is at or wider than the band half-width. The `[0,1]` block-half value from `deriveLearningRate` is retained as a labelled diagnostic and is not a reported score. Neither figure enters `scoreExam`'s output.
- **Requirements served:** R7 (auditability — the stage marker is what makes a rate recomputable from the stored trace), R9/R10 (claim boundaries — an ordinal band with an explicit refusal case, `validated=false`), R8 (feasibility — 30 items in one area rather than 32+ across four)
- **Alternatives considered:** (a) *Keep the per-area growth index and lengthen each area* — rejected on measurement: four 15-trial blocks recover at r ≈ 0.18 each and cost more items than one 30-trial block at r ≈ 0.45, so splitting the budget buys strictly less for strictly more (E-095). (b) *Keep it enforced per area at 8 samples and just report it more cautiously* — rejected: at 8 trials recovery is r ≈ 0.07 and the estimate is attenuated to a fifth of its true size, so no amount of caution in the wording makes the number informative, and enforcing it held the stop rule hostage to producing it. (c) *Pick each child's strongest area for the block* — rejected: a rate measured in different areas for different children is not comparable, and comparability is the only thing the figure is currently good for. (d) *Report a numeric rate with a confidence interval instead of a band* — rejected: at 30 trials the interval is wider than the spread children are thought to occupy, so the interval would be the entire scale and the point estimate would be read anyway. (e) *Report a band using a synthetic reference distribution* — rejected as the most dangerous option: it would put a made-up mean and SD behind a sentence about a child, which is why `LearningRateReference` has no defaults and must be supplied by the caller. (f) *Drop the learning-rate construct entirely* — not taken, because the block is cheap to administer and ordering a measured cohort is supportable now; but this remains the correct outcome if real data shows the between-child spread is narrower than any feasible block can resolve.
- **Evidence:** E-095 (recovery, attenuation and SE by block length; the confound demonstrated in this engine and shown not to affect the difficulty-conditioned fit). Builds on E-073, which reached the same conclusion in logit units on the retired engine.
- **Rationale:** A growth statistic computed over a bracketing trace measures the algorithm, not the child. While an area is still homing in, the hardest difficulty solved rises as the search converges on a child seeded away from their level — so a child who learned nothing reports growth, and reports MORE growth the further their seed started from them. The block removes that confound structurally rather than by correction: it begins after the search has finished, so the only thing left that can move is the child. Everything else follows from measuring the result honestly. At the block length the evidence supports, one child's estimate is less precise than the differences between children are believed to be, which is exactly the condition under which a band may be named for a cohort and must not be named for an individual.
- **Consequences:** Expect `indeterminate` on real sessions and treat it as correct output, not a defect — no reference distribution for lambda exists, and E-095 stays open until there is real per-child data at two or more occasions. Two named limits carry forward. The fit assumes a **linear** climb while empirical learning curves are commonly exponential or power-shaped, so the recovered slope summarises a possibly-wrong curve. And the response model uses **guessing = 0** to stay consistent with the standing fit, which is a known misspecification on four-option multiple choice; it is exposed as an option with that noted. Adding 30 items in one area lengthens the battery, and whether that is affordable against a child's attention is a product decision this does not settle. 45–60 trials is where the statistic becomes genuinely sharp (r ≈ 0.75–0.86), so if a rate is ever needed for an individual rather than a ranking, the block has to grow rather than the estimator improve.
- **Reversal:** Additive and cleanly separable. Setting `M-LEARNRATE` back to `enforced: true, minSamples: 8` in `packages/exam-engine/src/config.ts` restores the previous stop-rule behaviour exactly; `deriveLearningRate` was never changed, only re-labelled, so the old number is still computed by the same code. `stage` defaults to `standing`, so every trace, fixture and stored session predating this remains valid and re-scores identically — nothing needs backfilling. Deleting `learning-block.ts`, `learning-curve.ts` and `learning-rate-readout.ts` with their tests and the two barrel exports removes the new surface without touching selection, the standing fit, or `scoreExam`.
- **Owner:** Proposed by the exam structure workstream (session 2026-07-28); requires team-lead ratification.
- **Relationship to prior decisions:** Narrows the metric registry set by D-022 (one metric's enforcement and threshold) without changing the taxonomy. Respects D-029's ownership split: the fit and readout live in `packages/exam-scoring`, block administration in `packages/exam-engine`, and the database gains no new authority — the two packages remain mutually independent per BUILD_PLAN §7, so the target difficulty crosses between them as a plain number. Does not change selection (D-025), the standing bracketing (D-023), persistence (D-026), or verification (D-027). Supersedes nothing.

### D-031 — `M-PAE` and `M-REV` are unenforced in quantitative, because the retired banks were their only quantitative suppliers

- **Date:** 2026-07-29
- **Status:** Proposed (awaiting team-lead ratification)
- **Decision:** Two metrics change enforcement, and nothing else about them changes. `M-PAE` (proportional absolute error) becomes `enforced: false` in `quantitative`. `M-REV` (revision count) is split from one `scope:'all'` entry into four per-area entries, staying `enforced: true` in `fluid_reasoning`, `verbal` and `spatial` and becoming `enforced: false` in `quantitative`. Both are still declared, still counted wherever a type supplies them, and still bias selection toward coverage variety; they simply no longer gate the session stop rule in quantitative.
- **Requirements served:** R5 (the capability standard rests on the metric set actually being obtainable), R7 (auditability — the reason a battery ended is now the stop rule again, not an unsatisfiable coverage demand), R8 (feasibility — an unsatisfiable enforced metric pins every battery to the 60-item hard cap), R10 (boundaries — a metric no surviving type can emit must not be presented as measured)
- **Alternatives considered:** (a) *Keep both enforced and accept the cap* — rejected: it makes every child sit the maximum-length battery for a coverage target that can never be met, which is the worst outcome for the child and produces no extra information. (b) *Restore `QUANT-NUMLINE-01` and `QUANT-EQUAL-01`* — rejected: the reviewer pass retired them on construct grounds (`QUESTION_IMPROVEMENT_PLAN.md` §5), and resurrecting a type to satisfy a metric inverts the dependency. (c) *Add a new quantitative type that emits number-line placement* — rejected as out of scope here and as the wrong shape of decision to take inside an implementation task; it is the correct fix later, and the code comments name it as the re-enforcement trigger. (d) *Delete the two metrics* — rejected: they are still supplied and still informative in the areas that have suppliers, and deleting `M-PAE` would discard the taxonomy slot a future placement type should occupy.
- **Evidence:** The engine's own sole-source audit (`auditMetricSupply`) predicted exactly this failure and had recorded both metrics as sole-sourced before the deletions. Observed: 33 engine tests failed on the deletion commit until enforcement was narrowed; `packages/exam-engine` is at 132/132 after.
- **Rationale:** `areaMetricsCovered` is false for an area while any enforced metric there is short of `minSamples`. A metric with zero suppliers in an area can never reach `minSamples`, so the flag is permanently false and the battery can only end at `hardItemCap`. This is the same situation, and the same remedy, as `M-ROTSLOPE` in spatial: declared, derived or observed where it happens to be available, not allowed to hold the stop rule hostage. Narrowing enforcement is a smaller claim than the alternative — it says "we do not measure this here", which is true, rather than "we require this here", which was unachievable.
- **Consequences:** Quantitative coverage is now checked against a smaller enforced set, so a quantitative area can be declared covered with less process-metric breadth than before the deletions. That is a real reduction and it should be read as a consequence of retiring four quantitative types, not as an independent loosening — quantitative is the thinnest domain after the review (8 types). Re-enforcement triggers are named in the code comments: a quantitative type that records a revision count restores `M-REV`; a number-line placement type restores `M-PAE`.
- **Reversal:** Set `enforced: true` on the `quantitative` entries in `packages/exam-engine/src/config.ts` and collapse the four `M-REV` entries back to one `scope:'all'`. No data, scoring path, or stored session depends on the change, so nothing needs backfilling; the engine tests that failed before will fail again, which is the intended signal that the supply problem is unresolved.
- **Owner:** Proposed by the exam question-type workstream (session 2026-07-29); requires team-lead ratification.
- **Relationship to prior decisions:** Narrows the enforcement of two metrics in the registry set by D-022 without changing the taxonomy, in the same way D-030 narrowed `M-LEARNRATE`. Follows the `M-ROTSLOPE` precedent already in `config.ts`. Does not change selection (D-025), scoring (D-029, D-024), persistence (D-026), or verification (D-027). Supersedes nothing.

### D-032 — Reconcile the forked governance registers onto `dev` and band governance-ID allocation per lineage

- **Date:** 2026-07-30
- **Status:** Approved
- **Decision:** Restore onto `dev` the governance that was ratified on 2026-07-24 but stranded on unmerged branches, and adopt the dual-aim framing it carries. Four parts. (1) **Requirement:** add `R11` (scalable, tunable, GT-validated screening instrument) to `docs/product/project-requirements.md`, reframe `R5` to a capability-and-fit standard, restate the core success statement as a dual aim (a scalable screening decision *and* a credible counterfactual), and update the three non-requirements the reframe inverts — including that the screener itself is now in scope, tunable, with GT owning the cut. Port the matching mission, goals 7–8, and non-goals into `PROJECT_CHARTER.md`. (2) **Decisions:** transcribe `D-015` and `D-016` verbatim into this log at their original numbers, closing the `D-014 → D-017` gap. (3) **Evidence:** keep `dev` canonical for `E-071`–`E-077`, which were independently consumed while the other lineage was unmerged, and re-register the seven GT admissions-director interview entries as `E-096`–`E-102`, re-pointing every citation in the transcribed R11/`D-015`/`D-016` text. (4) **Prevention:** allocate `D-` and `E-` IDs from disjoint per-lineage bands recorded in `AGENTS.md`, require governance-register edits to reach `dev` on the same path as the code they authorize, and run `scripts/audit-requirement-ids.mjs --strict` as a blocking CI check so orphaned citations and duplicate definitions fail the build.
- **Requirements served:** R7 (auditability — an independent reviewer can now resolve every requirement, decision, and evidence citation in the merged tree, and the gate keeps it that way), R3 (the requirement-ID discipline is what stops the success definition drifting after results are seen), R11 (the requirement this restores); bounded by R10, whose claim boundaries are unchanged — restoring a requirement establishes nothing about the screener's validity.
- **Alternatives considered:** (a) *Retire `R11` as a phantom* — rejected: the audit established it was deliberately created and ratified in `D-015`, so retiring it would void an Approved owner decision, invalidate 272 citations across 39 files including nine shipped migrations, and leave the exam workstream with no requirement to trace to. (b) *Remap the `R11` citations to `R5`* — rejected: `R5` covers the capability standard but says nothing about operating at thousands-scale or GT owning a tunable cut, which is R11's whole substance. (c) *Transcribe `R11` alone, deferring the charter and `R5` reframe* — rejected: it would leave a requirement whose parent success statement does not mention it. (d) *Renumber `dev`'s `E-071`–`E-077` instead of the interview block* — rejected: those entries are already merged and cited on `dev`, so moving them would break working citations to fix broken ones. (e) *Fix the collisions without banding allocation* — rejected: the same race had already produced `D-019`, `E-072`, and `E-073` collisions plus a five-entry register hole, and would recur on the next parallel night.
- **Evidence:** `docs/governance/REQUIREMENT_ID_AUDIT.md` (2026-07-30 audit of all four ID families against `dev` @ `51e10c8`; origin commit `51776da`, follow-up `eef1d28`, neither an ancestor of `dev`); `D-015` and `D-016` as the ratifying decisions; `E-096`–`E-102` as the underlying interview evidence; `docs/OVERNIGHT_GOVERNANCE_DIVERGENCE.md` §1a, which reached the "stranded, not invented" finding on 2026-07-28 and preserved both entries verbatim. No new empirical claim is introduced: every factual assertion in the audit is a verified property of this repository, reproducible from the commands it records.
- **Rationale:** The failure was structural, not editorial. The exam implementation was merged onto `dev` along one path while the governance authorizing it travelled another and was never merged, so `dev` accumulated code that cited requirements, decisions, and evidence it did not define. Transcription rather than reinvention is correct because the definitions, both traceability row sets, and the ratifying decisions all existed and only needed porting. It was not mechanical because all seven of `E-071`–`E-077` had come to mean different things on the two lineages, so a verbatim port would have produced citations that resolve to the wrong facts — silently, since the IDs exist. Banding allocation addresses the cause: contributors on parallel branches cannot see each other's registers, so a shared counter guarantees eventual collision, whereas disjoint bands make the common case safe without coordination and the CI gate catches the residual same-band case.
- **Consequences:** `PROJECT_CHARTER.md`, `docs/product/project-requirements.md`, `docs/governance/DECISION_LOG.md`, `docs/research/ASSUMPTIONS_AND_EVIDENCE.md`, `docs/product/TRACEABILITY_MATRIX.md` (R11 coverage row, reframed R5 row, `GOV-007`, `GOV-008`, `RES-012`, `RES-013`, `TECH-004`), `docs/product/FEATURE_TO_REQUIREMENT_MAP.md` (R11 row and the `AX-01`–`AX-06` section), `AGENTS.md`, and `.github/workflows/ci.yml` are updated. The project now carries a dual aim, so future work must serve either the screener or the counterfactual and say which. `packages/exam-scoring/METRICS_BASIS.md`, `research/exam-question-types/METRIC_FRAMEWORK.md`, and `research/exam-question-types/PS_GAMEBASED_WARRANT.md` no longer claim the screener has "no human in the loop", which contradicted R11's preserved near-miss and shadow-day path. `E-085`–`E-089` remain permanently unallocated. Two dated audit records (`docs/GENB_VERIFICATION_2026-07-28.md`, `docs/HANDOFF_2026-07-28.md`) keep their point-in-time assertions and gain forward-pointers rather than rewrites. Deferred, unchanged: `RES-012` (GT-data validation study) and `RES-013` (brainlift and catalog re-orientation) both still await GT data. Does not supersede `D-014` (external CogAT), `D-011`/`D-012` (architecture/platform), `D-006` (born-synthetic scope), or `D-027` (database correctness authority). <!-- id-audit:ignore-line: names the permanently unallocated E-085–E-089 range -->
- **Owner:** Team lead
- **Relationship to prior decisions:** Restores `D-015` and `D-016` to the canonical log without altering what they decided; closes the open governance task recorded in `D-017`'s governance note; supersedes nothing. `D-019`, `E-072`, and `E-073` remain as `dev` holds them, per the audit's recommendation to keep merged entries canonical.

## Entry template

### D-XXX — Decision title

- **Date:** YYYY-MM-DD
- **Status:** Proposed / Approved / Superseded / Rejected
- **Decision:** Concise statement
- **Requirements served:** R/H IDs
- **Alternatives considered:** Options evaluated
- **Evidence:** Register IDs or source links
- **Rationale:** Why this choice best serves the requirements
- **Consequences:** Scope, risks, and follow-up work
- **Owner:** Named decision-maker
- **Supersedes:** Prior decision ID, if applicable
