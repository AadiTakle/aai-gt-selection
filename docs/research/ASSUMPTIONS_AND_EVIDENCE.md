# Assumptions and Evidence Register

## Evidence classes

- **Verified research finding:** Supported by a checked primary or strong secondary source.
- **Independent context:** Reported by an independent source, but not proof of program impact.
- **Company claim:** Statement made by GT School, Alpha School, or an affiliated party.
- **Reasoned inference:** Synthesis that must not be presented as a source's direct finding.
- **Open assumption:** Unverified condition that must be tested before dependent work proceeds.

Do not silently promote a claim from one class to another.

## Register

| ID | Claim | Class | Source or validation path | Consequence if false | Owner | Status |
|---|---|---|---|---|---|---|
| E-001 | Publicly available GT outcome reporting lacks a documented credible comparison group. | Independent context | BrainLift 5.4; confirm with current GT materials | The problem framing may need narrowing if a strong unpublished evaluation exists | Evidence owner | Needs current GT confirmation |
| E-002 | GT School has used a CogAT threshold near the 90th percentile. | Independent context | BrainLift 5.3; confirm current admissions policy | Selection-design assumptions may target an outdated process | Admissions liaison | Unconfirmed current policy |
| E-003 | Tuition or family financial resources materially shape access. | Independent context and inference | BrainLift 5.3 and 6.3; obtain current price, aid, and voucher rules | Financial selection may be overstated or mischaracterized | Financial/access owner | Needs current data |
| E-004 | Alpha reports approximately 2.6× MAP growth. | Company claim | BrainLift 5.2; company materials | Must not be used as established effect size or power input without data verification | Evidence owner | Claim under test |
| E-005 | A single hard cognitive screen produces meaningful false negatives. | Verified research finding | BrainLift 1.1–1.3; source register Tier A | Multi-measure selection may offer less benefit than expected | Selection owner | Supported |
| E-006 | Prior ability and achievement remain predictive, including at high levels. | Verified research finding | BrainLift 1.5, 2.3, 4.5 | Lowering or abandoning capability evidence would be less defensible | Selection owner | Supported |
| E-007 | Broad grit and growth-mindset measures are weak standalone selection signals. | Verified research finding | BrainLift 4.1–4.3; Credé and Sisk evidence | Motivation-based selection may deserve reconsideration if stronger local validity is shown | Selection owner | Supported |
| E-008 | Spatial ability contributes distinct predictive information and can be under-selected. | Verified research finding | BrainLift 1.6 and 4.5 | Spatial measures may not justify added burden | Selection owner | Supported with transfer caveat |
| E-009 | Random assignment among qualified applicants best balances observed and unobserved applicant traits. | Verified methodological finding | BrainLift 3.8–3.9 | A nonrandom design would need different identification assumptions | Evaluation owner | Supported |
| E-010 | GT will have enough eligible applicants and constrained seats for the selected design. | Open assumption | Obtain current funnel, capacity, and yield data; first direct evidence is E-400 | Some causal designs may be infeasible or underpowered | Operations owner | **Contradicted for the current cycle (2026-07-30).** E-400 reports that GT Anywhere admits essentially all applicants and that physical capacity opens on demand, so the seat-constraint half of this assumption does not hold today. Downgraded from `Unverified` rather than closed, because the applicant-volume half is untested and scarcity could return as demand grows. Randomized designs depending on it are non-binding under D-400 |
| E-011 | GT will permit independent evaluation, preregistration, data access, and null-result publication. | Open assumption | Written agreement with authorized GT sponsor | Independent causal evaluation may be impossible | Team lead | Unverified |
| E-012 | A suitable high-ceiling outcome instrument is available and operationally feasible. | Open assumption | Psychometric and operational validation | Growth may be unmeasurable for the target population | Measurement owner | Unverified |
| E-013 | The selection product can be operated within GT's calendar and staffing constraints. | Open assumption | Current process map and load assessment | Product concept may require narrowing or redesign | Operations owner | Unverified |
| E-014 | Broader access can be added without materially harming current high-performing students. | Open assumption | Program-capacity analysis and guardrail measurement | Cohort or shared-program design may need protection or staging | Program owner | Unverified |
| E-015 | CogAT Form 8 measures verbal, quantitative, and nonverbal reasoning—including Paper Folding—but does not directly measure achievement or broad dedicated spatial, working-memory, or processing-speed constructs. | Official test documentation; independent transfer caveat | `COGAT_GAPS_AND_TEST_SUITE_REPORT.md`; obtain proprietary Form 8 technical manual | Product may target nonexistent gaps or double-count overlapping measures | Selection owner | Supported for construct coverage; current tail evidence restricted |
| E-016 | One program-relevant MAP Growth subject is the strongest scalable achievement complement identified for a grades 2–12 two-test prototype, but threshold-specific precision and incremental validity after CogAT are not established for GT. | Research synthesis, not ratified decision | Current NWEA technical/norm manuals plus local prospective validation | Added testing may increase burden without improving selection | Selection and measurement owners | Candidate requiring local validation |
| E-017 | WJ V Cognitive and WISC-V can add individual profile, spatial, working-memory, and processing-speed information for targeted finalists, but examiner capacity and overlapping constructs limit universal use. | Research synthesis, not ratified decision | Current technical manuals and independent reviews; local workflow validation | Finalist assessment could become an expensive second false-negative gate | Selection and operations owners | Candidate targeted pathway |
| E-018 | Nonverbal-test substitution alone does not remove ELL, cultural, SES, referral, or opportunity effects. | Verified research finding with older-form transfer limits | Lohman et al. 2008; Card & Giuliano 2016; Peters et al. 2019 | Product could claim equity improvements unsupported by the test change | Access and evidence owners | Supported; Form 8-specific evidence limited |
| E-019 | Parent and teacher nominations contain useful contextual information but converge only moderately with direct measures and cannot defensibly gate access to assessment. | Meta-analytic and direct gifted-identification evidence | Marsili & Pellegrini 2022; Acar et al. 2016; McBee et al. 2016 | A nomination requirement could recreate large false-negative and advocacy effects | Access and selection owners | Supported |
| E-020 | A substantial share of gifted teacher-rating variance can be attributable to the teacher rather than the student. | Direct contemporary gifted-rating evidence | McCoach et al. 2024 | Common teacher-rating cutoffs could operate differently across raters | Selection owner | Supported |
| E-021 | Unstructured recommendations are weak incremental predictors and can encode advocacy, prestige, and demographic language differences; structured formats reduce but do not eliminate these problems. | Meta-analysis/systematic review with adult-selection transfer limits | Kuncel et al. 2014; Deshpande et al. 2024; Kim et al. 2025 | Parent/recommender prose may be mistaken for child capability | Access and selection owners | Supported with transfer caveat |
| E-022 | Common-condition performance tasks and scripted dynamic assessment are promising complements because they sample service-relevant performance and learning response, but their validity is task/procedure specific. | Meta-analytic, review, and adjacent selection evidence | Roth et al. 2005; Swanson & Lussier 2001; Caffrey et al. 2008 | Prototype scores could be overgeneralized into broad giftedness claims | Selection and evaluation owners | Candidate requiring local validation |
| E-023 | Freeform holistic review is less reproducible than structured pathways, anchored rubrics, independent raters, and mechanical combination. | Adjacent selection evidence plus gifted decision-rule research | Kuncel et al. 2013; Levashina et al. 2014; McBee et al. 2014 | More evidence could increase discretion and privilege rather than validity | Product and selection owners | Supported with transfer caveat |
| E-024 | Accommodation-use noninterference does not establish construct preservation or equal access. | Verified methodological finding | AERA/APA/NCME 2014; Sireci et al. 2005; `ACCESSIBILITY_AND_MEASUREMENT_FAIRNESS.md` | The product could overstate a software firewall as psychometric fairness | Access and measurement owners | Supported |
| E-025 | Translation requires cultural adaptation, cognitive pretesting, route lineage, and empirical confirmation; professional translation alone does not establish equivalence. | Verified methodological finding | ITC 2017; van de Vijver & Tanzer 2004; `ACCESSIBILITY_AND_MEASUREMENT_FAIRNESS.md` | Multilingual applicants could face construct or rater differences hidden by translated wording | Access and measurement owners | Supported |
| E-026 | Artifact and narrative routes have no established equivalent decision validity. | Open assumption | Matched-route pilot, reviewer/route analysis, and independent psychometric review | A “no penalty” configuration could be mistaken for route parity | Selection owner | Unverified |
| E-027 | The prototype can meet WCAG 2.2 AA and provide construct-preserving accessible routes. | Open assumption | Manual keyboard/screen-reader testing, user testing, and qualified construct review | R9/H7 claims and live participation would remain incomplete | Product and access owners | Unverified |
| E-028 | Contestability requires more than an explanation; participation and an effective remedy are distinct procedural safeguards. | Verified adjacent methodological and HCI finding | Lyons et al. 2021; Yurrita et al. 2023; `EXPLANATION_AND_CONTESTABILITY.md` | A transparent notice could be mistaken for a usable right to challenge error | Student/family advocate | Supported with transfer caveat |
| E-029 | Human involvement alone does not establish meaningful review; competence, authority, relevant information, independence, and resistance to automation bias matter. | Verified adjacent research finding | Yurrita et al. 2023; Sterz et al. 2024; Bahner et al. 2008 | A nominal human check could legitimate rather than correct errors | Operations and student/family owners | Supported with transfer caveat |
| E-030 | Factual/procedural correction, substantive rubric appeal, and later-cycle re-entry require different grounds, evidence, reviewers, and remedies. | Research synthesis requiring GT policy | Lyons et al. 2021; Gilliland 1993; NIST AI RMF; `EXPLANATION_AND_CONTESTABILITY.md` | One generic appeal route could enable advocacy, mix evidence versions, or deny valid correction | Product and student/family owners | Candidate policy; unresolved |
| E-031 | Feature-changing algorithmic recourse can create gaming incentives and unequal adaptation burdens and is unsuitable as admissions correction. | Verified methodological finding with domain transfer | Karimi et al. 2021; Barocas et al. 2020; Milli et al. 2019 | “How to qualify” guidance could reward resources and contaminate the construct | Selection owner | Supported with transfer caveat |
| E-032 | Privacy risk includes authorized but problematic data actions and consequences, not only unauthorized access or security breach. | Verified methodological finding | NIST Privacy Framework 1.0; NISTIR 8062 | Security controls alone could be misrepresented as protection from secondary use or loss of control | Privacy owner | Supported |
| E-033 | Pseudonymization and the label “synthetic” do not establish anonymity; sparse/outlier records can remain linkable or leak source membership. | Verified security research finding | NIST SP 800-188; Stadler et al. 2022; Chen et al. 2020 | A public fixture/export could expose a real child if derived from source records | Privacy and evidence owners | Supported |
| E-034 | Born-synthetic fixtures created independently of real child records materially reduce source-record leakage risk but do not replace access, provenance, or lifecycle controls. | Research synthesis | Synthetic-data leakage evidence plus fixture provenance validation | The prototype could ingest real or lightly modified child material under a synthetic label | Product and privacy owners | Supported as prototype boundary |
| E-035 | Append-only auditability and finite retention must be designed together; immutability does not justify indefinite payload retention. | Verified standards-based synthesis | NIST Privacy Framework; NIST SP 800-53; NIST SP 800-88 Rev. 2 | Audit architecture could make deletion/retention promises impossible | Technical and privacy owners | Supported |
| E-036 | Audit logs are sensitive datasets and require typed minimization, access control, retention, and deletion. | Verified standards-based finding | NIST Privacy Framework CT.DM-P8; NIST SP 800-53 AU controls | Logs could become an uncontrolled copy of scores, narratives, identifiers, or secrets | Technical owner | Supported |
| E-037 | Service-role / owner / `BYPASSRLS` credentials bypass RLS and are unsuitable for ordinary request or decision execution. (Originally stated for Supabase's service-role key; under D-012 the same holds for any AWS RLS-bypassing credential — owner/`BYPASSRLS` DB role or broad IAM.) | Verified vendor/platform fact | PostgreSQL RLS documentation; AWS RDS/IAM documentation (formerly Supabase security docs) | RLS acceptance tests could provide false assurance while privileged requests bypass them | Technical owner | Supported |
| E-038 | COPPA, FERPA, PPRA, and Texas privacy applicability depends on operator, funding, entity, user, and data-flow facts unresolved for GT. | Official-source legal-context finding; not legal advice | FTC COPPA Rule; 34 CFR Parts 98/99; Texas Codes 503/521/541/32 | The prototype could overclaim legal compliance or omit applicable duties | Privacy/legal owner | Unverified applicability |
| E-039 | Self-service user attributes cannot be an authorization source; identity must come from a verified token subject and business roles from an admin-controlled claim. (Originally: Supabase `user_metadata` + `auth.uid()`. Under D-012: Cognito `sub` + admin-controlled `custom:user_role`, surfaced to SQL as `current_setting('app.user_id')`/`app.user_role`.) | Verified vendor/platform fact | Amazon Cognito token/claims documentation; PostgreSQL RLS docs; `RLS_AND_AUTH_BLUEPRINT.md` (formerly Supabase Auth docs) | A user could self-assign reviewer/operator authority or access another application | Technical owner | Supported |
| E-040 | `SECURITY DEFINER`, owner-rights views, permissive grants, and table-owner/BYPASSRLS execution can bypass intended RLS unless ownership, search path, grants, and invoker semantics are explicit. | Verified platform fact | PostgreSQL RLS/function/view documentation; Supabase RLS documentation | RLS tests could pass while RPCs or views expose unauthorized data | Technical owner | Supported |
| E-041 | pgTAP metadata/behavior tests need complementary direct Auth/PostgREST tests with real local JWTs to validate token issuance, claims, API boundaries, and stale-role behavior. | Verified tooling guidance and research synthesis | Supabase local testing/RBAC documentation; `RLS_AND_AUTH_BLUEPRINT.md` | Database-only tests could miss forged/stale claims or API-layer IDOR | Technical owner | Supported |
| E-042 | Deterministic replay requires a normative canonical byte profile; PostgreSQL `jsonb` rendering, unordered queries, locale-dependent formatting, and ordinary `JSON.stringify` object order are not sufficient replay contracts. | Verified technical standards/platform finding | RFC 8785/8259/7493; PostgreSQL 18 JSON/ordering documentation; `CANONICALIZATION_AND_REPLAY_BLUEPRINT.md` | Semantically identical or adversarial inputs could hash inconsistently or collide through lost duplicate-key evidence | Technical owner | Supported |
| E-043 | Exact replay requires retained canonical inputs, policy, executable artifact, and relevant environment; matching hashes alone establish only byte integrity. | Verified reproducibility synthesis | W3C PROV; FIPS 180-4; SLSA provenance; `CANONICALIZATION_AND_REPLAY_BLUEPRINT.md` | The system could report successful replay without independent re-execution | Technical and audit owners | Supported |
| E-044 | SHA-256 commitments and internal hash chains do not establish authenticity or protect against an owner who can rewrite data and hashes; owner-level tamper detection requires a prior independently retained checkpoint. | Verified cryptographic/system boundary | FIPS 180-4; RFC 2104; PostgreSQL owner/RLS limits | “Tamper evident” could be overstated beyond application-role controls | Technical owner | Supported |
| E-045 | Disposed decision inputs make exact replay impossible; a retained digest cannot reconstruct or presently verify absent payloads. | Verified logical consequence and standards synthesis | NIST lifecycle guidance; replay definitions in `CANONICALIZATION_AND_REPLAY_BLUEPRINT.md` | Retention/deletion workflows could falsely claim replay success | Privacy and audit owners | Supported |
| E-046 | The BrainLift's intended evaluation is two-stage: randomized Track B offers identify the Track B initial-offer effect, while treated Track A/Track B comparison tests service-fit noninferiority. | Verified project-design interpretation | BrainLift SPOV 1 and SPOV 3; `OUTCOME_AND_FALSIFICATION_PLAN.md` | Collapsing both stages could either overclaim route causality or discard the pathway-expansion logic | Evaluation owner | Supported as design logic |
| E-047 | A nonsignificant Track A/Track B difference does not establish equivalence; service fit requires a prespecified noninferiority margin and adequate precision. | Verified statistical-method finding | Equivalence/noninferiority methods; `PROGRAM_MECHANISM_BENCHMARKS.md`; `SIMULATION_SPECIFICATION.md` | A small underpowered cohort could be misread as proof that Track B keeps pace | Evaluation owner | Supported |
| E-048 | Pooling prospectively comparable randomized cohorts increases precision, but valid annual updates require prespecified weights, stable estimands, and sequential-inference control. | Verified methodological synthesis | Multi-trial/blocked analysis and sequential-inference methods; `OUTCOME_AND_FALSIFICATION_PLAN.md` | Repeated ordinary testing or accidental sample-size weighting could produce false certainty | Evaluation owner | Supported |
| E-049 | The public growth multiple can be decomposed into counterfactual and GT-attributable components using randomized offered/non-offered growth, but the adjusted score/SD ITT should remain primary. | Research synthesis from randomized estimands and NWEA ratio definition | NWEA growth-ratio guidance; `OVERNIGHT_RESEARCH_REPORT.md` | The full observed 2.6× figure could be attributed to GT even when non-offered students would also outgrow projections | Evaluation owner | Supported as proposed reporting framework |
| E-050 | Analysts pooling cohorts across changed program, policy, population, site, outcome, or norm versions must preserve version-specific effects and model heterogeneity; this is not a new applicant/MVP collection requirement. | Verified transportability/drift synthesis | Transportability, hierarchical validation, and drift sources; `STANDARD_SETTING_AND_LIFECYCLE.md` | More cohorts could create a precise but uninterpretable average of different treatments | Evaluation owner | Supported as analyst guidance |
| E-051 | The pre-existing GT website can link to a separately hosted application origin for the prototype or later provide a reverse-proxy path mount. | Open integration assumption | Confirm host platform, navigation, CSP, cookie, and proxy capabilities with GT IT | Integration mode or return-navigation design may need to change | Technical owner | Unverified; Mode A synthetic placeholder |
| E-052 | GT can provide brand tokens (colors, fonts, logo, spacing) as CSS custom properties or equivalent static assets. | Open integration assumption | Obtain approved brand token package from GT product/design owner | Prototype can use neutral defaults, but production-shaped visual integration remains incomplete | Product owner | Unverified; low-risk default theme available |
| E-053 | The synthetic MVP requires no SSO with a live GT website and owns its own local synthetic authentication. | Approved prototype assumption | D-011 and synthetic-only architecture tests | A future SSO requirement would materially change Auth, cookie, and integration architecture | Technical and privacy owners | Supported for MVP only |
| E-054 | Retaining PostgreSQL means the RLS firewall, `SECURITY DEFINER` RPC surface, immutable versioning, hash-chained audit, and deterministic canonical replay carry from the Supabase platform to Amazon Aurora Serverless v2 unchanged. | Verified platform fact | Amazon Aurora PostgreSQL compatibility documentation; existing SQL migrations; D-012 | If Aurora diverged from standard PostgreSQL semantics, security/replay tests could regress | Technical owner | Supported (standard PostgreSQL features only) |
| E-055 | Amazon Cognito can issue JWTs carrying an admin-controlled `custom:user_role` claim, verifiable server-side against the user-pool JWKS, sufficient to bind identity/role into PostgreSQL session settings for RLS. | Verified vendor/platform fact | Amazon Cognito user-pool, custom-claims, and JWKS documentation; `RLS_AND_AUTH_BLUEPRINT.md` | The Cognito→GUC principal binding would need redesign; RLS could lack a trustworthy principal | Technical owner | Supported |
| E-056 | The application runtime can operate with no RLS-bypassing credential: the ECS task role connects only as the non-`BYPASSRLS` `authenticated` role (IAM DB auth / scoped Secrets Manager secret), with elevated provisioning access confined to a separate CI/operator IAM principal. | Verified platform fact / by-construction control | AWS IAM, RDS Proxy, IAM DB auth, and Secrets Manager documentation; ARCHITECTURE_PLAN §7.1; D-012 | The "no RLS-bypass credential by construction" guarantee (R9) would not hold | Technical owner | Supported |
| E-057 | A dedicated dev AWS account with synthetic resource tags can enforce a fail-closed born-synthetic guard (replacing the loopback check) so the prototype cannot run against a production account or untagged/non-synthetic resources. | Open platform assumption | Provision the dev account, tagging policy, and startup assertion (`LC-01`, AWS-rescoped); confirm with account owner | The synthetic-only fail-closed guarantee could be weaker on AWS than on the local stack | Technical and privacy owners | Unverified; dev-account provisioning pending |
| E-058 | GT/AWS can host the application container behind a CloudFront origin (subdomain or path behavior) attachable to a pre-existing website, portable to another cloud or self-hosting. | Open platform/integration assumption | Confirm AWS account, DNS/CloudFront, and host-site integration path (relates to E-051, B-06/B-07) | Integration/hosting approach may need to change | Technical owner | Unverified; portable container design in place |
| E-059 | In Bui–Craig–Imberman's final article, marginal gifted eligibility materially changed peers and advanced-course exposure yet produced well-bounded near-zero short-run effects across five standardized subjects; the separate elite-magnet lottery estimated program intensity over ordinary gifted services, and its positive science estimate was weakened by attrition bounds. | Verified causal research finding | Final *AEJ: Economic Policy* article, DOI 10.1257/pol.6.3.30; BrainLift 3.1/3.7; source register | Treating all gifted-program nulls as “better peers, same curriculum” would overstate the positive-program reconciliation and obscure the local/intensity estimands | Evaluation and evidence owners | Supported |
| E-060 | SSP's 2026 process is a live example of contextual review followed by a lottery among qualified applicants and a waitlist comparison arm, but research consent is a condition of lottery eligibility and public preregistration/power/sample details are unavailable. SSP's exceptional funding and scarcity do not establish GT feasibility. | Official-source operational finding plus peer-reviewed ethics transfer | SSP official evaluation/expansion materials; Jenkins 2023; `research/comparators/SSP_SELECTION_CASE_STUDY.md`; BrainLift 3.10 | SSP could be misrepresented either as proof GT can run the design under ordinary constraints or as an ethical template that permits research refusal to reduce admission rights | Evaluation, operations, and student/family owners | Supported with explicit transfer limits |
| E-061 | Effects on local/aligned or assisted tasks can materially exceed independent/standardized or later unaided performance, and educational intervention effects can fade; therefore a positive randomized MAP effect identifies a MAP effect but does not alone establish durable retention, transfer, independent performance, broad educational quality, or an AI-specific mechanism. | Verified research synthesis | Kulik & Fletcher 2016; Bailey et al. 2017; Bastani et al. 2025; `PROGRAM_MECHANISM_BENCHMARKS.md`; BrainLift 3.8/5.7 | GT could turn a valid narrow causal estimate into an invalid broad claim that its package or AI produces deep, durable education | Measurement and evaluation owners | Supported |
| E-062 | Pre-CogAT onboarding should use reusable guardian-owned student/household profiles plus an immutable cycle application containing the D-013 field inventory, while omitting the two essays, enrollment/prior-school history, and setup-stage W-2 proof. | Approved project direction | Team-lead direction dated 2026-07-20; D-013 | Frontend/backend contracts could model the wrong lifetime, omit required setup fields, or collect explicitly excluded data | Product and technical owners | Approved for born-synthetic MVP |
| E-063 | GT can provide and authorize a school-directory source whose IDs, names, types, addresses, update ownership, and `other/not listed` policy are stable enough for application autofill. | Open operational assumption | Confirm source, coverage, update authority, and snapshot policy with GT admissions/IT | Autofill may be incomplete, stale, or falsely authoritative; manual entry and signed snapshots may need redesign | Product and operations owners | Unverified |
| E-064 | GT/financial owners will define whether household income is exact or banded, currency/tax year, requiredness, household membership, authorized viewers, and a lawful post-admission proof trigger/retention policy. | Open financial/privacy assumption | Written financial-aid and privacy policy; B-08 | The system could collect excessive or uninterpretable financial data, expose it to admissions, or request W-2s without authority | Financial, privacy, and product owners | Unverified; synthetic fixtures only |
| E-065 | GT/accessibility owners will provide versioned accommodation/support-plan vocabularies, `other` handling, operational viewers, response obligations, and retention/deletion rules for support and discipline disclosures. | Open access/privacy assumption | GT accessibility, student-support, admissions, and privacy review; B-06 | Labels could be misleading, stigmatizing, overbroad, or visible to unauthorized decision-makers | Access, privacy, and student/family owners | Unverified; configurable synthetic codes only |
| E-066 | A gifted-screen effectiveness claim must be evaluated at the operational tail and decision point—not with global reliability/validity alone—and a head-to-head claim requires either a common calibrated metric or the same students plus a shared external criterion. | Verified psychometric-method synthesis | Lord 1980; Green et al. 1984; van der Linden 1984; Kolen & Brennan 2014; DeLong et al. 1988; BrainLift 9.1–9.13 and 11.5/11.8; overnight A1–A12 | A “2× better” claim could compare incommensurate scales, average away tail error, or mistake separate significance tests for a difference | Measurement and evidence owners | Supported methodologically; GT inputs unverified |
| E-067 | Delivered CAT precision at a gifted cut depends on real tail-bank depth, estimator bias, exposure/content constraints, stopping rules, calibration uncertainty, and drift; nominal information or the label “adaptive” is not sufficient validation. | Verified psychometric-method synthesis | Reckase 2010; Belov & Armstrong 2005; Warm 1989; Bock & Mislevy 1982; overnight A12; BrainLift 10.10 | A proposed adaptive screen could promise precision its actual bank and scoring system cannot deliver | Measurement owner | Supported methodologically; actual bank unavailable |
| E-068 | Child game-based cognitive assessments show genuine but usually low-to-medium convergence with established tests; no located study validates a game for high-stakes K–8 gifted identification, and gaming/device/motor familiarity remains construct-irrelevant-variance risk. | Verified review findings with explicit use gap | Aneni et al. 2023; Song et al. 2020; Bipp et al. 2024; Ohlms et al. 2025; BrainLift 12.4–12.6 | Engagement evidence could be misrepresented as gifted-identification validity or a game could disadvantage low-experience children | Measurement and access owners | Supported with high-stakes-use gap |
| E-069 | Cognitive-test retesting and form reuse can raise scores, while task comprehension, device mode, and administration error can also change observed performance; no CogAT-specific coaching trial was located. | Verified general research plus instrument-specific gap | Scharfen et al. 2018; Bors & Vigneau 2003; National Academies testing overview; BrainLift 6.3, 7.4–7.5, 13.7 | Repeated or mode-varying scores could be treated as stable capability, or absence of CogAT-specific evidence could be mistaken for proof of coach-resistance | Measurement and operations owners | Supported generally; CogAT-specific effect unverified |
| E-070 | Predicting high achievement level is distinct from predicting who benefits from gifted placement; in Card & Giuliano's setting, an achievement-rank RD was positive while a separate IQ-threshold RD was essentially null. | Verified causal research finding with local-generalizability limit | Card & Giuliano 2016, DOI 10.1257/aer.20150484; BrainLift 3.7 and DOK 2; overnight A11 | A valid ability predictor could be misrepresented as a validated benefit-allocation rule | Selection and evaluation owners | Supported locally; GT benefit validity unverified |
| E-071 | Baseline reading literacy is a prerequisite for benefiting from the reading-based, self-paced Timeback program; a child who cannot read the questions would not have had the program work for them, supporting a reading barrier in the in-house screener. | Stakeholder claim (Crystal Martel, Director of Admissions) — her belief, pending transcription of the 2026-07-23 interview | Team-directed report of the Crystal Martel interview, 2026-07-23; interview summary not yet in repo | Building a hard reading gate on an unverified/undocumented literacy prerequisite could exclude capable non-readers and create adverse impact without a validated basis | Product, admissions, and measurement owners | Unverified (stakeholder claim; interview record pending) |
| E-072 | The project must incorporate at least one AWS Lambda function; this is satisfied by deploying the exam scoring + deterministic-replay engine as an event-invoked Lambda (D-019), additive to the D-012 Fargate/Aurora architecture. | Project/tech constraint (external requirement) | Team-lead direction, session 2026-07-26; D-019; `ARCHITECTURE_PLAN.md` §3 | The project would miss a required deliverable, or a Lambda could be forced into an awkward, non-auditable place instead of the scoring/replay unit | Technical owner | Constraint accepted; D-019 Proposed (awaiting ratification) |
| E-073 | Under the born-synthetic 3PL simulator, a 6-item ascending novel block cannot recover an injected learning rate (best r = 0.118); a 30-trial single-domain adaptive block with a MAP learning-curve estimator reaches r = 0.623, per-domain SE ≤ 0.50 is unreachable at 15 four-option items under any selection rule, and a free response-pattern rule removes 88.7% of residual RT-gate engagement bias. | Born-synthetic method/power result — circular by construction; establishes statistical power and estimator correctness only, never that any metric measures real ability (R10) | `docs/PSYCHOMETRIC_SWEEP_2026-07-28.md`; seeds `psychometric-sweep-2026-07-28-*` and `persona-sim-2026-07-27`; extends E-067 | A learning-rate block, per-domain score reporting, or engagement-telemetry spec could be sized against power the design does not have | Measurement owner | Synthetic-only; `validated: false`; real-item and real-child validation outstanding |
| E-076 | `CX-achieve-02` ships a solvable outcome model to the client. `content.apparatus` carries the base value, per-level factor weights, and interaction bonus, so evaluating it over `content.conclusion.options` and taking the argmax recovers the correct key on **120/120 items (100%) with zero trials run**. Unlike E-075 this is fixable, because the model is not needed to render the controls — only to resolve a trial, which a server could do. It is blocked from serving in `research/exam-question-types/qa/NOT_SERVABLE.json` pending a decision. | Verified exploit, 2026-07-25 | `research/exam-question-types/qa/leak_scan.mjs` (RESPONSE-MODEL ATTACK, 120/120); the renderer's local `readingFor()` in `demos/CX-achieve-02.html` | Severe for this type specifically: the exploit runs **no** trials, so the process metrics that would otherwise catch cheating (M-PLANFUL, M-EFF) have nothing to observe, and a taker answering correctly with no experimentation reads as *efficient and insightful*. The type's per-type checker PASSES with the defect present, which is why a separate block list is needed | Assessment-integrity owner | Open — needs a decision between adding a host-mediated trial oracle (keeps the keyed conclusion) or scoring this type on experimentation process only (drops it) |
| E-077 | The `NOT_SERVABLE.json` block list was declarative only: nothing enforced it. `scripts/sync-exam-demos.mjs` never read the file, and the only reason `CX-achieve-02` was not being served was the *unrelated* fact that it had no server verifier. The moment a verifier was written for it (2026-07-25), it was silently added to the served registry with its E-076 leak intact — verified by inspecting `registry.generated.ts` after the verifier-registry gate landed. | Verified near-miss, 2026-07-25 | Direct observation: `rg "typeCode: 'CX-achieve-02'" apps/web/src/lib/exam/registry.generated.ts` matched after the gate change and no longer matches after the fix | A leaking type entering the battery would corrupt every score computed from it, and would do so invisibly: the type's own checker passes, and the exploit runs zero trials so the process metrics read it as insight. The general lesson is that a verifier makes a type *gradeable*, not *safe*, and the two gates must be independent | Assessment-integrity owner | Resolved — the block list is now a first-class gate in the sync, ahead of the verifier check, so a listed type cannot be served regardless of verifier coverage |
| E-075 | Several question types are inherently computable from `content` by a scripted client, even with no answer-key leak. `GB-TRACK-01` is the clearest case: `content.initialTargets` names the lit jars and `content.motion.phases` lists every swap, so applying the permutations yields the answer exactly. `FLU-CONCEPT-01` is similar (the gate's verdicts are stimulus, so the rule can be induced programmatically), as is any matrix-style item whose cells determine the answer. | Verified by inspection of served payloads, 2026-07-25 | `research/exam-question-types/banks/GB-TRACK-01.jsonl` content fields; the same property holds for every visual-tracking and rule-induction type by construction | This is NOT fixable by moving fields, because the renderer must receive the motion in order to animate it — the stimulus *is* the derivation. It is the boundary of what content-side hygiene can buy: a child cannot exploit it, but an automated client can score perfectly. Mitigation belongs in proctoring and in the process telemetry already collected (a scripted taker shows implausible response times and no revision behaviour), not in the item schema | Assessment-integrity owner | Open — record as a known limit of unproctored administration; do not claim content hygiene prevents automated cheating |
| E-074 | The generated question banks do not satisfy the canonical contract in `packages/contracts`. Probing the first item of all 63 banks: **0/63 pass `bankItemSchema`** and only **14/63 pass `servedItemSchema`**, so 49 built types cannot currently be served or validated through the contract. Causes, by field: `answer.distractorRationales` (keyed object vs the contract's array — 115 issues, including the `FLU-MATRIX-01` reference exemplar), `provenance` shape (all 63), an extra `demoPath` key rejected by `.strict()` (49), `answer` (38), `scoring` (21), and `typeCode` (9 — the regex `/^[A-Z]+-[A-Z0-9]+-\d+$/` rejects the 11 catalog IDs with lowercase middle segments such as `WM-bind-01` and `CX-achieve-02`). Separately, 19 types use `lureClass` labels outside the 9-value `lureClassSchema` enum. | Verified measurement, 2026-07-25 | Direct `safeParse` of real bank items against `bankItemSchema`/`servedItemSchema` from `packages/contracts/src` | Blocks the primary objective of wiring all 66 types: the adaptive runner cannot validate or serve most banks. Also blocks server-side M-ERRTYPE/M-LURETYPE, which need bucketable lure classes. Root cause is process, not any one agent: ~20 parallel generators each validated against their own per-type checker and `EXAM_ITEM_SCHEMA_SPEC.md`, and no test ever validated a bank against the contract | Contracts owner | **Resolved 2026-07-25 (D-020, D-021).** Re-measured after the reconciliation: **63/63 `bankItemSchema`, 63/63 `servedItemSchema`**, over every item in every bank rather than the first item only. The contract was widened where the banks' convention was better (keyed rationales, mixed-case `typeCode`, open `answer`/`scoring`/`provenance`, optional `demoPath`) and the banks were migrated where they were wrong (array-shaped rationales, missing `demoPath`, unbucketable lure labels). Lure labels are now two fields: a required coarse `lureClass` from the nine-value enum so M-ERRTYPE/M-LURETYPE stay computable, plus an optional `lureDetail` carrying the type's own label — all 219 observed labels are mapped in `generators/item-shape.mjs`. The regeneration was verified to be a pure shape migration: item counts, difficulties, content, and correct answers are unchanged. The recurrence guard is `packages/contracts/src/bank-conformance.test.ts`, which parses every bank on every `pnpm -r test` and was itself verified to fail on a pre-migration bank |
| E-094 | Correct-key positions are not balanced across most generated banks, so a fixed-guess strategy beats uniform chance by a type-dependent margin. Measured over 55 banks: `GB-FLAWFINDER-01` +33pt (25% uniform vs 58% for the modal key), `SPA-FOLDNET-01` +19pt, `SPA-VIEW-01` +16pt, `SPA-XSCAN-01`/`SPA-ROLL-01`/`FLU-CONCEPT-01`/`SPA-PICKFOLD-01` +13pt, `FLU-MATRIX-01` (the reference exemplar) +10pt; most others +1 to +9pt. `QUANT-WORD-01` and `QUANT-MOBILE-01` deliberately balanced positions and sit at +1/+2pt, so the practice is achievable. | Verified measurement of the born-synthetic banks, 2026-07-25 | Direct analysis of `research/exam-question-types/banks/*.jsonl`: modal-key frequency vs uniform over each bank's observed key space, restricted to types with a genuine discrete key space | Accuracy (M-ACC) sets the score bracket, so an unequal pseudo-guessing floor inflates low-ability accuracy, compresses the bottom of the scale, and makes raw accuracy non-comparable between types — a child served many high-floor types would out-score an equally able child served low-floor types | Measurement owner | **Partly resolved 2026-07-25, and the original measurement was partly an artifact of its own baseline.** 15 banks were rebalanced; a naive global allocation was tried first and rejected because option count is itself a difficulty lever, so flattening the pooled counts crammed the tail keys onto the few long items and made "count the options, pick the last" worth up to +50pt — a larger exploit than the one being fixed. Allocation is now stratified by option count, and the worst within-stratum advantage across those banks fell from +43.3pt to +5.1pt. Separately, the `100/observed key space` baseline used above is only the pseudo-guessing floor when every item shows the same number of options; the honest floor is the mean of `1/n_options` over items, and the stated figure also *rewards suppressing rare keys*. Re-measured against the true floor, the types reported here as capped at +11 to +14pt are in fact at or below chance (`SPA-VIEW-01` −9.6pt, `SPA-FOLDNET-01` −0.9pt, `SPA-XSCAN-01` −0.8pt), and the largest apparent outlier, `WM-gridflash-01` at +30.2pt, is an artifact of pooling two response formats into one key field — its change-detection shell is an exact 39/39 binary. Genuinely still above the true floor: `VER-SORTBOT-01` +7.3, `QUANT-BALANCE-01` +7.5, `VER-WORDTRAIN-01`/`VER-BUILDIT-01` +6.3, `CX-curious-02` +5.8. `FLU-CONCEPT-01` (+5.8) is at its arithmetic optimum of +5.6 given the rule semantics. Any future measurement must report the per-option-count breakdown alongside the pooled figure, and must not pool multiple response formats. |
| E-078 | The scorer's accuracy bracket is miscalibrated for a converged adaptive battery and discards the ability signal the trace carries. Simulating a child with equal true ability in all four areas, seeded at band '4-5', against the real registry and banks: as true ability rises 3 → 19, raw accuracy rises only 24% → 67% (a matched item is one the child has about an even chance on, so the battery holds accuracy near-constant by design) and the composite spans just **2.26 → 11.04** on a 1–20 scale, never approaching the top of the scale for a maximally able child and separating true ability 11 from 15 by **0.26 points** (6.33 vs 6.59). The engine's own per-area estimates over the same traces track true ability to within ~1.0 point, so the signal is present and the scorer loses it in stage 1. Re-bracketing the same traces on a difficulty-adjusted ability fitted from the trace (Rasch-style location; two-stage structure otherwise untouched) cuts mean absolute composite error over sixteen profiles from **5.73 to 0.69** and worst-case from **11.47 to 1.76**, and reaches 19.04 for true ability 19. | Verified measurement, 2026-07-25 | `scripts/exam-bracketing-comparison.ts` (`pnpm exam:bracketing`), driving `packages/exam-engine`'s real-bank simulation harness into `packages/exam-scoring` under both bracketing modes; eighteen profiles including unequal-across-areas, off-scale-high, off-scale-low, and above-level seeds with low and high ability | Any composite reported from a converged battery understates able children severely and compresses the whole population into the bottom half of the scale, so a gifted child and a band-matched child are not distinguishable by the number the product exists to produce. It also makes the composite non-comparable to the per-area estimates the same session produces | Measurement owner | Open — the alternative is implemented and available behind `ExamPolicy.bracketing.mode` but is **not** the default; the default remains accuracy bracketing and is verified byte-identical (D-024). Residual shortfalls of the alternative are recorded in D-024's consequences: the four-point bracket quantizes (true 17 fits at 16.7–17.3 but scores 18.76), and neighbouring levels sharing a bracket still separate by only 0.14–0.29 points |
| E-079 | Fifteen of the 63 served demos raced the host and could show a child developer text mid-assessment. Each demo detects embedding via `window.parent !== window` and, when embedded, must render only the item the runner sends in `{type:'init', item}`. But 15 demos backed that up with a timer — "if `gotInit` is still false at 500ms (1000ms for `GB-WORDFORGE-01`/`GB-WORDLADDER-01`), assume there is no host" — with no `!embedded` check. A host `init` arriving after the timeout therefore (1) appended "(Standalone preview: cycling bank items.)" to the instruction box the child is reading, (2) fetched `/banks/<CODE>.jsonl`, which 404s because the raw banks must never be published, and (3) started cycling the demo's own items alongside the host's, so a child could answer an item the runner never served and will not score. Affected: `GB-EXPLORE-01`, `GB-FILTER-01`, `GB-PATHFORGE-01`, `GB-ROBOPATH-01`, `GB-SHAPEFIT-01`, `GB-TRACK-01`, `GB-WORDFORGE-01`, `GB-WORDLADDER-01`, `SPA-FOLDNET-01`, `SPA-MAZE-01`, `SPA-PICKFOLD-01`, `SPA-PIPES-01`, `SPA-ROLL-01`, `SPA-SHADOW-01`, `SPA-TANGRAM-01`. | Verified defect, 2026-07-25 | First observed in a real browser on the exam runner (`/dev/family-preview/exam`, band 4-5): the page showed the preview text and issued a 404 `GET /banks/GB-EXPLORE-01.jsonl` even though host init did arrive and the correct bank item rendered. Reproduced deterministically with Playwright by embedding `GB-EXPLORE-01` in an iframe and never sending `init`: the pre-fix source shows the preview text and requests the bank; the fixed source does neither | The failure is silent and load-dependent, so it would have surfaced first on the slowest devices — exactly the families least able to report it. A child sees internal tooling language during a scored assessment, and any item answered from the demo's own cycle is unscored, so the session's item count and the ability estimate derived from it are both wrong | Assessment-integrity owner | Resolved 2026-07-25 — every fallback timer now also requires `!embedded`, so an embedded demo waits indefinitely for host init; the two 1000ms timers (which only ran when embedded) were removed outright. Standalone behaviour is unchanged and verified. The recurrence guard is `apps/web/src/lib/exam/standalone-guard.test.ts`, which fails any demo — source or published — whose timer checks `gotInit` without `!embedded`, and was itself verified to fail on the pre-fix demos |
| E-080 | The screener's item selection ranked an age-band match ahead of closeness to the ability estimate, which biased the estimate away from the truth in the two regions where the '4-5' band's item supply runs out. Nearly every wired type's `'4-5'`-tagged items stop between difficulty 11.8 and 13.4 and start around 7.6–8.1, so once an area's estimate left that range the engine kept serving items 2–3 points off target while on-target items were available and unseen. Because the difficulty update takes its direction from correctness alone, a correct answer on an item well *below* the estimate still raised it by a full scheduled step, producing a ratchet. Sweeping every integer ability 2–19 (equal in all four areas, band '4-5', real registry and banks) breached the 1.5-point tolerance at **five** abilities: 12 (2.58), 13 (2.67) and 14 (1.87) overshot, and 5 (1.53) and 7 (1.91) undershot by the mirror mechanism. The seven named `CONVERGENCE_PROFILES` all passed throughout, so the existing suite could not see it. Bank supply is **not** the cause: fluid-reasoning difficulty is near-uniform at ~90 items per integer, 95 of them at difficulty 13 exactly, and the closest available item was on target in every mis-served case. Pricing the age-band preference at `ageBandBias` = 0.5 scale points removes all five breaches, cuts the worst error across the sweep from 2.67 to 0.81, and shortens the gifted battery from 40 to 36 items. | Verified measurement, 2026-07-25 | The 2–19 ability sweep now pinned in `packages/exam-engine/src/real-bank.test.ts`; per-item traces and bank-supply histograms taken from the same harness; knob sweep over `ageBandBias` ∈ {0, 0.25, 0.5, 0.75, 1, 1.5, 2, 3, ∞} | A systematic +2.7 overshoot sits exactly in the 12–14 region where a screener decides whether a child looks gifted, so the defect could have misclassified band-typical children upward and low-ability children downward. The general lesson is that a shortlist of named profiles is not a sweep: five of eighteen levels were wrong while every named profile passed | Measurement and exam-engine owners | Closed by D-025 for the current banks; the mechanism is bank-shaped, so a bank whose age-band tagging changes must re-run the sweep |
| E-081 | The database and the app tier graded the same response by different rules, and the columns the audit path reads held the verdict nobody scored from. On a real 22-item battery the two disagreed on **15 of 22 items**, so recomputing the composite from `app.exam_scorer_input_json` — the canonical scorer input D-019's audit path hashes — gave **3.775** against a recorded composite of **7.434**. Cause: `app.exam_score_response`'s entire vocabulary was "compare the submitted `key`/`selectedKey`/`value` to `answer_key.correctKey`, or `value` to `answer_key.solution` for `computed_solver`", while 31 of the served types are graded by a per-type verifier that re-derives the expected answer from `content` or a non-key field of `answer`, a further set declare `computed_solver` but store `correctKey`, and several key by numeric option index that comparison never read. **Measured state after D-027's foundation:** driving 63 served types × 12 real bank items × 3 responses (the response a correct taker would give, a near-miss wrong one, and a skipped one) = 2,268 cases through both implementations, agreement rises from **1,659/2,268 (73.1%)** under the demoted `app.exam_score_response` to **1,989/2,268 (87.7%)** under `app.exam_verify_response`. All 33 generic-verifier types and all four ported per-type verifiers now agree on **every** case including the metric maps, and 444 of those agreements are cases both sides scored *correct*, so the agreement is not the vacuous all-false kind — under the old implementation the same four types agreed on 0 correct cases. Every one of the 279 remaining disagreements is one of the 26 unported per-type verifiers, each of which the harness reports as PENDING rather than passing. | Verified measurement, 2026-07-25 | The 15-of-22 disagreement and the 3.775-vs-7.434 gap from the D-026 round trip, recorded in `docs/architecture/EXAM_PERSISTENCE_NOTES.md` §4. The 2,268-case differential from `pnpm exam:verify:diff`, which runs the real registry, the real banks and both verifier implementations in one process; `--baseline` re-runs the identical cases against the demoted function to produce the before figure. Per-type inventory and difficulty ratings in `docs/architecture/EXAM_VERIFIER_PORT_INVENTORY.md` | A stored trace whose `correct`/`score`/`M-ACC` columns disagree with the verdict the reported score came from breaks the reproducibility claim R7 rests on: the hash still matches the trace, but what it hashes is not the scorer's input. It also means no trace recorded before this migration is comparable to one recorded after it. The residual risk is that agreement is currently partial and the harness is the only thing that makes the gap visible, so the coverage figure must be re-read after every batch of ports rather than assumed | Measurement and assessment-integrity owners | Partially closed by D-027 (4 of 31 per-type verifiers ported, 3 of 3 generic). Open until the coverage line reads 31/31 — except GB-WORDLADDER-01, which needs an owner decision first because its 4,000-word child lexicon is read off disk and is not a field of `app.exam_item` (inventory §6), and CX-achieve-02, which must not be ported while its E-076 leak keeps it blocked |
| E-093 | Statistical originality (M-ORIG) and flexibility (M-FLEX) cannot be computed for the open-ended types without a per-prompt response-frequency norm bank and a semantic clusterer. No such norm bank exists for this project, so any originality figure currently produced is a structural behaviour count, not norm-referenced rarity. | Open assumption / negative finding, discovered during the 2026-07-25 bank build | `CX-diverge-01` previously shipped a hand-written response-frequency table and keyword-to-category map in its demo and reported M-ORIG/M-FLEX from them; those values looked like norms but were invented, and were removed. `CX-curious-02` still labels a count of causal/hypothetical picks as "Originality (deep-question rate)" and asserts rarity without a norm basis; `GB-WORDFORGE-01` also references these metric IDs. | A behaviour count presented as statistical originality would make a creativity claim the data cannot support, and could place a child on a "gifted" tail on fabricated rarity | Measurement owner | Open — M-ORIG/M-FLEX are judge-deferred and must stay inert until a real norm bank exists |
| E-082 | All 66 renderer demos (63 of them served) displayed a researcher-facing sidebar to the child taking the scored assessment. Every demo carried `<aside id="telemetry">` headed `Live telemetry · per-child` — 32 of them adding "(display only)" — listing metric IDs with their live values and a plain-English description of the behaviour each one measures: `M-PLANFUL` "Planful: pause before first move", `M-ENGAGE` "Focus losses (tracked, not enforced)", `M-REV` "Revisited cells", and between 5 and 16 metric IDs per demo (median 9). Nothing suppressed it when embedded, and both child-facing routes render the same `PreviewExam` → `ExamRunner` component, which frames the demo at `/exam-demos/<CODE>.html` with no query string, so there was no route on which the child did not see it. | Verified defect, 2026-07-25 | First observed in a real browser on `/dev/family-preview/exam`. Confirmed exhaustively with Playwright against the running dev server: each of the 63 published demos loaded in an iframe, `#telemetry` computed-visible in **63/63** before the fix and **0/63** after, with the live runner's own frame reporting 12 metric IDs present in the DOM and the panel not rendered. Source-side confirmed by `rg 'Live telemetry' research/exam-question-types/demos/*.html` matching 66/66 | Two distinct harms, the second the more serious. (1) A scored child assessment looks like internal tooling. (2) Construct validity: the panel names the behaviour being measured *while it is being measured*, so a child who reads "pause before first move" has a reason to pause that is not planning, and the metric no longer measures the construct it is named for. The reactivity falls hardest on the process metrics meant to separate deliberate problem-solving from guessing, which is precisely the signal the profile depends on. Because the panel is legible to any reading child, the effect would also be uneven across readers — a differential, not a constant, bias | Assessment-integrity and measurement owners | Resolved 2026-07-25 (D-028) — the panel is hidden whenever the demo is framed unless its URL carries `?telemetry=1`; standalone is unchanged and was re-verified visible on 63/63. The fix is presentational: a battery run with the panel hidden still emitted `{type:'telemetry'}` events to the host (observed live on the runner), and no sampling or metric-computation code was touched. The recurrence guard is `apps/web/src/lib/exam/telemetry-panel-gate.test.ts`, which fails any demo — source or published — that ships the panel without the gate. **Not resolved:** whether any *previously recorded* synthetic trace is affected. All existing traces are born-synthetic and `validated=false`, so nothing downstream depends on them, but no trace collected before this date should be treated as free of the reactivity described here |
| E-083 | The exam runner raced each demo's `message` listener, so items the host had already served were unanswerable. `apps/web/src/components/exam/exam-runner.tsx` sent `init`+`start` from the iframe's `load` handler behind a one-shot `initiated` guard, and also on the demo's `ready`. `load` fires when the document is parsed but before the body script runs, so for any demo that installs its listener late the `init` was delivered to a window with no listener and dropped — and because `initiated` was already true, the demo's subsequent `ready` (its actual "I am listening" signal) was ignored. The item then sat on "Waiting for an item from the host" until the 4-minute `MAX_MS_PER_ITEM` timeout recorded it as timed out. Because it is a timing race the affected set varies per run; observed on `GB-EXPLORE-01`, `GB-WORDFORGE-01` and `GB-PATHFORGE-01`. Measured **6 of 14 items unanswerable** in one pre-fix battery. | Verified defect, 2026-07-25 | Found because the E-079 standalone-guard fix removed the fallback that had been masking it: with the fallback gone the demos stopped rendering at all. Isolated in a real browser — instrumenting the iframe showed it received `init` and `start`, yet still displayed the waiting placeholder, and re-posting `init` to the stuck frame made it render immediately, proving the listener worked and the first `init` had been dropped. Driving all 63 demos directly with a real bank item rendered 63/63, which is why the demos were exonerated and the runner implicated. Regression guard: `apps/web/e2e/exam-init-handshake.spec.ts`, verified to pass against the fixed build and fail against the unfixed one, naming the stuck types | A child is shown a blank activity for four minutes and then has it scored as a timeout, on an item they were never able to attempt. The engine consumes that as a wrong answer, so it lowers the ability estimate and corrupts both the difficulty trajectory and the final score. Being load-dependent, it would hit the slowest devices hardest and would present as intermittent | Assessment-integrity owner | Resolved 2026-07-25 — `ready` is now the handshake: the runner sends `init` on `ready`, and the `load` path is a 500ms fallback that fires only when no `ready` arrives (for the legacy self-rendering demos that never send one), so no demo receives `init` twice and no item timer is reset |
| E-084 | The reported score is computed from a trace the client sends, not from the server-verified trace the database stores, so the reproducibility claim printed on every outcome row is false and the score is client-forgeable. `apps/web/src/app/api/exam-results/route.ts` scores `scoreExam(trace.data.scoredItems)` where `trace` is the parsed **request body**; the comment above it ("Recompute the score server-side (authoritative, deterministic, reproducible)") is true only in that the arithmetic runs on the server. Nothing compares that payload to `app.exam_item_response`, whose rows were written by the server's own verifier. Measured on a real persisted 22-item battery (session `3e03558f`): the outcome records **spatial accuracy 0.651 over 6 items** while all 6 persisted spatial responses are `correct=false`, and the trace holds **3 of 22** correct in total. Re-scoring `app.exam_scorer_input_json` through the same `scoreExam` and `DEFAULT_EXAM_POLICY` yields composite **3.737** against the recorded **7.468**. The stored `scorer_input_hash` matches on re-hash, which is the trap: it is computed by `app.exam_scorer_input_hash` from the database, so it certifies that the *trace* has not drifted and says nothing about whether the *score* came from it. Distinct from E-081, which was two graders disagreeing; here the two tiers now agree on the verdicts (22/22 on this session) and the score simply does not read them. `POST /api/exam-submit` is not affected — it takes `difficulty` from the server bank item, computes the verdict with the server verifier, and spreads server metrics after `clientMetrics` so a client value cannot win. | Verified defect, 2026-07-25 | Found by writing the check the claim implies rather than trusting it: `pnpm exam:reconcile` (`apps/web/scripts/exam-reconcile-session.ts`) re-verifies every stored response through `app.exam_verify_response`, re-hashes the trace, and re-scores it through `packages/exam-scoring`, then compares all three to the outcome row. It reports the verdicts reconciling and the composite not. The per-area contradiction was confirmed directly in SQL against `app.exam_item_response`, which is what rules out a scorer bug: no arithmetic over 0/6 correct produces 0.651 | R7 rests on a score that can be recomputed from the stored evidence, and this one cannot be — the number a family would be shown is not derivable from anything the system verified. It is also the cheating vector D-027 was chosen to close, one tier higher up: a scripted client that posts `scoredItems` claiming `correct: true` at high `difficulty` for every item receives the score it asked for, while the trace stored beside it disagrees, and the anti-cheat value of verifying answers in the database is voided by a scorer that never reads the verdicts. Every composite recorded before this is fixed must be treated as unreproducible, including the ones in `EXAM_PERSISTENCE_NOTES.md` §4 and the 7.434 in E-081 | Assessment-integrity and measurement owners | **Resolved 2026-07-25 (implementing D-019 and D-027; no new decision).** When the payload carries an `examSessionId`, `/api/exam-results` now reads `app.exam_scorer_input_json` back through `api.exam_get_scoring_inputs` and scores THAT; the `scoredItems` array in the request body is not scored, and there is no fallback to it — if the database input cannot be read the request fails with `SCORER_INPUT_UNAVAILABLE` and nothing is stored, because a persisted score is a claim that cannot be withdrawn. `api.exam_get_scoring_inputs` already existed from D-019 with the required ownership check, so no migration was needed; `supabase/tests/124` pins that a second owner reading the first owner's scorer input gets `RESOURCE_NOT_FOUND`, that the `app.*` derivations stay revoked from every client role, and that a wrong answer stays wrong in the input the scorer reads. **Measured:** re-running the same seeded 22-item battery, the pre-fix route would have reported **7.468** (the client trace — byte-identical to the composite recorded on session `3e03558f`) and the fixed route reports and stores **6.421** (the database trace); `pnpm exam:reconcile` exits 0 on the new session (`60b78164`, verdicts 22/22, hash unchanged, composite and every per-area proficiency/accuracy/bracket and the profile identical) and still exits non-zero on `3e03558f`, which is left as recorded. The forgery is pinned in `persistence.integration.test.ts`: a payload claiming all 8 items correct at difficulty 20 for a session whose 8 persisted responses are all wrong would have scored **20.0** and instead returns and stores **2.2**. **Two findings the fix surfaced, neither of them E-084 and neither fixed here.** (1) `pnpm exam:reconcile` could not have passed for ANY session: `sameProfile` compared `JSON.stringify` output, and Postgres re-orders jsonb object keys by length, so the profile check always failed; the comparison is now key-order-insensitive and still strict on content and on array order. (2) On the same battery the app tier and the database still disagree on **1 of 22** items (`GB-PATHFORGE-01` #18, app `correct`, database not), which is the residual E-081/D-027 port gap and is the whole of the 7.468→6.421 gap; the reconcile gate cannot see it because it compares the database against itself. Still not quantified: whether the corrected input changes the E-078 score-separation picture |
| E-090 | Nine more per-type verifiers were ported to plpgsql under D-027 and grade every response in their banks exactly as the app tier does. The batch is the five types `EXAM_VERIFIER_PORT_INVENTORY.md` rates **easy** (`CX-curious-02`, `GB-DEBATE-01`, `SPA-VIEW-01`, `SPA-HIDDENCUBE-01`, `GB-FILTER-01`) and four of the five **moderate** working-memory types (`WM-bubble-01`, `WM-corsi-01`, `WM-bind-01`, `WM-gridflash-01`); `WM-gate-01` is not in it. Driving **every** item of all nine banks (1,620 items × 3 responses = 4,860 cases) through both implementations, the two agree on **4,860/4,860**, including the metric maps compared key-for-key and to 1e-9, and **1,624** of those agreements are cases both sides scored *correct* — so the agreement is not the vacuous all-false kind. A further 1,029 adversarial responses (empty, mistyped and partially-formed bodies over every response shell in each bank) and 70 hand-built fixtures covering paths the banks never take (a zero-target n-back stream, onset ties in a flash schedule, a creature rebound to a second house, a dial reading across the ±180° seam, a stack taller than its declared `maxHeight`, a cue that selects nothing) also agree case for case. **Three re-derivations were confirmed to beat a corrupted stored key**, which is what makes the port an independent check rather than a second reading of the same field: the information gap from `answer.evidenceModel`, the corsi trail from `content.presentation.schedule`, and SAME/CHANGED from the flashed hue. | Verified measurement, 2026-07-25 | `pnpm exam:verify:diff --items=1000` (the real registry, the real banks and both implementations in one process); pgTAP `supabase/tests/130_exam_verify_vbatch1.test.sql`, 36 assertions; migration `supabase/migrations/20260725174500_exam_verify_vbatch1.sql`. The adversarial and synthetic probes were throwaway harnesses run against the same dispatcher and are not retained — the bank-wide differential is the standing check and is re-run per batch | If a port disagreed with the app tier the database would be the authority on a verdict nobody had reviewed, which is the defect E-081 measured, reintroduced one type at a time. The residual risk is that agreement is measured against the app tier, so a rule the app tier gets wrong is now wrong in two places: `WM-bubble-01` is a live instance, see Status | Measurement and assessment-integrity owners | Advances E-081 (per-type coverage 4/31 → 13/31 from this batch alone). **Ported as written, believed wrong:** `verifyBubble` requires `targets > 0` for full credit, so an n-back block whose stream happens to contain no repeat scores the child wrong however they answer (`EXAM_VERIFIER_PORT_INVENTORY.md` §8 item 2). The plpgsql port reproduces it and assertion 34 of test 130 pins it, because changing it is a scoring-semantics decision D-027 does not grant. No bank item currently in `WM-bubble-01` has zero targets, so nothing is mis-scored today. The id E-090 was chosen rather than the next free E-084 to stay clear of two sibling batches adding entries concurrently |
| E-091 | Nine more per-type verifiers — the `moderate` batch of D-027's port — now grade identically in the database and in the app tier, on real bank items, including their metric maps. Measured with `pnpm exam:verify:diff` over 12 real bank items per type × 3 responses (the response a correct taker would give, a near-miss wrong one, and a skipped one) = 36 cases each, 324 in total: agreement rises from **216/324** before the batch to **324/324** after, and **108** of those agreements are cases *both* sides scored correct (12 per type) against **0** before, so none of the agreement is the vacuous both-said-false kind. Metric mismatches: **0**, comparing the whole metric map with `correct` exact and floats to 1e-9. The nine are CX-check-01, FLU-MATRIXBUILD-01, VER-SENSE-01, GB-WORDFORGE-01, GB-TRACK-01, SPA-SCENE-01, SPA-MAZE-01, GB-ROBOPATH-01 and GB-EXPLORE-01. Three parity hazards were found and closed in the port rather than papered over: JavaScript's `NaN !== NaN` has no Postgres analogue (Postgres considers `NaN` equal to itself), which would have let FLU-MATRIXBUILD-01's arithmetic rules fire on cells they could not read; `Array.prototype.sort` is specified STABLE and `order by` cannot express SPA-SCENE-01's cross-product comparator, so the port is an explicit stable insertion sort; and JavaScript's `%` on doubles is `fmod`, which Postgres has no float8 operator for, so GB-EXPLORE-01's circular-error reduction is written out as `x - trunc(x / y) * y` (exact for every argument that reaches it). **Two known TypeScript defects were reproduced verbatim, not fixed** — `verifyWordforge` scoring every child wrong when `answer.referenceTarget` is absent, and `verifyExplore` taking its fallback move count from the client-reported `response.cost.actual` (inventory §8.3 and §8.4). | Verified measurement, 2026-07-25 | `pnpm exam:verify:diff`, which runs the real registry, the real banks and both verifier implementations in one process; the before figure was taken by removing this batch's nine `app.exam_verifier_registry` rows and re-running the identical cases, which drops the types back to the keyed default and reports them as PENDING. Per-type fixtures for the parts a real bank item cannot discriminate are in `supabase/tests/124_exam_verify_moderate_batch.test.sql` (40 assertions), notably GB-TRACK-01's simultaneous-versus-sequential swap semantics: no bank item has two swaps sharing a slot within one phase, so the harness alone cannot tell the two readings apart. Migration `supabase/migrations/20260725181742_exam_verify_moderate_batch.sql`. **Measurement caveat:** the local Supabase stack is shared with concurrent batches, and one sibling batch's migration was applied when this was measured, so the whole-run figure of the same run (2,064/2,268 before, 2,172/2,268 after) covers nine types this branch does not implement. The per-type rows above are unaffected by that, because the dispatcher resolves a verifier per `type_code`. The id E-091 was chosen ahead of the next free number (E-084) to avoid colliding with a concurrent sibling's entry. | Same as E-081: a database verdict that differs from the app tier's makes the stored `correct`/`score`/`M-ACC` columns disagree with the verdict the reported score came from, which breaks the reproducibility R7 rests on. The residual risk specific to this batch is the two preserved defects: they are now implemented twice, so fixing either means changing both, and `supabase/tests/124` pins them so the change cannot be made silently | Measurement and assessment-integrity owners | Open, as E-081 is. This batch takes the port from 13 to 22 of the 30 served per-type verifiers; it closes nothing on its own, and the coverage line must be re-read after every batch rather than assumed |
| E-092 | One of the 31 per-type verifiers could not be ported to the database at all, because the input it grades on is not a bank field. `GB-WORDLADDER-01` decides whether a ladder rung is a real word by reading `research/exam-question-types/generators/lexicon-child-en.mjs` off disk and regex-parsing **4,091** curated word→band entries out of JavaScript source. That file is not `content`, not `answer`, not `scoring`, not `provenance`, so it is not in `app.exam_item`, and it is **not derivable** from the item either: `answer` records one optimal ladder plus a count of equally short ones, never the words some *other* legal ladder may step through, and "is this a word" is precisely the judgement being scored. Resolved by moving the lexicon into the database as `app.exam_lexicon(word, band)`, seeded by migration — inventory §6 option (a), taken under D-027's existing authority rather than as a new decision, because the alternatives were to leave one type on the app tier (reopening exactly the split D-027 closes) or to enumerate the legal words into `answer` (a bank regeneration, out of scope). **Locked down** exactly as `app.exam_item.answer_key` is: in the `app` schema no client role holds USAGE on, every privilege revoked from public/anon/authenticated/service_role, SELECT granted only to `api_executor`, row-level security enabled *and forced* behind a single `api_executor` read policy, and no word or band in any `api.*` return value. **Drift from the source file is prevented twice over**: the migration recomputes the source module's own `lexiconHash()` over the rows it inserted and refuses to apply unless it is `428d7d7227fbf5cd` — the same hash every `GB-WORDLADDER-01` bank item independently records in `answer.equivalence.lexiconHash` — and a unit test re-parses the source with the app tier's own reader and compares it entry for entry against the migration's seed. (Recorded as E-092 rather than the next free id: two sibling port batches were adding register entries against the same file at the same time, and the gap avoids a concurrent collision.) | Verified port constraint and its resolution, 2026-07-25 | The blocked input is documented in `docs/architecture/EXAM_VERIFIER_PORT_INVENTORY.md` §6 and visible in `childLexicon()` in `apps/web/src/lib/exam/verifiers/quantitative.ts`. The table, seed and apply-time hash guard are in `supabase/migrations/20260725183000_exam_verify_awkward_batch.sql` §8. The firewall is asserted in `supabase/tests/124_exam_verify_awkward_batch.test.sql` (grant posture, forced RLS, single policy, runtime 42501 for anon and authenticated, non-empty table, and the recomputed hash); the source-to-seed comparison is `apps/web/src/lib/exam/verifiers/lexicon-parity.test.ts`. Equivalence with the app tier is `pnpm exam:verify:diff`: 360/360 cases over all 120 bank items, 120 of them scored correct on both sides | The lexicon is the answer key for this type in everything but name — a client that could read it could decide rung legality locally and, with `content.start`/`content.goal`, enumerate a shortest ladder without solving anything. Holding it in `app` behind a forced RLS policy makes it exactly as unreachable as the stored keys. The residual risk is the one every seeded copy carries: the lexicon now exists twice, and if the two ever diverge the app tier and the database will disagree about whether a rung is a word. That is what the hash guard and the parity test exist to make loud rather than silent; neither prevents someone from deleting both | Assessment-integrity and measurement owners | Resolved 2026-07-25 — the table is seeded, the port agrees with the app tier on every bank item, and both drift locks are in the test suites |
| E-095 | A within-session learning rate is recoverable from a novel block, but only at a length the previous per-area configuration was nowhere near, and the precision reached even then does not support placing an individual child on a scale. Measured on the [1,20] difficulty scale at `slope: 1.0`, over 400 simulated children per cell, with adaptively-targeted difficulty, a 0.5-point item grid and a deliberately noisy handover from the standing phase: recovery of an injected climb runs **r = 0.066 at 8 trials, 0.183 at 15, 0.448 at 30, 0.746 at 45 and 0.862 at 60**, with the estimate attenuated to **0.22 of its true size at 8 trials** and unattenuated (0.98) by 30. Mean posterior SE falls **0.138 → 0.101 → 0.047 → 0.026 → 0.017** across the same lengths. Two consequences. (1) The configuration this replaces — `M-LEARNRATE` enforced per area at `minSamples: 8`, fitted as a block-half ceiling contrast — spent items in all four areas to produce a number that correlates with truth at 0.07 and is attenuated fivefold; that is not a weak signal but no signal, and the stop rule could not end a session without it. (2) `r` is not a property of the estimator alone: it depends on how much children actually differ, which is unknown. At a wider assumed spread (lambda SD 0.10) the same 30-trial block reaches r = 0.885. So 30 is justified by what is invariant across those assumptions — it is where the SE stops falling steeply and attenuation disappears — and is a floor, not a comfortable length. **Separately verified: the confound is real in this engine.** A synthetic child whose ability is pinned at 15 and seeded at the 'K-1' band walks 3, 5, 7, 9, 11, 13, 15 all correct before the estimate brackets; the block-half growth index reports growth (>0.5) for that child, and reports MORE growth the further the seed started from them, exactly as `deriveLearningRate`'s own claim boundary warns. The difficulty-conditioned MAP fit reports ~0 on the same traces, because it explains the ramp by the ramp. | Verified measurement under known synthetic truth (circular by construction — see Consequence), 2026-07-28; **reproduction path added and two corrections recorded 2026-07-30 — see Status** | **REPRODUCTION PATH (added 2026-07-30).** The sweep that produced the figures below was not committed and its parameters were not recorded, so the numbers could not be regenerated by anyone. `pnpm exam:block-harness -- --calibrate --guessing 0 --fit-guessing 0 --target-guessing 0` (`scripts/exam-learning-block-harness.ts`) is now the reproduction path. It is a **re-implementation from the published description, not a re-run of the original sweep**, and it agrees at the 30-trial cell within the tolerance it states: r 0.472 against 0.448 published, mean SE 0.047 against 0.047, attenuation slope 0.879 against 0.98. Parameters required to regenerate, all of them harness defaults and none of them recorded in the original entry: 400 children per cell; λ ~ N(0.06, 0.03²); **θ0 ~ N(10.5, 3²) clamped to [1,20]**; **handover-noise SD 1.5 scale points** (the engine's own `stabilitySd`, chosen because the entry says only "deliberately noisy"); `slope` 1.0; target offset +1; item pool a 0.5-point grid from 1 to 20 with 12 items per rung; selection through the shipped `selectNextNovelItem`; seed 20260730. Recovery, attenuation and SE figures originally from an uncommitted seeded simulation over `estimateLearningCurve` and `nextTargetTheta` in `packages/exam-scoring/src/learning-curve.ts`; the table is restated in that module's and `learning-rate-readout.ts`'s docblocks. Standing assertions: `packages/exam-scoring/src/learning-curve.test.ts` (attenuation at 8 vs 30, SE monotone in length, bit-for-bit replay), `packages/exam-scoring/src/learning-rate-readout.test.ts` (`confound guard` — the growth index fooled, worse with seed distance; the fit not fooled), `packages/exam-engine/src/learning-block.test.ts` (`confound guard` — the real engine's standing trace climbs monotonically for a constant-ability child). Runnable end to end as `pnpm exam:phase2-demo`. Builds on E-073, which reached the same qualitative conclusion in logit units on the retired engine | Recovering an injected climb from responses generated by that same climb is CIRCULAR: it establishes statistical power and estimator correctness under known truth, and is never evidence that a within-session climb measures real learning, learning potential, or program benefit. If the real spread between children is narrower than the smallest value modelled, no block of feasible length would order them at all and the metric should be withdrawn rather than lengthened. If the functional form is wrong — the fit assumes a LINEAR climb, while empirical learning curves are commonly exponential or power-shaped — the recovered slope is a summary of the wrong curve and these figures do not transfer | Measurement owner | **TWO CORRECTIONS RECORDED 2026-07-30, both material; the figures below should not be quoted without them.** (1) **The ladder is sensitive to a parameter the entry never recorded.** Across handover-noise SD 0, 0.5, 1, 1.5, 2 and 3, the 30-trial cell moves r = 0.515, 0.519, 0.597, 0.472, 0.491, 0.459 (`--noise-sweep`). The published 0.448 sits at the bottom of that range, so the missing parameter changes the result by more than the gap between the published figure and the reproduction, and the agreement above is therefore weaker evidence than it looks. Mean SE is stable across the same sweep (0.046–0.050), so the SE column is the robust one. (2) **Every cell assumed a child with NO guessing floor**, i.e. a constructed-response item; every item the block is administered from is five-option multiple choice. Re-run against a five-option responder with the floor correctly specified, the 30-trial cell is r = 0.254 / SE = 0.062 on the same grid and r = 0.249 / SE = 0.072 on `FLU-MATRIX-01` — roughly half the published recovery. E-200 records that measurement and supersedes this table as the description of the block as administered. Otherwise open, and open in a specific way: the reference distribution (mean and SD of lambda in scale points per trial) is UNKNOWN, with no K-8 data. `learningRateReadout` therefore returns `indeterminate` for any honest narrow reference at 30 trials, and that is the designed output rather than a gap. Ordering a measured cohort (`learningRateCohortRank`) is the comparative claim that survives at this length; placing an individual against a population is not. Closing this needs real per-child data at two or more occasions, which also bears on whether retakes should be revisited |
| E-096 | GT School's current admission rubric is multi-path: high CogAT; or two MAP screeners above the 95th percentile (CogAT may be ~90th); or a blended CogAT+MAP aggregate above the 90th percentile; or a 99th-percentile composite exceptional-ability waiver — with a hard 85th-percentile fall MAP reading gate for every student, excepting exceptional-cognitive (often ESL) applicants expected to catch up in reading. | Company claim (GT admissions-director interview) | Crystal Martel interview 2026-07-23 (Otter pt.2); confirm against GT admissions materials and forthcoming CogAT/MAP data; re-registered here from the forked lineage's `E-071` (D-032) | Screener validation could target the wrong operational rule or reading gate | Selection owner | Reported by GT; confirm against materials |
| E-097 | GT's selection target is thriving and accelerating on the Timeback platform, not giftedness alone: some gifted students (twice-exceptional, ESL, heavy-repetition needs, non-academic prodigies) do not fit the platform, and some students who are not conventionally gifted accelerate on it and "mimic giftedness." Giftedness is a necessary but not sufficient component. | Company claim / approved project direction (D-015) | Crystal Martel interview 2026-07-23 (Otter pt.1); ratified in D-015; re-registered here from the forked lineage's `E-072` (D-032) | The screener could optimize a criterion GT does not actually select for | Selection owner | Approved direction (D-015) |
| E-098 | The "speed" GT values is the rate of absorbing and adapting to new information (mastering a concept in ~1-2 exposures, then advancing) plus fast pattern/spatial recognition and relational connection-drawing — not raw response latency. | Company claim (GT admissions-director interview) | Crystal Martel interview 2026-07-23 (Otter pt.1); re-registered here from the forked lineage's `E-073` (D-032) | A screener could reward peak speed rather than learning rate, mis-ranking reflective high-ability children | Measurement owner | Reported by GT |
| E-099 | In GT's operational experience CogAT shows few false positives and some false negatives (especially in young children); a prior Mindprint-based admission produced students who struggled, so giftedness must be paired with predicted acceleration; CogAT remains GT's most trusted single measure. | Company claim (GT admissions-director interview) | Crystal Martel interview 2026-07-23 (Otter pt.1); relates to E-002; re-registered here from the forked lineage's `E-074` (D-032) | Dropping or over-trusting a single measure could reduce selection validity | Selection owner | Reported by GT operator experience |
| E-100 | GT can provide screener-validation data: dummy-testing its ~46 GT School students (a <=45-minute test), plus every student's CogAT and MAP screeners, plus historical CogAT score ranges from Riverside. | Company claim (GT commitment) | Crystal Martel interview 2026-07-23 (Otter pt.1); data delivery pending; re-registered here from the forked lineage's `E-075` (D-032) | Without this data the screener cannot be validated against GT's cohort; partially relaxes E-010's data-availability gap for validation | Evaluation owner | Committed by GT; delivery pending |
| E-101 | Current cohorts are ~40-46 students (physical GT School) and ~300 (GT Anywhere virtual), K-8; campuses open at >=20-30 committed students (cap ~100), with 3-4 new physical campuses targeted next year; the screener must scale to thousands of applicants. North-star: ~100,000 "MIT-ready by 8th grade" (leadership goal, expansion-dependent). | Company claim (current numbers reported; 100K is a leadership goal) | Crystal Martel interview 2026-07-23 (Otter pt.1); Joe Liemandt goal; re-registered here from the forked lineage's `E-076` (D-032) | Volume/scale assumptions could be mis-set; the 100K north-star must not be treated as a near-term deliverable | Operations owner | Reported by GT; 100K aspirational |
| E-102 | The requested deliverable is a tunable model plus facilities for GT admissions to tune the parameters/cut themselves (GT owns the cut), integrated into a unified "GT Universe" portal; algorithmic screening replaces per-applicant human review at scale, while a human shadow-day for behavioral fit remains. | Company claim / approved project direction (D-015) | Crystal Martel interview 2026-07-23 (Otter pt.1/pt.2); ratified in D-015; re-registered here from the forked lineage's `E-077` (D-032) | Building a locked, non-tunable, or fully-automated end-to-end system would not match GT's request | Product owner | Approved direction (D-015) |
| E-200 | **The Phase 2 learning-rate estimator manufactured a learning rate out of chance successes, and the mechanism is the adaptive targeting loop rather than the fit.** `estimateLearningCurve` shipped with `guessing = 0` while every item the novel block serves is five- (or four- or six-) option multiple choice. Measured over 400 simulated children per cell at 30 trials, all with **λ_true = 0 — no learning whatsoever** — against a responder with a real 0.2 floor: the shipped estimator fits **λ̄ = 0.0398 ± 0.0043** on an idealised 0.5-point grid with no bank involved, and **0.0547 ± 0.0049** on `FLU-MATRIX-01`; the same cohort at a zero responder floor fits **−0.0006 ± 0.0023**. 0.04 is two-thirds of the 0.06 the project uses as a plausible reference centre, and against the SD-0.15 reference (the only spread at which bands separate at this length) `learningRateReadout` calls **32.8%** of those non-learners `above` average pace, rising to **48.8%** on the bank. **Mechanism, isolated by measurement rather than argument:** freezing the served difficulty at `standing + 1` — which breaks the loop without touching the fit — drops the same misspecified fit to **0.0018 ± 0.0023** (6.0% false `above`), and with the floor also corrected to **−0.0034 ± 0.0029**. So the fit alone is approximately unbiased and mildly NEGATIVE, exactly as STAGE2_QUESTION_DESIGN §4.6 predicted; the positive sign is produced by the closed loop, because the inflated fit raises `theta0`, `nextTargetTheta` aims higher, and the fit then reads its own rising difficulty walk as a climb. **Remedy costs, same cohorts:** correcting the floor in the readout fit ONLY (targeting left misspecified) barely helps — λ̄ = 0.0338 — and pushes 48.5% of blocks to `indeterminate`; correcting it in both roles gives **λ̄ = 0.0098 ± 0.0033** on the grid and **0.0183 ± 0.0035** on the bank, false `above` 14.0% / 12.8%, r 0.178 → **0.254** / 0.149 → 0.249, mean SE 0.047 → 0.062 / 0.050 → 0.072. **The correction is necessary but not sufficient: the residual is 3 Monte-Carlo SEs from zero on an ideal grid and 5 on the wired bank.** Lengthening the block to 45 / 60 trials gives λ̄ = 0.0085 / 0.0041 and r 0.532 / 0.680 on the grid, but only 0.0199 / 0.0150 and r 0.324 / 0.398 on the bank. Declaring the measured residual as a `contaminationFloor` on the reference and testing separability against SE-plus-floor takes the SD-0.15 reference to **32.0% indeterminate at 30 trials on the grid and 94.5% on the bank**, 0.5% / 19.3% at 60 trials. **Against the SD-0.03 reference the project's own synthetic work used, every arm at every length from 30 to 60 is 100% indeterminate — before and after the fix.** Assuming a floor that is not there is also harmful and not symmetric: a declining child (λ = −0.35) answering without a floor is attenuated to −0.168 and its posterior SE doubles when the fit assumes 0.2. Mixed option counts are a real second-order exposure — `FLU-MATRIX-01` is 63 four-option, 27 five-option and 30 six-option items, and assuming 0.2 against a 0.25 truth raises the grid residual from 0.0098 to 0.0176. | Verified measurement under known synthetic truth (circular by construction, in the one direction that is not circular: it measures what the pipeline produces from a child who did nothing) | `pnpm exam:block-harness -- --guessing-probe` reproduces the defect; `-- --fix-probe` produces the full remedy table; `-- --calibrate --guessing 0.2` produces the corrected recovery ladder. Parameters as recorded on E-095, with the responder floor as stated per row. Standing assertions in `packages/exam-scoring/src/learning-curve.test.ts` (`the guessing floor is not misspecified` — a matched pair, one asserting the shipped default keeps a null cohort under 0.02 and one asserting a zero floor does NOT, so the guard cannot pass vacuously) and `learning-rate-readout.test.ts` (`the declared contamination floor`) | If the five-option floor is not the right lower asymptote for a real child on these items — plausible, since a plausible distractor set raises the effective option count and a child who disengages lowers it — then the correction is itself misspecified, in the direction shown above to attenuate declining children. The residual is measured on a born-synthetic bank with design-estimated difficulties and does not transfer to a calibrated bank. Nothing here says a within-session climb measures learning; it says the pipeline reports a climb for a child who had none | Measurement owner | **Open — the estimator fix is implemented and awaiting owner sign-off (D-200). The reportability conclusion is not fixed by it: on the bank the block would actually run on, a 30-item block does not support a reportable learning rate once the guessing floor is handled correctly.** |

| E-201 | **A Phase 1 battery spent one instruction screen per item, and metric-coverage pressure rather than burst eligibility was the cause. No per-type response-time data exists anywhere in this repository to price the trade in seconds.** An instruction screen is a served item whose type differs from the item before it, so it counts the times a child must read how to answer. Measured over 8 born-synthetic probabilistic children spanning the ability range in all four areas, grade band `4-5`, 40-item cap, selecting over the served INDEX with the app's own `EXAM_ENGINE_OVERRIDES` — not the engine defaults and not the research catalog's measurement lists, whose substitution is itself the reason bursting was demonstrated in simulation while never firing in a browser. **`dev` as shipped: 26.9 items, 26.9 instruction screens, 100% of items opening one.** Three independent causes, each individually sufficient: (1) the served index sets `content` to `{}`, so no item anywhere looked like a bounded choice and **0 of 48 types were burstable** whatever else was true; (2) the eligibility rule disqualified any type declaring `M-PATH`/`M-EFF`/`M-PLANFUL`/`M-IDEAFLU`; (3) `nextType` summed tracked-inert metric shortfalls without limit, making the type in each area that declares the most process measurements worth 4 points against its rivals' 2 — more than the 1.5-point age-band preference and 40× the tie-break jitter — so it won every selection in its area until its counts filled. That is why the child met exactly six types (`QUANT-MIX-01`, `SPA-XPLANE-01`, `GB-WORDFORGE-01`, `CX-check-01`, `GB-EXPLORE-01`, `VER-SEQUENCE-01`), five of them constructed-response with **zero** of their items offering an option list, so never burstable under any rule. **Arm-by-arm attribution, and it inverts the expected diagnosis: 19 types were ALREADY eligible and simply never selected.** Option count on the index alone: 25.4 screens (−6%) with the session 8% LONGER (29.1 items). Plus retiring the process-metric rule: 21.9 (−19%), 5 more types eligible. Plus capping tracked-inert coverage at one metric's worth: **15.6 screens (−42%), 24.0 items (range 19–28), no session on the cap** — so the cap is 24 of the 42 points, and the only change that also shortens the battery. Caps of 2 and 3 give −28% and −5% with **every** session running to the 40-item safety cap. **Costs, measured:** distinct types per area 4.3 → 3.2 and 24 → 15 of 48 wired types across the cohort — arithmetic, not a side effect, since instruction screens ARE the count of type-runs and distinct types cannot exceed runs. Bursting also let one area draw every item from a single type (observed in the app's battery test), addressed by requiring 2 types per area in the stop rule. **Counterintuitive and load-bearing before anyone raises the ceiling:** a burst of `n` commits the session to ~`4n` items under `coverageIsEven`, so a fixed 6-ceiling put the exits at 24 then 48 items and ran a 40-cap battery to the safety net; bounding length by remaining budget gives both fewer screens AND a shorter session, at an effective ceiling of 4–5. | Verified measurement of routing behaviour over born-synthetic uncalibrated banks (`validated=false`); the "too instantaneous" report that prompted it is a single subject's impression of one session | `pnpm exam:instruction-cost` (`scripts/exam-instruction-cost.ts`) reproduces every arm; method and claim boundaries in `docs/product/EXAM_BURST_INSTRUCTION_COST.md`; decision D-201. **Absence of response-time data verified on every path, 2026-07-30:** `app.exam_item_response` in the local Postgres holds **0 rows** (0 sessions, 0 telemetry events, 0 outcomes); `research/exam-question-types/measurements.json` specifies `M-RT`/`M-RTFIRST` and contains no observations; `packages/exam-engine/src/testing/real-bank.ts` simulates `M-RTFIRST` as a flat `0.35 × M-RT` for every type, a constant ratio that cannot distinguish types; `METRICS_BASIS.md` wires both at scoring weight 0. Standing assertions in `packages/exam-engine/src/burst.test.ts` and `apps/web/src/lib/exam/adaptive.test.ts`, the latter driving `planNextSelection` over the served index so the loop the runner runs is the loop under test | An instruction screen is COUNTED, never priced: if a screen is cheap relative to a question then 42% fewer screens is a smaller improvement than it reads, and if construct variety matters more than the owner's stated objective then the 4.3 → 3.2 narrowing is a loss rather than a cost. Burst eligibility claims only that consecutive items are each a single unpaced choice; if answering one of them is in fact slow, a burst is merely less beneficial, not harmful. Nothing here bears on score validity, reliability or fairness | Measurement owner | **Open — implemented, awaiting owner sign-off (D-201). Which types are "fast" remains unmeasured; the collection that would settle it is specified in `EXAM_BURST_INSTRUCTION_COST.md` §5 and needs one persisted session plus one query, no code change.** |
| E-202 | **Every Phase 1 sitting served the same question types in the same order, and the fixed seed was only one of three causes.** `DEFAULT_CONFIG.seed` was the constant `0xc0ffee` and nothing overrode it, so every selection draw was identical in every sitting; the type choice was a strict argmax whose only variation was a ±0.1 jitter, too small to outweigh a metric weight (1 or 2) or the age bonus (1.5); and nothing discouraged returning to a type just served. Measured over 20 differently-seeded sittings on the REAL wired bank with the ENGINE defaults and the research catalog's measurement lists: **6 of 49 distinct types reached before, 23 after; worst within-session repeat over 20 items 5.0 → 3.1.** On the wired bank the order was literally a four-type cycle (`VER-POLYSEME-01 > FLU-LADDER-01 > SPA-XPLANE-01 > QUANT-GRAPH-01 > ...`). **Attribution:** the seed alone fixes variety BETWEEN children and leaves within-session repetition completely unchanged (5.0 → 5.0); all three parts were needed. **Two constraints found by measurement rather than reasoning, both pinned by tests.** `itemSelectionTolerance` must stay strictly below `ageBandBias`: a non-matching item is charged exactly `ageBandBias`, so a tolerance of 0.5 makes a band-matching and a non-matching item at the same difficulty interchangeable and silently cancels D-025; set to 0.25. The recency discount must not apply while an area is still homing: applied throughout it cost a distant-seeded child 4 items and broke D-023's convergence budget (52 against 48), while after the estimate straddles the same discount is free. | Verified measurement of routing behaviour over born-synthetic uncalibrated banks (`validated=false`) | `packages/exam-engine/src/selection-variety.test.ts` over `loadRealBanks()`; decision D-202. Superseded in part by E-203, which re-measures every figure here against the configuration a browser actually runs and finds the fixed-rotation diagnosis does NOT reproduce there | If a child's experience of repetition matters less than targeting precision, then the tolerances are a cost rather than a benefit: each one serves a less well-targeted item on purpose. Nothing here bears on score validity, reliability or fairness | Measurement owner | **Open — implemented, awaiting owner sign-off (D-202). The published figures are taken against the ENGINE defaults and the research catalog, not against the browser configuration; see E-203** |
| E-203 | **Capping tracked-inert coverage (D-201) and adding exposure control (D-202) are COMPLEMENTARY rather than mutually cancelling, but only after the type tolerance is re-derived against the capped scores, and the combination gives back half of D-201's stop-rule reliability.** Four arms at matched settings — same abilities, responder seeds, engine seeds, grade band and 40-item cap — over 80 differently-seeded sittings on the REAL wired bank, selecting over the served INDEX with the app's own `EXAM_ENGINE_OVERRIDES`, i.e. the configuration a child actually sits. **Instruction screens per session: `dev` 28.6, D-201 alone 18.5, D-202 alone 28.0, combined 19.7.** **Distinct types per session: `dev` 18.1, D-201 alone 14.9, D-202 alone 19.8, combined 17.1** — so D-201 alone costs 3.2 types per session and adding D-202 recovers 2.2 of them, at a cost of 1.2 instruction screens. **Worst return-to-type over 20 items (a burst of four counted as one arrival): `dev` 3.4, D-201 alone 1.5, D-202 alone 3.1, combined 1.4** — the lowest of the four arms. **Enforced metric coverage completed in 80 of 80 sittings in every arm.** **The residual cost: 15 of 80 sittings reach the 40-item safety cap in the combination, against 10 for D-201 alone and 17 for `dev`** — so the combination is better than `dev` and worse than D-201 alone on the signature of a battery becoming fixed-length. **The retune, and its mechanism.** `typeSelectionTolerance` was derived at 1.0 against the UNCAPPED coverage sum, where a type could earn four tracked-metric points; capping the tracked total at one point compressed the score spread, making the same tolerance proportionally a much wider net. At 1.0 exactly, a type holding a tracked shortfall and one holding none fall within tolerance of each other, and a type closing an ENFORCED shortfall (weight 2) falls within tolerance of a rival closing none but holding the full capped tracked gain (2 − 1 = 1.0). Swept over 80 sittings: **1.0 costs 1.9 items per session against 0.5 and buys 0.3 of a distinct type**, so the tolerance moved to 0.5 and the constraint is now stated as `typeSelectionTolerance < trackedCoverageCap`. Below about 1/3 the knob does nothing at all, because no score gap is narrower than the smallest recency step. `typeRecencyPenalty` 1.0, `typeRecencyWindow` 3 and `itemSelectionTolerance` 0.25 were swept on the same grid and are at their measured optimum; they are unchanged. **E-202's fixed-rotation diagnosis does not reproduce under the browser configuration.** With a per-session seed, `dev`'s own rule already reaches 47 of 48 wired types across 20 sittings there, because the generated registry gives types much flatter metric lists than the research catalog does and the argmax therefore ties far more often. E-202's 6-of-49 is a fact about the ENGINE defaults. What a cohort meets today, on the constant seed actually shipped, is 24 distinct types across 20 sittings. **A pinned constraint went slack.** D-202's homing guard was held by D-023's convergence budget; with the coverage cap in place the worst battery runs 47 items against a budget of 48 whether the guard is present or removed, so that test would no longer catch its removal. Re-pinned directly on the served type order, and the new assertions were verified to fail when the guard is removed. The `itemSelectionTolerance < ageBandBias` pin was verified to still fail when the tolerance is raised to 0.5. | Verified measurement of routing behaviour over born-synthetic probabilistic children and uncalibrated banks (`validated=false`) | `pnpm exam:selection-integration` (`scripts/exam-selection-integration.ts`, `--sweep` for the knob grid, `--sessions=N` for the cohort size) reproduces every figure; method and claim boundaries in `docs/product/EXAM_SELECTION_INTEGRATION.md`; decision D-203. Shares `scripts/exam-selection-harness.ts` with `pnpm exam:instruction-cost`, so the two measurements cannot drift apart | If the residual cap pressure matters more than construct breadth, the combination is worse than D-201 alone and the tolerances should go to zero. Instruction screens are COUNTED, never priced in seconds — no per-type response-time data exists in this repository (E-201). Nothing here bears on score validity, reliability or fairness | Measurement owner | **Open — implemented, awaiting owner sign-off (D-203), which inherits the sign-off D-201 and D-202 are each still awaiting. The 40-item cap pressure the combination adds is unresolved: 15 of 80 against 10** |
| E-204 | **Stage 1 selection never read its own uncertainty, and the standing level it reported was about a point and a half above the child. Correcting the chance-success floor and selecting on a live belief fixes the bias and what the child is handed, and costs a little precision per item.** Four arms at matched settings — same abilities, responder seeds, engine seeds, grade band and 40-item cap — over 60 differently-seeded probabilistic sittings on the REAL wired bank under the app's own `EXAM_ENGINE_OVERRIDES`. **`staircase` (as shipped) against `mepv` (proposed): reported standing bias +1.577 → +0.017 and RMSE 2.352 → 1.250; refitted ability RMSE 2.047 → 1.300; sessions concluding on evidence 52/60 → 58/60 with safety-cap hits 12 → 2; distinct types per session 17.6 → 18.0 and per area 4.39 → 4.50.** **What the child is handed, which is the BrainLift's actual claim and was never previously measured: items at least 3 scale points above them 21.4% → 7.6%, and the worst run of consecutive misses within an area 4.08 → 2.22.** **The cost is precision per item, in the opposite direction to the intuition that motivated the change:** mean final SE 1.235 → 1.286, items to an SE of 1.50 5.1 → 5.6, to 1.25 6.6 → 7.0, and areas ever reaching 1.25 159/240 → 147/240. The corrected aim deliberately serves items EASIER than the child — with a floor `c`, information peaks at `P = (1+sqrt(1+8c))/4`, about 0.653 for a five-option item, roughly 0.27 scale points below ability (Birnbaum 1968; Lord 1980; Magis 2013) — so the gain is accuracy, not speed to a stated precision. **`mfi` (maximum information at the belief mean) was measured, not assumed away, and it wins the obvious metric while losing the one that matters:** 6.4 items to an SE of 1.25 against `mepv`'s 7.0, 195/240 areas reaching it against 147, mean SE 1.138 against 1.286 — but refitted RMSE 1.740 against 1.300, so it reports ±1.14 while erring ±1.74 where `mepv` reports 1.29 and errs 1.30. **The SE comparison is partly circular in `mfi`'s favour and is reported as such:** `mfi` maximises information at the estimate and the SE is one over the root of exactly that information, so scoring the two criteria by SE scores `mfi` on its own objective, while RMSE against a planted ability favours neither by construction. This is a partial reproduction of Chen/Ankenmann/Chang and Choi/Swartz — elaborate criteria add little on PRECISION — and a non-reproduction on accuracy. **`cut` (maximum information at a decision point) reproduces Spray & Reckase and is not adopted:** against an ILLUSTRATIVE cut of 15 (no real GT threshold exists in this repository and none was invented) it reaches a 95% above/below call in 2.3 items across 223/240 areas, against `mepv`'s 3.0 across 183/240 — and pays with an ability estimate worthless away from the cut (refitted RMSE 3.403, mean SE 3.199, area SE spread 4.038 against 0.528), with the child's experience (47.8% of items at least 3 points above them, worst miss run 5.42), and with exposure control: **265 distinct bank items against 1,329**, the most-served item reaching 28% of sittings against 13%. **Two negative results worth as much as the positive ones.** Capping the ENFORCED coverage sum so diversity could outrank it changes nothing at all — identical results at caps of 2, 4, 6 and none — because no wired type ever earns more than one enforced gap's weight, so the enforced sum D-201 deliberately left free was never the binding constraint on diversity; the knob was reverted rather than shipped inert. And `areaSpreadSlack` of 1 is inert under the portal's `evenSpreadTolerance` of 1 while changing behaviour under the engine's own defaults, where it caused a within-session type-repeat regression; it ships at 0, so uncertainty breaks ties among the least-served areas and even spread is unchanged by construction. **The floor is harmful when wrong in either direction and is pinned in both.** Against a guessing child, assuming none inflates the reported level (+1.32 at floor 0, +0.35 at 0.1, +0.12 at 0.2, −0.06 at 0.25, −0.46 at 0.35); against a child who does NOT guess, assuming one attenuates them, and for a FIXED trace a floor can only move an estimate down, since `P(wrong) = (1−c)(1−s)` and the `(1−c)` cancels in the likelihood ratio — a miss carries identical information under any floor while a pass carries less. | Verified measurement of routing and RECOVERY behaviour over born-synthetic probabilistic children and uncalibrated banks (`syntheticOnly = true`, `validated = false`) | `pnpm exam:selection-diversity` (`scripts/exam-selection-diversity.ts`; `--sessions=N`, `--floor-sweep`, `--knob-sweep`) reproduces every figure, sharing `scripts/exam-selection-harness.ts` with `exam:instruction-cost` and `exam:selection-integration` so the three cannot drift apart. Standing assertions: `packages/exam-engine/src/posterior.test.ts` (the information peak against the closed form, the refutation of endpoint targeting as arithmetic, and that a belief will not claim a scale bound from a one-sided trace), `real-bank.test.ts` (`the chance-success floor, in both directions` — a matched pair, so the guard cannot pass vacuously), `packages/exam-scoring/src/ability.test.ts` (the same pair on the reporting fit, plus that omitting the knob reproduces the previous fit exactly). Decision D-204. Builds on `docs/architecture/ITEM_SELECTION_BRACKETING_ANALYSIS.md`, whose +1.44 bias estimate this reproduces and corrects | The 0.2 floor, the slope of 1.0 and the prior SD of 6.0 are DESIGN ASSUMPTIONS on an uncalibrated born-synthetic bank, not calibrated parameters; an estimated `c` is typically below the reciprocal of the option count, so 0.653 is an upper anchor rather than a constant, and the wired banks mix four-, five- and six-option items. Recovery under a simulated responder is not evidence about score validity, reliability, fairness or giftedness — it says the machinery can read back an ability the trace was generated from, and nothing more. The `cut` figures are shaped entirely by an illustrative threshold and must never be read as how a real gate would perform. Every standing level already shown to anyone moves downward under this change | Measurement owner | **Supported — implemented, and D-204 approved by the owner 2026-07-31. The product question this measurement was meant to inform is now settled: Stage 1 is a LOCATOR, not a gate — a per-area standing level that scales Stage 2, where precision away from any cut is the product (D-205, decided on arm four of this measurement). `cut` selection remains a supported policy value carrying the price recorded above, so a later decision to run a gate is a configuration change rather than a rebuild.** |
| E-400 | GT does not currently ration seats, so the oversubscription a randomized admission design requires does not exist today. The virtual program (~300 students) admits essentially all applicants — "for GT Anywhere right now, they just let everybody in" — and physical capacity is demand-limited rather than seat-limited: campuses open once 20–30 committed students exist, hold up to ~100, and leadership "can exponentially open ten campuses tomorrow if we had enough interest." | Company claim (GT admissions-director interview), bearing directly on E-010 | Crystal Martel interview 2026-07-23 (Otter pt.1 ~27:46–29:45, pt.2 ~4:23–5:10); `docs/interviews/CRYSTAL_MARTEL_INTERVIEW_NOTES.md` L430–444, L504–511. Confirm against GT applicant/offer/enrolment counts when available | If scarcity in fact exists (or returns as demand grows), the randomized route to R2 becomes available again and D-400's demotion of R2 loses its strongest evidential support. Conversely, treating this as permanent would wrongly foreclose a design GT may later be able to host | Operations and evaluation owners | Reported by GT for the current cycle; contradicts the open assumption in E-010, which is downgraded accordingly |
| E-401 | GT already operates its own admissions application: an applicant-facing portal plus a reviewer/admissions dashboard aggregating candidates, scores and stats, built for the admissions director by an outside team and live on a non-public page. Asked whether the in-house test could be integrated into that environment, the director answered "100% — that is the goal … that is exactly what we want to be doing," and offered to share the tech stack so the team could match it. | Company claim (GT admissions-director interview) | Crystal Martel interview 2026-07-23 (Otter pt.1 ~2:07–5:20); `docs/interviews/CRYSTAL_MARTEL_INTERVIEW_NOTES.md` L170–176, L200–219, L204–211 | If GT's portal is in fact unusable, abandoned, or unavailable to us, the applicant-facing surfaces D-400 places out of scope would have to be rebuilt, and the integration seam (UI-05) would carry weight it cannot bear alone | Product owner | Reported by GT; portal walkthrough and tech-stack details were promised and are still pending delivery |
| E-402 | A custom in-house test is what GT asked the team to build. The admissions director raised it unprompted — "my goal … was to see what I could build out with the team regarding custom tests" — and specified that automated decisioning is a requirement rather than a preference: at "thousands and thousands of children," per-applicant human review is "impractical," so "the test we create will need an algorithm to make that decision — the instant cutoff," and "the algorithm needs to be pretty darn accurate." | Company claim (GT admissions-director interview) | Crystal Martel interview 2026-07-23 (Otter pt.1 ~5:25, pt.2 ~1:51–4:00); `docs/interviews/CRYSTAL_MARTEL_INTERVIEW_NOTES.md` L224–226, L483–493. Complements E-102 (tunable deliverable) and E-101 (scale) | If the screener is not the actual ask, D-400's primary aim is mis-set and the project is optimizing the wrong deliverable | Product owner | Reported by GT, consistently across both interview parts |
| E-403 | No program-outcome measurement window exists inside the capstone. The internship's final day is ~2026-08-14 and GT's school year begins 2026-08-12, which the admissions director noted directly: "testing is going to be tough. We don't even start school until August 12th." Two days of instruction cannot supply the pre-program baseline, fixed horizon, or attrition handling R6 requires. | Verified scheduling constraint | Crystal Martel interview 2026-07-23 (Otter pt.2 ~6:14–6:51); `docs/interviews/CRYSTAL_MARTEL_INTERVIEW_NOTES.md` L522–535 | If the engagement extends past the school year's start, a growth-measurement horizon becomes possible and R6's operational infeasibility argument weakens (its non-binding status would then rest on D-400 alone) | Team lead | Verified for the current engagement |
| E-404 | The GT admissions-director interview contains no request for program-effect evaluation. Across both transcripts the terms "program effect," "causal," "counterfactual," "lottery," "random," "control group," "comparison group," and "impact" appear zero times; the two references to validation are both to criterion validity of the screener against CogAT and MAP. This is an absence, not a refusal: the director did not decline program-effect work, she was not asked about it and did not raise it. | Reasoned inference from a bounded source — NOT a company claim | Reproducible with `grep -ci` against `docs/interviews/CRYSTAL_MARTEL_INTERVIEW_NOTES.md`; validation references at L92 and L392 | Over-read, this becomes "GT does not want program-effect work," which one 41-minute conversation with one stakeholder cannot establish — other GT stakeholders (leadership, the Timeback team, an evaluator) were not asked. D-400's demotion of R2/R3/R6 must therefore rest on E-400 and E-403, not on this entry | Team lead | Open. Verify against GT leadership before treating the absence as an institutional position |

## Update procedure

When adding or changing an entry:

1. Cite the exact source or validation artifact.
2. Record whether the claim describes the current GT context or general research.
3. State what downstream requirement, decision, or work item depends on it.
4. Update the traceability matrix when the evidence changes product scope or acceptance.
5. Add a decision-log entry if the change alters a requirement or chosen direction.

## Evidence-use rules

- Company claims may define what is being tested, not prove that it is true.
- General research does not establish GT-specific feasibility.
- Absence of public evidence is not evidence that no private evidence exists.
- Correlation and predictive validity do not establish program impact.
- Weak or unresolved evidence must narrow the claim, not be hidden.
